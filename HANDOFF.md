# Handoff — Top Shelf (umbrella: business audit)

> Save-state read FIRST on resume. GitHub holds the code; this holds the thinking.
> **This is the canonical handoff for the Top Shelf BUSINESS AUDIT** (registry `vault_slug: top-shelf`, covering CRM + Website + Outreach together).
> `top-shelf-crm.md` and `top-shelf-website.md` are now **secondary archives** (CRM engineering log, website log). They are no longer generated. Read this file first; write handoffs here.

**Last updated:** 2026-09-17 · **By:** Claude Code (Opus 4.8, Ledger Pro session, bound Top Shelf) · **Continuing on:** Taylor OS or Desktop, project "Top Shelf"

**✅ PRE-SEO-LANDING PUNCH-LIST — #5 CITATIONS + #11 THIN PAGES SHIPPED (2026-09-17, Opus 4.8, Ledger Pro session bound Top Shelf). START HERE (newest).**
Executed the owner-approved batch from the CEO+client site review. All LIVE on Hostinger + mirrored to Drive.
- **Cursive plan labels / #13 meta / #17 audit form** — DONE earlier this session (cursive "on the Signature/Essentials Plan" on 20 Owner's-Words cards; 10 pages' meta ≤158 chars; contact.html now REQUIRES a Google Business Profile field + a "what your audit covers" section framing the free audit as an info-for-report exchange).
- **#5 stat citations — DONE + LIVE (commits 150b9c2, 02fc92f).** Every headline stat sitewide now carries a linked reputable source via `.pain-src`; every URL HEAD-checked. Corrected figures to match sources: homepage $49K→$126K & 57→68%; marketing 57→68%; pos 65→66%; websites-seo 88→76% ("within a day" = Google's real figure, not 88%/24h). Dropped 2 unsourced claims (ai-phone "62% call competitor"; marketing "~1% unprompted"). Repaired 4 malformed citation hrefs (booking, reviews). Homepage 73% reworded to "never contacted at all" (kixie's actual claim). Sources: getaira, kixie, InsideSales/MIT, Invesp, BrightLocal, SOCi, SGB, Safari Digital, Wrise, Google, Think with Google.
- **#11 thin pages — DONE + LIVE, Judge PASS (commit 3252206).** payments/booking/reviews were ~1,030 words vs strong pages ~1,800. Added the strong-page "Before & After, In Plain English" section (7 industries/side) + grew How-it-Works 2→5 feature-rows each → ~1,550 words, structural parity. New rows are honest operational copy, deliberately stat-free (no fabrication), em-dash-free, voice-matched.
- **OPEN (owner action / decision):** (1) **#6 branded CRM subdomain** crm.topshelfsolutions.io — needs owner's Hostinger DNS CNAME → Railway. (2) The **"Gap Finder" quiz modal auto-pops immediately** on page load, covering the hero — offered to delay it to scroll/exit-intent; owner to decide. (3) **Verify the CRM backend captures the new `gbp` field** (`top-shelf-production…/api/website-lead`) — backend is a separate Railway service, not in the static/showcase repos; NOW DONE + deployed. The gbp field was discarded client+server; fixed contact.html payload (a8e2185) AND the backend (validation + owner email + SMS, taylor-os/top-shelf commit f8e25a5), deploy verified live via a side-effect-free probe. NOTE: `top-shelf` GitHub repo was RENAMED to `top-shelf-crm` (same repo, one backend); Railway follows the rename.
- **Non-blocking legacy:** pre-existing em dashes still live in site chrome (hero eyebrows, comparison table, CTA, one booking 40%-stat line) across ALL pages incl. the strong ones — a separate global copy pass if a fully em-dash-free site is wanted (Judge flagged, out of #11 scope).

**🔒 HIPAA / BAA — ADVERTISED NOW, BUILD PER-CLIENT ON REQUEST (owner decision 2026-09-16).**
Owner decided: advertise HIPAA/BAA on the dental/medical page and target the search, but do NOT build the compliance program proactively. Kick off the build only when a client wants it. Advertised copy is LIVE on `industry-medical-dental.html` (H2 "HIPAA-Compliant Setup, BAA Included" + title/meta/Service-schema all targeting HIPAA/BAA). Owner is comfortable telling a client "compliant setup + signed BAA within ~30-45 days of kickoff."
- **Realistic build time once triggered: 3-6 weeks** (aggressive ~2-3 wk; up to ~8 wk if the CRM host migration is heavy). Long pole = moving the CRM's PHI off Railway to a BAA-backed host (AWS/GCP/Aptible) + adding PHI access-logging; plus attorney BAA review (start that early).
- **Owner's chosen simplification: keep AI OUT of the PHI path** (removes the no-BAA-on-Claude-subscription blocker, keeps the no-metered-API rule). Does NOT by itself make them compliant.
- **Checklist when a client says yes (from the legal-compliance-guardian brief, this session):** (1) Security Risk Analysis + written policies + named security officer + training + breach procedures — fastest via a HIPAA platform (Accountable / Compliancy Group / Thoropass). (2) Sign the Telnyx BAA, scoped to recordings/transcripts. (3) Move PHI CRM off Railway to a BAA-backed host; add encryption / MFA / least-privilege / audit-logging. (4) HIPAA email (Paubox / LuxSci / Workspace-BAA) or keep PHI out of email. (5) Cyber/E&O insurance with a HIPAA rider. (6) TX HB 300 (likely a direct covered entity: 90-day customized training, TX AG breach notice at 250+ residents). (7) Healthcare-privacy ATTORNEY sign-off on the client + vendor BAAs — non-negotiable. Recommend carving a separate "Top Shelf Health" offering to isolate liability. NOT legal advice.
- **Deferred scoping question (owner did not want it done pre-client):** can the custom Laravel CRM move off Railway to a BAA-backed host cleanly? That answer sets the true floor on the timeline.

**🖥️ CRM DEMO WIRED INTO THE MAIN SITE + SIGNATURE-GOLD RE-THEME LIVE — SHIPPED (2026-09-16, Opus 4.8, Ledger Pro session bound Top Shelf).**
Owner: "ship it, make sure there is a new Main navigation tab that says CRM and thats where the demo CRM lives."
- **Main-nav CRM tab — DONE + PUSHED (commit `58f87ed`).** Added a `CRM` link after "Why Us" in the primary nav across all 28 live pages in `top-shelf-website`, pointing at the live showcase `https://tap-crm-showcase-production.up.railway.app/8`, opening in a new tab so the marketing site stays anchored. The mobile drawer picks it up automatically (site.js builds the panel from `.nav-links`). Verified desktop + mobile in-browser.
- **Drawer wiring also pushed** (prior local commits `fda8a69` + `c12d7fd`): the homepage service drawer + solution pages iframe the live phone demos from the showcase `/e/<key>` embed routes (EMBED_BASE + ANIM map in `index.html`).
- **Deploy:** the Hostinger GitHub App auto-deploys `main` to `public_html` (~15s), so the push IS the deploy — topshelfsolutions.io updates on its own.
- **Signature-Gold re-theme is LIVE on the Railway showcase**: all 10 industry phone embeds + the /8 CRM page renders + 6 repainted screenshots now use gold `#C9A86A` app chrome (via `--embed-accent` / `--embed-accent-text` / `--embed-band` CSS vars in `Embed.tsx`; PhoneWalkthrough defaults are gold so /8 goes gold too). CEO (Business Strategist) + client (Persona Walkthrough Specialist) both endorsed; 5 polish fixes applied.
- **OPEN (owner to pick, offered in chat):** the CRM tab points at the raw Railway URL. Clean upgrade = a branded `crm.topshelfsolutions.io` (Railway custom domain + DNS CNAME) so the address bar stays on-brand. Not blocking.

**🧰 HOME-SERVICES KEYWORD BANK (full long tail, Tier-1 trades) — DELIVERED, Judge PASS (2026-09-10, Opus 4.8, Ledger Pro session bound Top Shelf). Research/build-spec — NO site code changed. Third doc in the set (playbook → target map → THIS).**
Owner agreed to "pull the full long tail" before building pages + "I don't mind standalone pages to maximize exposure." Pulled the real keyword universe for the 5 Tier-1 home-services trades and organized it into a BUILD SPEC (cluster = page, primary term = H1).
- **Method (reuses the Railway-cred path):** `keyword_suggestions/live` (US), 20 pulls = marketing/seo/leads/website seed × 5 trades → ~640 raw kws → curated (noise stripped, synonyms collapsed). Script `fetch_longtail.py` + raw `longtail.json` in THIS session's scratchpad (temp).
- **Deliverable:** PRIVATE Artifact **https://claude.ai/code/artifact/e27c73fa-07a7-4694-925d-75f78cc2c999** (companion to playbook 718cce20 + target-map a9fa3ecb). Judge verified ALL 55 rows vs longtail.json (0 errors, 0 fabrications) → PASS (1st); 1 cosmetic fix (electrical leads wording).
- **CLEANEST WINS (build first):** `roofing seo` 2,400/**KD4** (flagship) + `seo for roofing companies` 1,000 · `seo for electricians` 720/**KD0** + `electrician website` 390/KD0 · `hvac leads` 880/KD0 · `hvac marketing agency` 880/KD11 · `local seo for plumbers` 590/**KD0** + `plumber website design` 480/KD0.
- **3 cross-trade shortcuts:** (1) `local seo for [trade]` recurs at KD0 = Top Shelf's local-SEO service; (2) the modifier variant beats the head (`roofing marketing agency` KD4 vs `roofing marketing` KD19; `digital marketing for plumber` KD0 vs `plumber marketing` KD22); (3) SEO is the biggest/lowest-KD door in every trade — lead each trade with its SEO page.
- **Trade calls:** Garage Door = skip a website page (opener-brand noise: genie/stanley/precision), build lean + lean on GDR proof; Electrical = skip a Leads page (cluster too thin). Plumbing marketing head is KD22 → lead with SEO+Website.
- **RECOMMENDED FIRST BUILD:** Roofing SEO + Electrician SEO pages, prove they index/rank, then roll the cluster pattern + multiply by city (doorway-safe).
- **NEXT (owner to pick — offered in-chat):** (1) draft the Roofing SEO + Electrician SEO page TEMPLATES (title/H1/outline/long-tail/FAQ from KD0 question terms/Service+FAQPage schema); (2) pull the same long tail for the Auto trades (repair/dealership/tire). Nothing pending on me.

**🎯 INDUSTRY-PRIORITY + HOME-SERVICES KEYWORD MAP — DELIVERED, Judge PASS (2026-09-10, Opus 4.8, Ledger Pro session bound Top Shelf). Research deliverable — NO site code changed. Follows the SEO playbook below.**
Owner: research all 8 site industries → rank the easiest exposure wins before building; and go DEEP on home services, keywording all the specific trades. Grounded in **LIVE DataForSEO data** (not eyeballed).
- **HOW THE DATA WAS PULLED (reuse this — desktop DataForSEO MCP is UNwired):** creds live in the **Taylor OS Railway project** env (`DATAFORSEO_USERNAME`/`DATAFORSEO_PASSWORD`); from `C:\Users\taylo\Agentic-OS`, `railway variables --kv | grep DATAFORSEO...` → curl `https://api.dataforseo.com/v3/dataforseo_labs/google/keyword_overview/live` (Basic auth, location_code 2840, language_code en). ⚠ curl writes to `/c/...` (mingw) but **Windows python needs `C:/...`** to read the file back. Payloads + parser saved in THIS session's scratchpad (temp — not durable).
- **Deliverable:** viewable HTML → **PRIVATE Artifact https://claude.ai/code/artifact/a9fa3ecb-2863-487f-9137-0c14702081dc** (companion to the playbook artifact 718cce20). Judge traced EVERY cell to source JSON, PASS (1st); 2 cosmetic fixes applied (salon KD 0–22, fencing CPC attribution).
- **INDUSTRY VERDICT (data-confirmed):** **Home Services #1 + Auto #2 win on all 3 axes at once** (low KD 3–21 · high CPC $33–80 · only two with real proof GDR/TAP/Reids). Wave 2 = Med-Spa/Salon (KD0, $77 CPC sleeper) then Dental (vol 1,900 but "dentist marketing agency" KD76 + no proof). Opportunistic = Restaurants (low-ticket). Long game = Legal (CPC $112–164 but hardest, sharky). Deprioritize = Retail (easy KD but $12–15 value). "Your Business" = catch-all, don't SEO-target (digital marketing agency 49,500 vol / KD75 = unwinnable). Pure-KD-easiest would be Salon/Retail — but weighted by value+proof, the prior Home-Services+Auto call is correct.
- **HOME SERVICES TIER 1 (build first):** Roofing, HVAC, Plumbing, Electrical, Garage Door. **Standouts:** `roofing seo` 2,400/KD4 (best on the board), `roofing leads` 1,300/KD0, `hvac leads` 880/KD0, `pest control seo` 880/KD0, `seo for electricians` 720/KD0, `seo for plumbers` 720/KD13. Garage Door is Tier 1 DESPITE modest volume because GDR = real case study. Tier 2 = Pest Control/Solar/Landscaping/Painting/Restoration/Cleaning. Tier 3 = the rest (colony). No-data (national): septic/chimney/drywall → local-colony only. All 25 trades tiered in the doc.
- **KEY INSIGHT:** national agency-term volume is SMALL (100–900) → real traffic is local×trade + AEO, not the national head (confirms the playbook thesis).
- **NEXT (owner to pick — offered in-chat):** (1) pull the FULL long tail per Tier-1 trade (`keyword_suggestions`/`related_keywords`, roofing+HVAC first) so pages target real searches; (2) draft the Tier-1 trade-page templates (title/H1/FAQ/schema). Nothing pending on me.

**🔍 SEARCH-EXPOSURE PLAYBOOK (SEO+AEO) FOR THE SITE — DELIVERED, Judge PASS (2026-09-10, Opus 4.8, Ledger Pro session bound Top Shelf). Guidance deliverable — NO site code changed.**
Owner is mid-build on the FULL agency site (explicitly NOT a GBP/map-pack play) and asked for a comprehensive audit of how to get max search + AI-answer exposure + clicks — "every trick," standalone keyword pages included — to build from himself.
- **Confirmed forks (AskUserQuestion):** National (US) reach · SEO center-of-gravity = BOTH but weighted to AGENCY services (software/comparison = long-tail support, never head-on) · build **Home Services + Auto** deepest first (only verticals with real, nameable proof: GDR/TAP/Reids).
- **Delivered:** a 13-section viewable HTML playbook, published as a **PRIVATE Artifact → https://claude.ai/code/artifact/718cce20-b393-4345-8bc5-8cd539d08821** (source in THIS session's scratchpad only; G: not mounted so NOT filed to Drive — re-file to `Projects/Top Shelf/SEO/` when G: is up). **Judge PASS (1st)**; Judge independently re-verified pricing / GDR-pos-47.3 / NAP against the vault.
- **The thesis (baked into the doc):** a new domain can't out-authority incumbents (Thrive / Blue Corona / Autoshop Solutions) this year — win on **depth + AEO-citability + the "you own it, no lock-in" wedge**. Three fronts: organic (slow compounder), **AEO (biggest near-term door — a new domain gets cited in weeks)**, long-tail capture. Build **feature terms first** (AI receptionist / missed-call text-back = already-shipped features) + **[service]×[industry] intersections**; defer broad "[industry] marketing agency" heads; NEVER chase "gohighlevel alternative" head-on (SaaS-listicle wall + wrong frame for an own-it agency).
- **Grounding honesty:** DataForSEO/GSC are cloud-only (unwired on desktop) → REFUSED to fabricate volume/KD; winnability tags = LIVE WebSearch SERP recon (directional, pull real numbers before a build wave). Doorway-safe city colony anchored to GDR's OWN ~200-page / pos-47.3 failure.
- **⚠️ MOST URGENT for the owner:** verify Cloudflare is NOT blocking AI crawlers (GPTBot/ClaudeBot/PerplexityBot/Google-Extended) — if it is, the entire AEO play is dead on arrival (five-minute check, gates everything).
- **NEXT 3 (owner to pick — offered in-chat, nothing pending on me):** (1) pull real volume/KD via the Taylor OS `seo-analyzer` to harden the winnability tags; (2) draft `llms.txt` + Phase-1 feature-page templates (AI receptionist / missed-call text-back / reviews × home services + auto — titles/H1/FAQ+schema filled); (3) map the intersection-page URL tree so the sitemap is baked in from day one.

**✅ REIDS ONE-PAGE DEMO — OFFER EDITS SHIPPED (2026-09-10, Opus 4.8). DONE, no follow-up.** Owner asked to edit ONLY the Reids one-page (`top-shelf-website/demo/reids-auto-connection/concepts/dir-4-onepage/index.html`; live at topshelfsolutions.io/demo/reids-auto-connection/concepts/dir-4-onepage/). Added ONE `.offer` band after the hero carrying all four asks: giant standout **$0 DOWN** (DM Serif on clay, hedged "on most loans"), a plane-icon **"We fly you in, free."** callout (replaced the tiny hero footnote), + checkmark chips **"Late-model, low-mileage vehicles"** and **"By appointment only"**; also strengthened the 3 existing "by appointment" to "by appointment only" and added late-model to the close checklist. Kept the site's em-dash voice (new copy dash-free). Grounded in the facts file (Reid's own banner promotes $0 DOWN + 2020-2025 + "Low Milage"); **added "$0" to `vault/platter/facts/reids-auto-connection.json` stats** (bare "0" does not authorize "$0 down" in the money check). WCAG AA verified 4.59-7.46:1 (fixed the fly card 3.82 to 6.5 by using a dark scrim, not a white overlay), 0 console errors, 0 mobile overflow. Judge PASS (1st). Deployed `c5df6ba` to Hostinger (live ~15s), filed to Drive (Reids/Website/concepts/dir-4-onepage). Non-blocking: the site's pre-existing "since 2009" founding claim is unverified per facts `_notes` (untouched here), a candidate for a future pass.

**🖥️ CRM MARKETING SHOWCASE — 5 STYLES DELIVERED (Judge PASS) + STYLE 6 "LINEAR LIVE" BUILDING OUT (2026-09-08, Opus 4.8). START HERE.**
New standalone project `C:\Users\taylo\top-shelf-crm-showcase` (Vite+React18+TS+Tailwind4+framer-motion+gsap+lenis+lucide+react-router7; **local git only, NO remote** — owner decides repo/host). Showcases the REAL TAP Services (=Contractor-Pro / FieldPro-family) CRM to promote on Top Shelf. Owner wants to PICK A WINNER, then deploy the winner (a Vite app can't drop onto the static Hostinger Top Shelf site — host separately or export static).
- **Real screens** captured from a LOCAL run of the Contractor-Pro app (`owner@tap.test`/`password`, company "TAP Home Services") via `tools/capture.mjs` (Playwright chrome-channel, 2x desktop/3x mobile) → `public/screens/*.png`. To make them showcase-grade I committed 2 CP fixes (**CP `ff8f3de`**): fixed a REAL `/schedule` 500 (strict lazy-load on `Job::serviceAddress` → eager-load `job.property`+`job.client.primaryProperty` in `Schedule\Index` week/day/month + mobileAgenda; **FieldPro mirror = spawned `task_b6a524fc`**) + `ShowcaseSeeder` (pipeline deals, call logs → fills the phone dock on EVERY desktop screen + dashboard Calls, paid invoices → dashboard revenue/A-R). Dev server: `npm run dev` → **localhost:5174** (this session; another chat holds 5173).
- **5 styles built + Judge PASS (1st) + committed `5e09081`, screenshots sent:** /1 Command Center (dark, framer laptop-reveal) · /2 Field Light (bright; subagent-built; LightBrowser/LightPhone chrome so dark shots read on a light bg) · /3 Top Shelf Luxe (espresso/gold Cormorant+Jost) · /4 Interactive Tour (clickable stepper) · /5 Bento (violet mosaic; subagent-built). Shared kit: `src/components` (DeviceFrames, Reveal, ScrollReveals[LaptopReveal/TiltReveal], **FadedSnippet**), `hooks/useLenis`, `data/{screens,styles}`, `pages/Home.tsx` (index lists all styles). Humanizer run — **all em dashes cut from visible copy** (only JSX comments keep them). tsc clean. Spec: `docs/superpowers/specs/2026-09-08-crm-showcase-design.md`.
- **⚠️ VERIFY MOTION VIA `tools/shoot.mjs <url> <tag> <fractions>`** (Playwright reduced-motion → Lenis off → exact scroll; composites 3D/WebGL). The in-app Browser pane renders 3D transforms AND linear.app BLACK — NEVER trust it for motion/3D pages.
- **⭐ STYLE 6 "LINEAR LIVE" — owner LOVES it, building out (foundation `b928e8f`).** linear.app aesthetic: product as **faded-edge snippets** (`FadedSnippet.tsx` masks all/bottom edges = "cut out of the app, no hard box"), dark `#08090a` + indigo `#5e6ad2`, ⌘K + "+ New" pills. Foundation = dashboard faded-snippet + a **live flow toast** cycling over the phone dock: Incoming call → AI receptionist answered → Job booked → Text sent.
- **NEW owner requirements for Style 6 (verbatim gist 2026-09-08) — NOT yet built:**
  1. **CRM sidebar nav + softphone tabs must be CLICKABLE** → clicking swaps the MAIN screen / softphone screen. NO free scroll, NO settings — only nav-driven screen swaps. Must **COLLAPSE the softphone AND collapse the sidebar**.
  2. On page load, an **animated fake SMS conversation about scheduling an appointment** (the app "texting" live).
  3. **Follow linear.app design for the REST of the showcase** (+ buttons, sidebars, faded snippets, feature sections).
  4. **Replicate Linear's in-product MOVEMENT renders** for our CRM (rows/cards/status animating so it looks live).
- **Linear.app design notes** (from `get_page_text` — its renders are WebGL, BLACK in the pane, couldn't watch): hero = a LIVE product view with activity happening (issue created→labeled→agent working→PR opened→Todo→In Progress); feature sections = headline + copy + faded product snippet w/ subtle in-app movement; **"+"-suffixed feature pills** (Linear Agent + / Triage + / Projects +); FIG 0.1/0.2 figure labels; persistent sidebar chrome.
- **BUILD PLAN (Style 6, next):** (a) re-capture MAIN-CONTENT-ONLY per nav dest — Playwright element-screenshot of `<main>` (or hide the app sidebar + Phone dock) for dashboard/schedule/clients/pipeline/jobs/invoices/estimator, + softphone per tab (Recent/Dialer/Texts/Recordings from `/phone`), + a conversation thread. (b) Build an **interactive CRM shell** React component: own sidebar (clickable+collapsible) + main (swaps the main-content shots) + softphone panel (clickable tabs+collapsible), state-driven, no real scroll/settings. (c) On-load animated **SMS scheduling conversation** (bubbles appear). (d) Linear-style **feature sections** for the flows (dispatch/pipeline/payment/estimator/texting) w/ faded snippets + framer micro-motion (row slides in, status pill Scheduled→On-my-way→Paid, deal card moves stages, customer pays via texted link). Then humanizer + Judge.
- **OPEN:** owner to (1) confirm Style-6 feel/pace/fade, (2) decide DEPLOY to a live link. Impeccable v4.3.0 update available (unmentioned).

**🔎 TOOLING SCAN — "showcasing app/CRM screens on a website" (2026-09-08, Opus 4.8). RESEARCH ONLY, no build, delivered in-chat (no artifact). Feeds `crm-page-linear`.**
Owner: find a GitHub repo or skill that's really good at showcasing screens like CRMs on a website — evaluate, say what they're good at + if they're any good; "then we can figure out our next moves." Facts (stars/license/component names) verified against the primary repos/docs, not search snippets.
- **Technique split (pick one before choosing a tool):** (1) static screen in a device frame · (2) scroll-driven cinematic reveal (Linear/Apple/Stripe) · (3) animated marketing sections arranging screens · (4) genuinely-clickable demo.
- **Best drop-in GitHub libs (both free to start · React/Tailwind/Framer · copy-paste):** **Aceternity UI** (ui.aceternity.com) — *MacBook Scroll* + *Container Scroll* = the exact "CRM screen emerges/tilts as you scroll"; individual components free, paid one-time all-access for templates/blocks. **Magic UI** (22.2k★, MIT, github.com/magicuidesign/magicui) — *Device Mockups* (iPhone/Safari/Android holding a screenshot **or video**) + *Hero Video Dialog* + Bento/Marquee/Animated Beam; core free, Pro templates $199. The two together ≈ 90% of a CRM showcase.
- **Device-frame primitive:** **devices.css** (picturepan2, 2.4k★, MIT, pure-CSS, add `class="device-screen"` to an img/video) — dependable + framework-agnostic. React options (react-device-mockup, react-framify, liamjohnston/react-browser-frame, DevManSam777/device-mockup web component w/ clickable+iframe) noted but judged from listings, NOT hands-on.
- **Scroll "experience" (most bespoke):** GSAP ScrollTrigger + Lenis (± Three.js); reference repos pulkitxm/claude-directory + the Apple-iPhone-clone repos.
- **Clickable demo (technique 4):** Arcade / Storylane / Navattic (paid SaaS, not GitHub) — already ruled OUT on this workstream (owner chose a self-built clickable vanilla replica).
- **KEY FINDING — we already own the skill layer.** `showcase-motion`, `device-story-scroll`, `scrollcraft`, `build-awwwards-quality-sites`, `anti-slop-web` + agents `showcase-website-builder` / `topshelf-website-builder` (Awwwards Showcase funnel). Not missing a *skill* — missing the drop-in component *primitives* (Aceternity/Magic UI/devices.css) those skills can feed from. (claude.ai SearchSkills returned empty; the local plugin stack is authoritative here.)
- **NEXT 3 (owner to pick):** (a) build a side-by-side PROOF — a real LedgerPro/CRM screenshot in an Aceternity MacBook-Scroll vs. a Magic UI device frame, on a live page, so he can see the difference; (b) hands-on quality/perf pass of the React frame libs; (c) wire one straight into the `crm-page-linear` page already scoped (`vault/projects/top-shelf/crm-page-linear/`).

**✅ 8 INDUSTRY SAMPLE WEBSITES — ALL 8 LIVE (2026-09-08, Opus 4.8). Judge-gated. START HERE (newest).**
Owner intent (verbatim gist): a 5-page sample website for every industry in Top Shelf's Industries section + a custom "Your Business" built as a veterinarian hospital (8 total). Full creative rein; "Apple design quality, enterprise-level, ready to ship as if a real customer." Each: exactly 5 pages, a dynamic hero, quality transitions, a FICTIONAL company w/ high-quality logo, industry-fit theme, believable background/story. Phone 123-456-7890, emails contact@companyname.com. Built one at a time to avoid overlap. Delivery decisions (owner, verbatim): showcase via a NEW "Websites" main-nav gallery + each site on its matching industry page; KEEP the "sample by Top Shelf" footer credit (lead magnet); do the integration ONCE at the end.
- **DONE — all 8 built, Judge-PASSed, LIVE** under `top-shelf-website/websites/<slug>/` (push to main auto-deploys to topshelfsolutions.io/websites/<slug>/), each filed to Drive `Projects/Top Shelf/Sample Websites/<slug>/`, retros in `vault/memory/agent-logs/engineering.md`:
  1 everline-home-services · 2 ember-and-oak · 3 brightwater-dental · 4 halcourt-vale · 5 field-and-fawn · 6 apex-auto-werks · 7 lumen-wellness · 8 wellspring-animal-hospital (the vet). Each is its own world (distinct palette + Google-fonts pair), sharing one theme-agnostic `assets/app.js` (Lenis + GSAP ScrollTrigger, IO reveals, count-up, rotating hero word, demo form). Roster/status/conventions: `websites/_PROGRAM.md`. 5 of the 8 took exactly 1 Judge REVISE each (all contrast/asset issues), 0 took two.
- **NEXT — the deferred INTEGRATION pass (NOT started):**
  1. Build `websites.html` — a gallery of 8 cards linking to the live samples, matching the marketing site (site.css: dark `#0B0906` + gold `#C9A86A`, Cormorant Garamond + Jost). Clone the nav/footer from any `industry-*.html`. (Thumbnails: capture consistent 1440x900 hero shots per site.)
  2. Add a "Websites" nav item — nav is HAND-DUPLICATED across ~28 top-level pages (no shared include), so each page needs the edit. Add a "see the sample site we built for this industry" showcase section to each of the 8 `industry-*.html` pages (slug -> industry page; `industry-custom.html` = "Your Business" = the vet).
  3. Commit/push (auto-deploys) + file the gallery to Drive.
- **⚠️ OWNER DECISION PENDING before editing `index.html`:** the homepage `top-shelf-website/index.html` has a LARGE uncommitted change from a PRIOR unfinished session — an interactive "Solutions" detail-drawer (`#svcDrawer` / `#svcScrim` + `#tsd-css`/`#apple-polish`/`#odo-css` style blocks + a `DETAIL` script), unrelated to these samples and unreviewed. `HANDOFF.md` (repo root) is also modified-uncommitted. Do NOT silently ship or discard either. Because the "Websites" nav also belongs on the homepage, wiring it in entangles with this WIP — Taylor was asked how to handle it (keep/finish/discard the drawer; whether to add the nav to the homepage now). Resolve before touching index.html.
- **TRAPS / reusable (all also in engineering.md):** inline SVG `<use>` sprite needs `svg{fill:none;stroke:currentColor;...}` in CSS or every icon renders as a black fill; never put `data-reveal` on an element that `gsap.from` also animates (gsap reads the reveal's opacity:0 as its END state -> element stays invisible); a theme/section override must WIN by SPECIFICITY not source order (`.tier.tier--feature li`, not `.tier--feature li`, which tied `.tier li` and lost); for a translucent overlay pill compute contrast against the COMPOSITED bg, not the base token; pre-compute WCAG in-browser BEFORE dispatching the Judge to avoid a REVISE cycle; sites live under `websites/` (NOT `samples/`) to dodge the `expire-samples.yml` 60-day sweep; verify with Playwright (in-app Browser pane won't composite below-the-fold after a programmatic scroll).

**✅ FULL MOCKUP RECONCILIATION + CONTRACTOR PRO — COMPLETE (2026-09-06, Opus 4.8). Judge PASS (1st). START HERE (newest).**
Owner: "evaluate everything desktop and mobile to make sure nothing was missed" (Top Shelf) + "for CP go over the items FieldPro doesn't have — AI Estimator + that nav — and do what you'd recommend."
- **TOP SHELF — reconciled EVERY mocked screen** (2 parallel evaluators graded shipped code vs the mockup 278c5d64). Gaps closed: Reputation rating-distribution **histogram** + Needs-reply filter; Payments **contact-first rows** (avatar+name, inv# subtext) + All/Unpaid/Paid filter; Calendar one-tap **copy booking link** (`CalendarController` now returns `booking_url`); Pipeline **win-rate chip**; Prospecting control cards **collapsed into one command bar**. (Contacts list+detail shipped earlier as `b75be50`.) Batch shipped `e2316b7` + Prospecting `6771ae5`. Already-faithful, left alone: Dashboard, Conversations, Automations.
- **HONEST DECLINES (not faked, Judge grep-confirmed absent not stubbed):** Payments Remind (no backend endpoint), Payments Products-panel removal (real feature — kept), Calendar utilization bar + Pipeline time-in-stage/next-step (no data fields exist), Pipeline per-column add (drag + global Add cover it), Dashboard ⌘K palette (big new feature).
- **CONTRACTOR PRO** (`C:\Users\taylo\Contractor-Pro`, Livewire/Blade, own repo) — CP-unique vs FieldPro = **Estimator** (Index/Wizard/Show) + **Commissions**. Read all four: **already at/above the bar** — the Estimator Wizard is the best-built screen in the ecosystem (7-step photo→estimate, wire:key everywhere, transient/retry states, before/after slider, reconciled pricing). No redesign needed. ONE real fix: Estimator Index desktop table rows missing `wire:key` (a delete could mis-target under Livewire DOM diff) → shipped `ad525e3`.
- **Judge PASS (1st):** all screens reconciled with real structure; no feature/handler/route/data-path dropped; new controller reads tenant-scoped (BelongsToLocation→LocationScope); 0 new tsc errors (8 pre-existing baseline unchanged, none in touched files); declines confirmed absent-not-stubbed. Verified live desktop 1440 + mobile 375 on every changed screen (0 horizontal overflow; filters / copy-link / histogram / command-bar working).
- **OPEN / NEXT:** nothing pending. The whole Top Shelf CRM now matches its approved mockups (or has an honest, logged reason not to); CP's unique screens confirmed enterprise-grade.

---

**✅ CONTACTS LIST+DETAIL — CORRECTED (2026-09-06, Opus 4.8). [Superseded by the full reconciliation above.]** Owner caught that the Contacts LIST (`contacts/index`) shipped only live-search + avatars on the old table, NOT the **list+detail (Apple Mail)** the mockup (278c5d64, change #1 "a relationship view, not a dump") showed. Now built for real: **desktop two-pane** — searchable list left + selected-contact detail right (avatar, Call/Text/Email, channels, source, tags, recent activity, "Open full profile"); `ContactController@index` loads the `?selected=<id>` summary (Inbox pattern, `only:['selected']` so the list stays put); **mobile** keeps the card list → full profile. Omitted the mockup's All/Leads/Customers segmented filter — no lead/customer field backs it (won't ship a dead control). Verified live at 1440+375 (0 overflow, selection loads the detail, two-pane `display:none` on mobile). Shipped **`b75be50`**. **Lesson: when a mockup shows a STRUCTURAL redesign, "done" means the structure — a lighter styling pass is not the redesign, and I should reconcile the other originally-mocked screens (dashboard/pipeline/conversations) against what was shown.**

---

**🎨🚀 FULL-APP APPLE-POLISH SWEEP — SHIPPED TO PRODUCTION (2026-09-06, Opus 4.8, this session). Judge PASS (1st).**
Owner: "go over every desktop and mobile page, every button, every function ... optimized to redesigned to look better like an enterprise apple app." Confirmed via structured prompt: **full sweep in verified waves, public/customer-facing pages included.** Approach: evaluated ALL ~50 pages (5 parallel general-purpose evaluators graded each against the reference bar = the already-redesigned pages), then shipped fixes wave-by-wave — each page branch → `npm run build` → verify desktop(1440)+mobile(375) via browser DOM → `merge --ff-only main` → push — while LEAVING the ~24 pages already at the bar untouched (no churn; owner-endorsed). One consolidated judge-agent gate over the whole sweep: **PASS (1st).**
- **5 waves shipped** (range `a96d769..9f0c248`, 25 files, +533/−383):
  - **W1 Auth** `440816e` — auth-simple-layout heading `text-xl`→`text-2xl` (lifts all 7 auth pages to the Cormorant display scale); login **Remember-me bound** (was uncontrolled → never sent); verify-email button outline+full-width (was near-invisible `secondary` on dark); inputs disabled-while-processing + emerald status; accept-invite title double-space.
  - **W2 Core** `fbb7837` — invoices/show: semantic status pill + **Balance-due serif hero** (was buried text-sm) + shadcn buttons w/ `sending` guard; subscriptions: MRR serif hero + semantic pills + **table→cards on mobile** (fixed a real 59px overflow) + per-row `busyId` guard; contacts/show: **fixed missing `Link` import (runtime crash for contacts with an application)** + tab-badge count bug + semantic opp colors + bigger tag/enrollment tap targets + timeline empty icon; broadcasts empty-state icon.
  - **W3 Secondary** `329a02e` — training: **fixed focus-loss bug** (`ModuleForm` declared inside render → remounted each keystroke, forms unusable) via a Dialog + one inline `useForm`; workflows/builder: StepNode retokenized `bg-card`/gold-trigger (was hardcoded white-on-dark) + Published emerald + destructive/muted (graph logic untouched); custom-fields empty-state icon + header scale.
  - **W4 Settings** `65efce9` — settings/profile: **fixed dark-mode contrast bug** (`neutral-800` ≈ invisible on `#0b0906`) → tokens + gold link + emerald; appearance-tabs: gray segmented pill → **gold `bg-primary` pill** (verified computed `#c9a86a`); password/access/clients/onboarding semantic tokens + access empty-state icon.
  - **W5 Public** `fe22a14` + hardening `9f0c248` — invoice.tsx: **fixed money-path freeze** (post-charge Helcim confirm now try/caught → button can't lock on 'working'; copy never says "try again"; + a charge-guard so the button stays disabled once SUCCESS fires → no double-pay); review error state; portal semantic pills (sent reads gold like the pay page, was blue) + bigger Pay-now; book real labels + full validation + submit gating (mirrors `BookingController::book`) + 'Booking…'; funnel aria-labels + per-field errors + email/tel types + 'Sending…'.
- **5 real bugs fixed** (not cosmetics): remember-me unbound · training focus-loss · contacts/show `Link` crash · invoice money-path dead-end · subscriptions mobile overflow.
- **Verification:** each shipped screen checked at 1440 + 375 (0 horizontal page overflow); interactive fixes confirmed LIVE — remember-me toggled false→true, training focus held through 7 typed chars, builder nodes non-white (`anyWhiteNode:false`), appearance gold pill `rgb(201,168,106)`, book form labels ["Your name","Email","Phone"]. TRAP: the in-app Browser pane's mobile emulation reports an inflated `innerWidth` on an overflowing page — trust `documentElement.scrollWidth > clientWidth`, not innerWidth (the subscriptions overflow hunt).
- **Judge PASS (1st):** all 4 code-level fixes verified root-cause; every feature/route/handler preserved (frontend-only .tsx diff, no tenant/route/query change → no leak, no fabrication, figures derived from props); book gate blocks no valid submit. UNVERIFIABLE (non-blocking): the live Helcim charge round-trip (no real card) — client logic correct as written.
- **Left untouched (already at the bar, ~24):** dashboard, contacts/index, invoices/index, pipelines (index+show), reviews, workflows/index+executions, calendars (index+edit), conversations, prospecting, tasks, inbox (index+compose), funnels (index+edit), agency (phone/clients/essentials-onboard), settings (integrations/onboarding), sign, welcome, contacts/application.
- **OPEN / NEXT:** owner reviews the live app; nothing pending. The whole CRM now sits on the espresso/gold enterprise-Apple bar.

---

**🚀 CRM APPLE-POLISH REDESIGN (the 8 named screens) — SHIPPED (2026-09-06, Opus 4.8). Judge PASS (1st). [The full-app sweep above extended this to every page.]**
Owner reviewed the passed before/after mockup Artifact (278c5d64) and said **"I like all of them so far go ahead and change whatever you recommend"** + **"keep going"** → I implemented the approved redesigns in the REAL production CRM (Laravel 12 / Inertia 2 / React 19 / Tailwind 4), one screen per branch → commit → deploy, preserving every feature, verified desktop + mobile with seeded demo data before each push (Railway auto-deploys on `main`).
- **8 screens shipped** — commit range `f80e44f..fcf2300` (10 files, +375/−134):
  1. **Dashboard** `48ad036` — centered max-w workspace · compact hairline KPI strip (dropped serif/gold-top) · prospecting-funnel hero w/ meter bars + honest "none contacted yet" · smart "you're all caught up" attention row · Tasks empty state.
  2. **Payments** (invoices) `1460463` — "Outstanding" money hero tile (N unpaid · M overdue) · overdue-first sort (`statusRank`) · inset destructive rail on overdue rows.
  3. **Contacts** `1fb3997` — debounced live search (300ms, replaces the submit button) · initials avatars in desktop table + mobile cards.
  4. **Opportunities** (pipelines/show) `7a53c3f` — weighted stacked pipeline bar + legend · contact avatar on deal cards.
  5. **Reputation** (reviews) `e03a240` — "How reviews are routed" triage panel (4★+→public / below→private) w/ REAL guarded percentages · public-review-URL prompt when unset.
  6. **Automations** (workflows + controller) `ffa3c84` — flow-chain (Zap trigger → step → step) on each card · rollup overview.
  7. **Calendars** (+ controller) `778c395` — tenant-scoped "Upcoming" appointments panel grouped by day (the booking-page list never showed who's actually coming).
  8. **Nav sidebar** `fcf2300` — grouped by job: Work / Grow / Money / Setup. SAME items + `isHouse` conditionals + badges + `nav_disabled` filtering; sidebar default state unchanged at ship, then **flipped to default-EXPANDED on owner request → `cb488c8`** (fresh users open with the group labels + full names; an explicit collapse is still remembered; mobile sheet unaffected).
- **Left untouched (already at target):** Conversations (already the threaded + AI-draft redesign) and **Prospecting** (already redesigned in the earlier "Atelier" wave — command-bar finder, bucket triage, worklist table/cards, green website-gap signal; confirmed via git log `cee96f0`/`833644b`/`c01926c`, NOT re-churned).
- **Verification per screen:** temp local-only `/dev-login` (`Auth::loginUsingId`, gated `app()->environment('local')`, NEVER committed) + seeded SQLite house account (id-3 "Top Shelf") → `npm run build` + `php artisan test --filter` green → rendered desktop+mobile on Vite:5173 / artisan:8000 → `merge --ff-only main` → push.
- **Judge: PASS (1st)** — independent judge-agent over the whole diff traced each screen old-vs-new: zero dropped controls/links/filters/badges/data-paths, every displayed number real + divide-by-zero-guarded, the one new server query (CalendarController `upcoming`) correctly tenant-scoped (Appointment → BelongsToLocation → LocationScope, no opt-out), redesigned code type-clean. Two NON-blocking, PRE-EXISTING notes: reputation `positive_pct` is computed over the latest 100 reviews (`ReviewController.php:19`), not all-time (still honest); the repo's `tsc --noEmit` baseline was already red before this diff (Vite/esbuild ships fine).
- **No Drive file** — the deliverable is production code (in the `top-shelf-crm` repo + live on Railway), not a tangible asset; the mockup Artifact was already Drive-filed (entry below).
- **Desktop + mobile re-audit (owner: "has every screen been evaluated desktop and mobile?"):** re-checked all 9 screens LIVE at 1440px and 375px via browser DOM (horizontal-overflow / render / nav). Desktop: all clean (`overflowX 0`). Mobile: all clean **after fixing ONE real bug** — **Payments (`/company/invoices`) overflowed ~44px on phones**: the invoice grid cell `lg:col-span-2` defaulted to `min-width:auto` and expanded to the table's intrinsic width (~403px) instead of letting the inner `overflow-x-auto` scroll it → added `min-w-0` (**`a96d769`**), re-verified 375px `overflowX 0`. Console errors seen are NOT from this work: stale HMR `toolItems`/`visibleTools` (grep-confirmed zero live refs) + a pre-existing local-dev `GET /telnyx/webrtc-token` 422 (Telnyx creds unset locally; fine in prod).
- **OPEN / NEXT:** owner reviews the live app; nothing pending. Optional future: fix the repo's pre-existing red `tsc` baseline so the typecheck gate can catch regressions.

---

**🎨 CRM APPLE-POLISH DESIGN REVIEW (mockups) — SHIPPED (2026-09-06, Opus 4.8). Judge PASS (1st) + Drive-filed. [The production build above implements these.]**
Owner: use our app-focus design skills (apple-design + impeccable + a functional lens) to review a few Top Shelf CRM pages (desktop + mobile) and show before/after RENDERS of what they'd recommend, to make the app feel like a polished Apple product — as a viewable HTML with explanations. Confirmed via structured prompt: **after = mockup redesigns (recommendations to review), NOT live code**; lens = apple-design + impeccable + functional.
- **DELIVERABLE (viewable Artifact):** https://claude.ai/code/artifact/278c5d64-ce1e-40cd-8df5-d35c865e326f ("Top Shelf, Refined"). Source: scratchpad `topshelf-crm-apple-review.html`. Self-contained; before + after as live HTML mockups in device frames using the REAL CRM tokens (espresso #0b0906 / gold #c9a86a / Cormorant + Jost) and REAL demo data.
- **9 screens:** Dashboard · Prospecting · Contacts · Conversations · Pipeline (real demo data), + **Payments · Reputation · Automations · Calendar** (added on the owner's follow-up "render new sections"; REPRESENTATIVE data since the demo account is empty on these — disclosed via a per-page pill). Each = current UI (before) beside a redesign (after; desktop always shown + a per-case "Show mobile views" toggle), then a principle-mapped change list, + a top "seven systemic moves" section. **2nd Judge cycle: PASS after 1 REVISE** (it caught Reputation's triage split 92%/8% not matching the on-screen 88/8/4 distribution → fixed to 96%/4%, avg still 4.8).
- **Core thesis (honest):** the FOUNDATION is good (tokens/type/frosted rail) — fixes are at LAYOUT + STATE level (sea-of-identical-cards, wide-viewport thinning, weak empty states, icon-only nav, serif overused on every number). Per-screen: dashboard→funnel-led centered workspace; prospecting→controls collapse to a command bar + likelihood meter + website-gap chip; contacts→list+detail (Apple Mail); conversations→real threaded convo + AI-draft on composer; pipeline→content-hugging columns + richer deal cards + pipeline viz.
- **Reference method:** logged into the LIVE app via Claude-in-Chrome (owner typed the demo password — user `taylor@topshelf.test`, credential rule honored: I never typed the password) + read the CRM source (`top-shelf-crm/resources/js`: dashboard.tsx, app-sidebar.tsx, bottom-nav.tsx, app.css tokens). Entered the Acme Plumbing client account to capture the populated Kanban ($16,500 open; deals Website redesign/Jordan Lee, Monthly retainer/Priya Shah, Brand refresh/Marcus Reed, Landing page/Priya Shah Won).
- **OPEN / NEXT:** (1) ✅ Judge PASS (1st); applied its 3 non-blocking polish nits (pipeline win-rate chip → "1 won · $1,500"; dropped the wrong "Tuesday" weekday; nav-groups copy aligned to Work·Grow·Money) → re-published + Drive-filed + episodic-logged. (2) **Owner to pick which screens/moves to build** → become real Inertia/React work against the existing token system. (3) ✅ Drive: `Projects/Top Shelf/Documents/topshelf-crm-apple-polish-review.html`.
- **TRAP (build learning):** each mockup renders in an isolated `<iframe srcdoc>` so its media queries respond to the FRAME width (desktop vs phone) — that's WHY iframes, not inline. 20 iframes froze the renderer → fixed with IntersectionObserver lazy-MOUNT-ONCE (rootMargin 250) + only 2 desktop iframes on the scroll path (mobile behind the toggle). CDP screenshots on the claude.ai nested-iframe viewer time out WHILE an iframe mounts (capture-tool limit, not a page bug); every settled view captures fine — verify by landing on already-mounted content, not mid-mount.

---

**💲 BLEND PRICING LADDER — SHIPPED + LIVE (2026-09-06, Opus 4.8). Judge PASS. START HERE.**
Owner asked to update the live marketing site to the NEW blend ladder. He first said "premium / those prices," then corrected to "the $299 etc." and confirmed the **locked 2026-08-19 ladder** via structured prompts:
- **Essentials Blend $299** (was Essential $499) · **Signature Blend $899** (UNCHANGED, "Most Popular") · **Platinum Blend $2,500** (was Premium $1,599; Premium→Platinum).
- **"Apply the full locked structure"** on Platinum: application-only · ad spend billed separately (pass-through) · 2nd ad platform +$500–750/mo.
- **Publish scope = PRICING ONLY** (do NOT publish the held Frisco demo — see traps).

**DONE (all local, verified, NOT pushed):** exact-string migration (`scratchpad/blend_migrate.py`) across **22 live pages** — pricing.html (cards + comparison headers + meta/OG/Twitter + FAQ), index.html + home-centered-review.html teasers, and "From $499/mo"→"From $299/mo" hire-vs-us row on every industry/solution/why-us page. Platinum card: label "By application · ad spend billed separately" + application-only note + "Apply to Qualify" button. Anchors ($1,582/$7,345/$9,605 "to hire it out") KEPT (included-service costs unchanged). `offer.py` (authoritative linter) updated to match: PLANS Essentials $299/Signature $899/Platinum $2,500, AUTHORIZED_AMOUNTS in sync, RETIRED_PLAN_NAMES maps Premium→Platinum & Essential→Essentials, PREMIUM_ONLY→PLATINUM_ONLY + app-only note. **Verified:** residue grep clean (0 old values on live pages); offer.py imports clean; pricing.html DOM rendered correct at localhost:8899 (3 equal-height cards). **Judge: PASS.**

**SHIPPED (2026-09-06):** owner said "go" → pushed commit **ce04e89** (21 pages) to `main`; **live + verified** on topshelfsolutions.io/pricing.html (Essentials $299 / Signature $899 / Platinum $2,500, 0 old values). The 11 Frisco-demo commits were isolated onto branch **`frisco-demo-hold`** (unpublished, intact) so the pricing-only publish didn't ship them. 21 pages mirrored to Drive `Projects/Top Shelf/Website/`. `home-centered-review.html` left as an untracked draft (updated on disk, not live).

**DONE:** publish ✅ · Drive mirror ✅ · handoff ✅ · episodic ✅.

**OPEN / NEXT:**
1. **`frisco-demo-hold`** branch holds the Frisco realtor demo — owner still decides if/when it goes public (the reason for the pricing-only publish).
2. **offer.py cloud-sync gap:** the price edit is desktop-only (the `topshelf-seo-audit` skill dir is gitignored in the config repo, `.gitignore:38`) — un-ignore it if the hosted runtime needs the new prices for prospect audits.
3. Optional: soften the "$9,605/mo vs From $299/mo" comparison ratio if wanted (Judge eyes-open note; currently literally true + the owner's existing framing).

**TRAPS:**
- ⚠️ **top-shelf-website is 11 commits AHEAD of origin — ALL are the Frisco realtor demo (`demo/frisco-luxury-realty/`), deliberately held from publish.** A plain `git push` publishes them; the step-1 sequence isolates the pricing commit onto origin/main and parks the demo on `frisco-demo-hold`.
- **offer.py is GITIGNORED** in the config repo (`.gitignore:38 skills/topshelf-seo-audit/`) → the edit is **desktop-only**, won't reach the hosted runtime. Cloud-first gap; owner decides whether to un-ignore.
- Browser pane won't composite these pages on this machine (black screenshots) — verify via `get_page_text`/`javascript_tool` DOM.
- Eyes-open (Judge non-blocker): the hire-vs-us row now reads "$9,605/mo vs From $299/mo" (was From $499) — literally true, owner's existing framing, but a wider ratio he may want softened.

**🥃 SIGNATURE BLEND SCROLL SHOWCASE — NEW PAGE BUILT + SHIPPED (2026-09-04, Fable 5.1). Judge: PASS.** A separate, DOM/GSAP one-page showcase (not the 40-sec film above) built by the new `showcase-website-builder` agent on Fable, then verified + published by the Manager. Concept **"One job, start to finish"**: follows one T.A.P. customer (Maria Alvarez, $21,400 bath remodel, Denton) from the incoming call to the 5-star review, on the **16 real CRM screens**. Signature move **"Scroll to answer"** — hero incoming-call phone docks onto a rising glass desktop as the call is answered (FLIP-style, measured from a hidden slot). Stack: Lenis + GSAP ScrollTrigger + SplitText 3.13, luxury espresso/ivory/gold, no WebGL. Sections: hero · answered · thread · onmyway · paid · review · blend grid · plans · audit CTA.
- **Viewable link (Artifact):** https://claude.ai/code/artifact/a12421ca-d6d6-4622-8a60-dbc932a009e7
- **Files:** scratchpad `signature-showcase-agent/` (index.template.html + screens.json + build) ; **Drive** `Projects/Top Shelf/Website/signature-blend-showcase/` (index.html self-contained 1.4MB + DIRECTION.md + README).
- **Two bugs the Manager fixed during Ship:** (1) ROOT — Lenis was loaded from a **cdnjs URL that 404s**; the whole animated experience silently fell back to a static page (MOTION gated on `window.Lenis`). Fixed → jsdelivr `lenis@1.3.11/dist/lenis.min.js` (sets `globalThis.Lenis`). Also fixed the same wrong URL in the **showcase-motion skill** (config repo, pushed). (2) CSS `.plan` class collision — the pricing-card `.plan` rule (bg + min-height:100%) bled onto the blend-card labels, ballooning them into gray boxes over the device screens. Fixed → renamed blend labels to `.plan-note`.
- **Honesty verified (Judge + Manager):** live-demo/sample-data markers in nav/hero/fine-print/footer; tiers exactly Essential/Signature/Premium (no "Platinum"); **GBP posts on Premium card only**, Signature carries only "advanced monthly local SEO + tuned GBP"; "the gap" not "the leak"; CTA → topshelfsolutions.io/booking.
- **SCREEN-QUALITY + SCROLL PASS (2026-09-04, owner feedback round 2):** (5) initial hero hold shortened (dock now starts at timeline 0.35 vs 1.0 — motion begins ~350px in, was ~630px). (1) thread-beat phone was the same dim receptionist screen — built a NEW distinct customer **text-thread** screen (`thread`) + revealed it at T1 at full brightness. (2) on-my-way **Text icon fixed** (was a malformed glyph) — proper Heroicons chat bubble. (3) **resolution**: rebuilt the phone screens as crisp HTML mockups in the CRM's midnight-gold system + re-captured ALL screens at 2× deviceScaleFactor (desktops 2880×1800, phones 780w+) via `screens-src/capture.mjs` (Node Playwright, imports playwright from `Ledger Pro/node_modules`). (4) on-my-way is now **full-height (780×2680) & scrollable** — during its beat hold the phone's job screen pans object-position 0%→100% through all line items to the $21,400 total, then releases to the page (GSAP tweens objectPosition fine). Phone layer stack grew to 6 (added `thread`; renumbered phoneL[2..5]). screens.json now ~3.7MB base64; page ~3.8MB (<16MB). Screen-source pipeline archived to Drive `Website/signature-blend-showcase/screens-src/`. Verified all beats + the pan + mobile, 0 console errors. Artifact republished (same URL). NOTE: not re-Judged — honesty-critical content (tiers/GBP/claims/markers) unchanged from the PASS; only screen images + one interaction changed, verified visually.
- **POLISH PASS (2026-09-04, owner feedback):** fixed 4 visual bugs the owner flagged — (1) renders looked *see-through* at the paid/thread beats because the dim used `autoAlpha:0.5/0.72`; switched to `filter:brightness()` so devices stay solid but recede. (2) A gold decorative *thread* line (`.thread` SVG) crossed over the screen faces; removed it (`display:none` + dropped its tweens + orphan var). (3) Headline *descenders were clipped* by the SplitText `mask:'lines'` box (line-height .98–1.02); raised `.display` to 1.14 / `h1.display` to 1.06. (4) Blend *render frames were cropped* (schedule only 170px); raised `.card__media` heights (sched 170→320, book 280→340, base 210→250, + mobile) so the website/booking/schedule screens read. Re-verified all beats + blend + plans on desktop, 0 console errors; Artifact republished (same URL).
- **Infra added this session:** `showcase-website-builder` agent + `showcase-motion` + `webgl-hero` skills + matrix `showcase-website` deliverable row (all committed earlier). The agent funnels Direction→Build→Animate→Wow(opt)→Review→Ship, one primary skill per phase.
- **NEXT:** owner reviews the Artifact link. Not deployed to a live URL (publishing to topshelfsolutions.io needs his go). If it becomes a real page: drop into the site repo / staging path. Optional: trim the `<title>` explainer, add a WebGL hero variant if he wants more flash.

**🎬 DREAM SCROLL PAGE BUILT (2026-09-02 night, Fable 5.1) — Judge: REVISE×3 → delivered with the last objection fixed post-gate (mask bottoms +4 cqh, verified with the Judge's own masked-vs-raw script at 0.7/2.9/28.9/29.38/37.2; not re-judged, 3-attempt cap).** The 40-second film is stitched and the scroll page runs against it. Files: scratchpad `tap-crm-scroll/build/` + Drive `Projects/Top Shelf/crm-scroll-prototype/build/` (index.html + scrollcraft engine + media + renders/*.webp + lab sheets + tools). Run locally: `node ~/.claude/skills/scrollcraft/scripts/serve.mjs --root <build> --port 4500` → http://localhost:4500 (launch.json "dream" in `Ledger Pro/.claude`). BUILT: (1) `film-native.mp4` = 20 segments per the §1b excerpt list (`tools/_stitch.py`), 40.000 s, frozen frames under the build-only beats (CASCADE/MAGIC/FLY/SNAP), two stills for SC-16's CLEAR/SMILE; `film.mp4` = + six chimes at 8.7/9.3/22.6/23.6/28.0/32.2 (`_mix.py`: chime bus padded to 40.1 s, native audio sidechain-ducked, placeholder bed); `film-web.mp4` (1080p, 14.9 MB) + `film-m.mp4` (720p, 6.5 MB), GOP 12 for scrubbing, audio 40.085 s on all three. (2) `chime.wav` (synth E5→B5 bell) + `bed.wav` (placeholder pad). (3) `index.html`: scrollcraft scrub act (span 16vh, clip-map travel, lerp .2) + bespoke JS keyed to film time T = the displayed video frame: MONS (monitor masks OPEN/GLOW/NOTIF/CLIMB/CLEAR/SMILE/SNAP as flat perspective rects, held on the poster until the clip paints), DEVS (the one phone: zooms out of the character's phone to a reading position with the real render inside, fast-scrolls to the bottom with hard stops, receipt swap 22.75), CARDS (fan-out 11.5–13.25, 62cqh cards at [21,49]/[50,52]/[79,49], pipeline on top, Alvarez highlighted in each), takeover 31.0–33.0 (pin drops onto the blue T.A.P. marker at 32.2 + "Avg. rank 5.2→1.3" callout + dashboard flash 32.7), gold bloom (MAGIC) + green bloom (PAY), the silent-cue kit (buzz arcs, ring arcs, tap rings, shutter flashes, chime sparkles, sent/post streaks, wipe bar), end card 37.42+ (h1 "Stop imagining it." / "Get your free audit." / CTA → https://topshelfsolutions.io/booking / wordmark), Play control (real playback drives scrollY, engine playhead detached via `rec.ready=false`, wheel/touch/keys interrupt, pause hands the playhead back), sound toggle (off by default), gold progress hairline. VERIFIED in real Chrome (playwright channel chrome, the in-app Browser pane reports a 0×0 viewport between actions on this machine and is useless for this page; `fit()` now guards zero sizes): scrollcraft harness desktop/mobile/reduced-motion = no dead scroll, clip keeps moving; 32+21 beat-time frames + full-res cue crops (`lab/`); `_play.cjs` state machine; the Judge's own acceptance script (`scratchpad/_judge7/hl2.cjs`) green.
**DISCLOSED GAPS (verbatim for the delivery note):** SC-16 not generated (Higgsfield out of credits) so CLEAR (33.0–35.5) and SMILE (35.5–37.0) hold two stills (sc15 keyframe, desk-clean-final); the score bed is a placeholder pad; the chime is synthesized; the before/after slider is a sweeping light bar over a static split; star taps are ripples over a pre-lit row; the $200 tip appears on the receipt and PAID screens only, not on the pay page; monitor masks are flat rectangles with no sticky-note occlusion or tracking; portrait phones centre-crop the film (monitor masks + takeover partly off-screen/small; phone masks centre); native audio is ducked under the chimes by sidechain; reduced-motion visitors get the poster with the ad masked + the overlays + the end card (the clip is never fetched); humanizer ran on the meta description + hidden screen-reader summary only (end-card lines are the owner's locked words, button labels are UI strings). Non-blocking: em dashes inside app renders (the AI transcript line, the paid screen) are app strings, Taylor's call whether to re-render; the scheduler render's gold "D. Huang" card competes with the Alvarez highlight in CASCADE.
**JUDGE HISTORY:** attempt 1 REVISE (audio ended at 33.4 s: the chime bus length drove the sidechain + final mix; mask strips; cascade highlights off/hidden; reduced-motion poster unmasked; sparkles #3/#4 mis-timed; approximations undisclosed) → all fixed. Attempt 2 REVISE (residual mask slivers; scheduler highlight under the pipeline card; disclosure note not on disk) → rects widened, cards shrunk + spread, this note. Attempt 3 REVISE (one residual: the film's baked taskbar under the OPEN/SNAP and NOTIF mask bottoms) → fixed after the gate: MONS[0]/[6] rect [20.0,18.2,17.4,39.5], MONS[2] rect [18.8,17.6,17.2,44.5]; masked-vs-raw crops clean (Drive `lab/mask-bottoms-masked-vs-raw-final.jpg`). Lessons: pad a side-chain bus to the programme length; probe overlay occlusion with elementsFromPoint, not eyes; never trust the Browser pane's viewport here.
**NEXT:** Taylor reviews at localhost (or says the word to deploy to a staging path on topshelfsolutions.io: publishing needs his go) → decide the progress-indicator style (Artifact `88f145ee` comp) → SC-16 when credits return (spec above; swap the two stills in `_stitch.py` CLEAR/SMILE rows for the clip excerpts 1.5–4.0 / 6.5–8.0, re-run `_stitch.py` → `_mix.py` → the two encodes) → a real score bed → optional polish: customer-pay render with the $200 tip selected (`_customer_docs.py`), true slider wipe + star lighting, sticky-note occlusion mattes, a portrait layout → deploy into the site repo.

**TRIAL RUN #1 DONE (2026-09-02 late) — Frisco luxury realtor demo, Judge PASS after 1 REVISE.** Fictional agent "Marlowe Vance" (Frisco TX), one page: 3-plane parallax hero (copy plane static), word-rise headline, sample-labeled counters, 3 listing cards + off-market band, two-row neighborhood marquee, 3-step process, about, dusk-fade → consult form (functional, inline confirm), noindex + Top Shelf attribution + RealEstateAgent JSON-LD. Artifact `64ca6469`. Source committed LOCALLY in top-shelf-website `demo/frisco-luxury-realty/website/` (ddab78b, 6dd162f) — **NOT PUSHED** (repo auto-deploys /demo live; Taylor decides whether a fictional test page goes public). 4 generated exteriors in assets/. What the trial showed: the skill's verification loop found 6 real defects; the Judge found 3 more incl. the skill being mis-applied against its own routed guard (body copy on a parallax layer). Verdict on the skill: earning its place; decision to fold into permanent menus still Taylor's after he reviews the demo + skill map.

**LAYERING REBUILD (2026-09-02, later) — Judge PASS.** Taylor: "I don't see any layering" (right — one photo + gradient ≠ planes) and "photo should be head on." Hero now FOUR planes: head-on limestone facade (far, +.14) · dusk veil (+.08, left-weighted for copy legibility) · oak-canopy cutout (near, −.22, `assets/branches.webp`, rebuilt via luminance matte after the bg-remover hollowed it) · boxwood-hedge cutout (nearest, −.34, `assets/hedge.webp`) · copy static. Parallax on phones at 0.6x; listing images get in-frame parallax on a `.pw` wrapper; light foliage silhouette returns behind the consult form (solid `--dusk-2` inputs). Latest local commit `0e4f8a1` — still **NOT PUSHED**. TODO before reusing as a real-client template: re-encode branches.webp (494 KB → ~300), shoot a phone frame of the consult section.

**LAYERED-HERO RESEARCH (2026-09-02) — Artifact `f6f116f2`.** Owner said the hedge is invisible until it moves and the top oak is hidden by the header change. Opened 12 live sites (Firewatch, Climb Wales, Palmdream, Fora, SpaceX, Apple, Mo Ibrahim, Cyclemon, NASA Prospect, Blok, Picsart, McKinnon) and distilled 7 principles. Both faults = one root: foreground must be a COMPOSED MASS legible AT REST + depth PAINTED with value/scale/blur (not revealed by motion), and the header must be static-over-hero OR turn solid only AFTER the hero (never condense in the first 40px on the art). Pending owner go-ahead: apply the fix-list to the Frisco hero (fuller hedge into lower third + oak into top corners readable at rest; atmospheric haze on facade; vignette away from the CTA; Apple-style delayed header; OR drop to the calmer SpaceX/Fora one-photo model). **APPLIED 2026-09-02 (commit a5d801a, still local/unpushed):** foreground now reads at rest (oak into top corners, hedge mass + plumes across bottom third), header stays transparent over the hero and solidifies only after it (Apple pattern), depth painted (facade desaturated + aerial-haze band, foliage darker), featured line lifted above the hedge. Verified at rest + scroll-scrub desktop+mobile, no overflow. Kept the layered approach (owner had iterated toward it) rather than the one-photo model.

**HERO REDESIGNED — "THE ARRIVAL" (2026-09-02, latest, Judge PASS after 1 REVISE), commit `e00c2b7` local/unpushed.** Owner asked for a complete rethink. New hero = viewer at the street end of a long paver drive at blue hour: gate piers + lanterns framing both edges (nearest, -0.30, `assets/gate.webp` luminance-matted, CSS bottom-mask), live-oak canopy top corners (near, -0.18, `assets/branches.webp`), lit estate down the drive under violet sky (far, +0.10, `assets/scene.jpg`), veil +0.06, copy static + centered in the sky with a radial scrim. All planes belong to ONE scene (the research lesson). Nav: transparent at rest → dark translucent while scrolling inside the hero (y>80) → stone after. Old hero.jpg/hedge.webp deleted. TODO if it becomes a real-client template: re-encode plates, shoot a phone consult frame, decide whether to push the demo live.

**OWNER VERDICT ON THE ARRIVAL HERO (2026-09-02, end of day):** "You are doing it correct but you need better assets. The photos are all of different quality and look like they are just plastered together. I will do a better test tomorrow." → Technique approved; ASSETS are the blocker. **Root cause:** the scene, gate piers and oak canopy were three separate generations, so light direction, lens, grain and color temperature don't match — the eye reads the seams even when placement is right. **Plan for tomorrow's test (in priority order):**
1. **Derive every plane from ONE master image.** Generate (or photograph) the full arrival scene WITH the gate piers and canopy in frame, then matte the near planes OUT of that same image (gate piers / canopy → cutouts; the remainder → far plane, inpaint the holes). Identical light, grain, perspective by construction. This is how Firewatch reads as one world.
2. **If the owner supplies real photos:** shoot/pick a frame where the foreground is in-camera (house seen through the gate or under the tree), and only separate that one foreground; keep everything else as-is.
3. **Unify the grade regardless:** one shared curve/LUT + a single film-grain overlay across all plates, and match black point + white balance before compositing.
4. Then re-run the at-rest + scroll-scrub verification and the Judge.
Nothing pushed; demo still local at `e00c2b7`.

---

## 🎬 LATEST (2026-08-31) — CRM SCROLL EXPERIENCE

**▶▶ v2 (2026-08-31 late) — STORY + PIXAR ART DIRECTION LOCKED; progress-style DEFERRED (START HERE) ◀◀**

The scroll is now a **movie**: **"The Contractor's Dream"** (treatment Artifact `9e10d059`; files: `tap-crm-scroll/the-contractors-dream.html` + Drive + vault `projects/top-shelf/scroll-story-treatment.md`). Owner (a contractor, **unnamed in story/visuals** — the CRM keeps "Marcus Bennett · Manager") is drowning at a paper-buried desk → a **flash-forward dream** of his business running itself → we follow **one job** (Alvarez bath) start→finish across the 3 Blends (Essentials Capture → Signature Operate → Platinum Grow), a **two-phone hand-off** throughout, **hybrid ending** (hold his smile → half-second snap to the messy desk → "Get your free audit"). **FULL COLOR throughout — no B&W.** Entry = customer taps **call** on the site → **AI receptionist** montage books it. The estimator's 7 screens **collapse to one "+"** tease (→ its own page). All 14 beats + a render inventory are in the treatment. Companion: the **Blends positioning brief** (Artifact `7a9c41a8`).

**ART DIRECTION (owner, LOCKED):** human/world scenes = **Pixar-style 3D**; the CRM screens stay **real app renders** composited into the animated devices' screens.

**GEN-MEDIA PIPELINE (works):** installed **visual-skills** (smixs, CC-BY-4.0; drop-in skills `visual-image`=Nano Banana + `visual-video`=Seedance 2.5; pushed to config + vault) — writes model-correct prompts so we don't burn credits. Image MCP = server `c209e5fb`: `nano_banana_pro` for stills incl. **image-to-image** (feed a ref, keep the room/character); `recraft_v4_1` did the real bathroom before/afters. **OWNER ANCHOR LOCKED = v2** at `assets/story/owner-anchor.png` (the Scene-00 desk) — generate every other Owner scene **image-to-image FROM it** for character consistency. Scene 13 (clean desk) already generated from it (variant pick pending, rec v2). Estimator now uses real recraft bathroom photos.

**CONTINUITY BIBLE (owner, LOCKED 2026-08-31) — apply to EVERY render:** Owner = the v2 anchor face/build; **charcoal henley ONLY in desk scenes 00/13**, **black polo + tan (khaki) pants in every other (in-field) scene**. Customer (Maria) = **slender female, black hair, yellow dress** (complements the T.A.P. logo accent), same look every scene. **Phones = one consistent design** every frame. Truck / house / CRM screens stay consistent. Method: build an Owner **field anchor** (black polo + tan) + a **customer anchor**, then generate every scene **image-to-image** from an anchor — no drift, no loose ends. Full detail → `projects/top-shelf/scroll-story-treatment.md` §Continuity Bible.

**⛔ HOLD — do NOT render the story scenes yet** (owner, 2026-08-31): waiting on the other agent to finish the **estimate / invoice / receipt** pages; owner will say go. When cleared: build the two new anchors first, then render every scene image-to-image.

**⚠️ BEFORE WE FINISH / SHIP THE SCROLL PAGE — REMIND TAYLOR (he asked, 2026-08-31):**
1. **Decide the PROGRESS-INDICATOR style** — comp Artifact `88f145ee` (`play-progress-comp.html`) offers three: **Spine** (left rail) · **Timeline + act-cards** (my rec) · **Thread** (flowing gold line). **DEFERRED** because the comp is **stationary** (cards flip in place) while the real page **scrolls down** — the Thread especially can't be judged until the page actually moves. Decide **after** the Pixar video/images exist, evaluated on the real scrolling page.
2. The **Play control** (opt-in "Play the story" pill; auto-advances the playhead smoothly, recedes while playing, any scroll takes over) is **approved** — just implement it.
3. Pick the **clean-desk (scene 13)** variant (v1 / v2).

**✅ CRM SCREENS RENDERED + REVISED to owner feedback (2026-09-01):** the 17-render app-UI gallery (Artifact `266a1c0f`, Drive `Projects/Top Shelf/crm-scroll-prototype/` + scratchpad `tap-crm-scroll/renders/out/`) is built and re-ordered to the new story. **Second-pass revisions (owner feedback, all done + visually verified):**
- **03/05/10/13 owner job screens (booked / on-my-way / send-invoice / paid+tip)** → REBUILT as full, dense, faithful job pages replicating the REAL `Contractor-Pro/resources/views/jobs/show.blade.php` (header w/ wrench + status pill + customer/address/schedule meta · 3-col Call/Text/Directions · `field-status` stepper w/ advance CTA · 3-stat strip Scheduled/Job total/Balance · line-items card grouped Services/Materials · invoice card w/ Send-or-Pay / Paid+Refund states · visit + customer cards). New builder **`_job.py`** owns these four now (`_story.py`'s old simpler versions are neutralized via a `BUILD_ONLY` guard so re-runs can't clobber them). Owner said "use what the actual app would render, it's more filled out."
- **11/12 customer pay + receipt** → line items cut 9→**5** (owner: "less line items so it appears better in the scroll"). `_customer_docs.py`, still faithful to `hub/invoice.blade.php`.
- **16 flywheel** → REBUILT mobile→**DESKTOP** as a realistic **Google search result** ("general contractor denton", T.A.P. **#1 in the map-pack**, 4.9★/63) beside a **Local-Falcon-style geo-grid rank tracker** (avg map rank #1.3 ▲ from #5.2, green #1 grid, keyword ranks, reviews 63 ▲16, calls ▲62%). Standalone `st-flywheel.html`, captured at 1440. Old `_flywheel.py` (mobile) is superseded. Owner: "a google search result with their company ranking #1 on the map pack along with SEO analytics."
- **06 pipeline** → columns densified to 8–9 cards each and captured at a **fixed 1440×860 viewport** (new `_shot.cjs` 5th-arg fixed-height mode) so the board **fills the screen with the last cards half-cut, implying more jobs below** (owner's exact example of the "empty space" fix).
- **General "fill the screen"** → swept all 17; **st-review** filled (What-stood-out tag chips + add-photos row + footer) and **st-review-notification** enriched into a real lock screen (3 stacked notifs: review→payment→auto-request + flashlight/camera controls).
- **04 inbox + 07 scheduler (3rd feedback round)** → both had a fixed-height content box sitting in a taller screen (dead canvas below). FIXED to fill the viewport: inbox grid `height:calc(100vh-104px)` + 4 more threads (14 total) so the list runs to the bottom and the conversation composer pins to the bottom; scheduler calendar card `height:calc(100vh-108px)` + `overflow-hidden` and the time axis extended 7 AM→7 PM (13 rows) so the grid outline fills to a clean bottom edge. Both now captured as fixed **1440×860** screens (same framing as the pipeline). Booking / AI-receptionist / dashboard / est-1 / est-5 were already full.

**📝 SCROLL-BUILD NOTES to honor when wiring the media (owner, 2026-09-01):** (1) any ticket taller than one phone screen (the tall job pages, customer pay/receipt) → in the video the character should **"scroll to the bottom"** so the audience sees the whole ticket; (2) the **15→16 transition** = his phone getting the review alert (lock screen) → **cut to him at his desk** watching the company climb the map-pack (the new desktop flywheel).

Built via `_job.py` (owner job pages) + `_customer_docs.py` (hub docs) + `_story.py` (review screens) + `_assemble.py` (desktop shell) + `st-flywheel.html` + `_gallery.py`. tapservices.pro captured but hero sparse (bonus).

**✅ SHOT BIBLE WRITTEN (2026-09-01, Fable 5):** the complete production script for the film — Artifact `7260c1b9` ("Dream Shot Bible"), source `tap-crm-scroll/shot-bible.html`, Drive `Projects/Top Shelf/crm-scroll-prototype/shot-bible.html`. Contents: the film's five anchors (emotion=relief · motif=the two-note chime at every automated win + glow-to-glow transitions · object=the one phone · break=papers lift · final image=smile→snap back); a **Continuity Kit** of verbatim copy-paste blocks (STYLE / BANS / OWNER + desk & field wardrobes / MARIA / PHONE / TRUCK / HOUSE / BATH-BEFORE / BATH-AFTER / OFFICE — identity text written FROM the locked anchor images); **3 pre-production anchors to generate** (A1 owner-field, A2 Maria, A3 truck+house — nano_banana_pro 2K); **18 scenes** (SC-00→SC-17, ~95s: 14 Seedance 2.5 clips + 3 build takeovers + the type ending), each with story beat, environment/lighting/camera, an i2i keyframe prompt, and a full Seedance 2.5 clip prompt (3-module anti-collapse structure: @Image role bindings → global settings+bans → ≥3s timestamped windows with end states; dialogue only in SC-03 and it's the ai-receptionist render transcript verbatim); the **screen-scroll choreography table** (every ticket >1 screen scrolls to its bottom in the build's device mask, synced to generated thumb-swipes — customer-pay 2265px is the slowest/tallest); a continuity ledger (henley ONLY desk scenes 00/00B/14/15/16/17; polo+tan elsewhere; Maria's yellow dress ×6; the chime rings exactly 5+1 times); and the generation order + per-asset QA gate (fix-don't-re-roll policy). Key architecture: **generated screens are ALWAYS blank warm glow** — real render PNGs are masked+scrolled by the build (no AI text garble); SC-16 uses first+last frame mode (last frame = clean-v2.png); SC-00B/SC-15 are mirrored push-into-the-monitor transitions. Prompt grammar per the visual-image/visual-video skills (read in full this session).

**Judge on the bible: PASS after 2 REVISE** (caught: a sun-rises-after-setting light regression · SC-15/16 had no first-frame path · beat windows under the ≥3s bar · duration ledger vs headers — all fixed + re-verified). **Then extended per Taylor (2026-09-01 mid-turn): §3 is now an APPROVAL-GATED REFERENCE LIBRARY** — every recurring asset (owner-field A1, Maria+kitchen A2, truck+house A3, **phone hero A4** — plus 4 already-locked refs) gets its image generated → **Taylor approves** → only the approved image is bound as the @Image ref in every shot containing it; keyframes get a quick thumbs-up before clips spend credits. That workflow is the doc's spine now.

**✅ REFERENCE LIBRARY LOCKED (2026-09-02, Taylor-approved):** A1 owner-field + A2 Maria approved round 1; A3 truck+house + A4 phone approved at v2 (owner notes applied: F-250-scale truck via an edit of the same scene; modern edge-to-edge bezel-less phone). Locked files: `tap-crm-scroll/assets/story/{owner-field,maria-anchor,truck-house,phone-hero}.png` (+ Drive `crm-scroll-prototype/assets/story/`). Approval page Artifact `6d9a14b2`. Bible kit blocks (PHONE/TRUCK) + §3 table updated to the approved designs and republished. Higgsfield media ids: owner-anchor `0b746eb1` · logo `cd430bf6` · before-wide `059b7bbb` · after-matched `fc30e141` · clean-v2 `1ed4b4b7`; anchor job_ids (usable as refs): A1 `e2389c9f` · A2 `99df61ad` · A3v2 `edac263b` · A4v2 `691ed807`.

**✅ ALL 14 SCENE KEYFRAMES GENERATED (2026-09-02, awaiting Taylor thumbs-up):** SC-01/02/04/05/07/08/09/10/11/12/13/14/15/16 via nano_banana_pro i2i from the locked library (SC-02 chained from SC-01’s job; SC-16 camera-matched to clean-v2). Files `tap-crm-scroll/assets/keyframes/sc*.png` + Drive. Approval page Artifact `9d438103` (“Dream Keyframes”). My QA: 14/14 usable — faces/wardrobe/phone all hold; 4 soft notes flagged on the page (SC-08 freestanding tub, SC-10 sneakers on, SC-14 leaning-in pose, SC-16 stapler). Keyframe job_ids for first-frame refs: sc01 `c3d71a14` sc02 `e7c840ee` sc04 `9a08a8cf` sc05 `ca88f3f0` sc07 `62cc7cfc` sc08 `100ac05b` sc09 `6716081c` sc10 `4bdb47b3` sc11 `e065c6dc` sc12 `a41109e4` sc13 `a6beedc1` sc14 `1f30d370` sc15 `cccccbe2` sc16 `fa0dd988`.

**📝 STORY REVISIONS from keyframe review (Taylor, 2026-09-02) — bible updated + republished:** (1) **SC-00’s monitor now shows a Top Shelf CRM ADVERTISEMENT** — the dream trigger (build composite; the ad frame is the one screen asset still to render — TODO). (2) **SC-08 (work montage) is CUT** — replaced by the MAGIC TRANSITION: camera dives into the phone’s estimator after-render → gold glow → pulls out of the phone in SC-09’s doorway with the bathroom FINISHED (render becomes the room). Film is now 17 clips / 110s. (3) **SC-16 desk-clearing syncs to the map-pack**: frame 1 fully buried; papers lift as the on-screen rank climbs; #1 pin lands as the last paper lifts; ends clean desk + #1 on monitor (monitor bookend: first monitor = the ad/promise, last = #1/kept). (4) Keyframe fixes rolled: SC-02 v3 (phone gripped, full face), SC-10 v2 (screen faces her), SC-11 v2 (phantom hand + bg truck removed), SC-14 v2 (happy), SC-15 v2 (screen faces him + happy), SC-16 v2 (fully-buried first frame). New job_ids: sc02 `a9eea459` sc10 `d1c07696` sc11 `6f7050b4` sc14 `217dab96` sc15 `980479a5` sc16 `8f499b1a`. Approval page (Artifact `9d438103`) republished round 2; Drive synced.

**🎞 ROUND 3 (2026-09-02): THE DESK GRADIENT (Taylor-directed):** the desk now cleans itself progressively — SC-00 opens on a NEW buried+defeated frame (job `46919d91`, same camera as the finale = matched bookends) → SC-14 v3 half-cleared (`47983cab`) → SC-15 v3 nearly clear w/ tray + cabinet shut (`13f2d487`) → SC-16 finishes to a NEW accomplished-clean final frame (`fd2ff37e`, assets/keyframes/desk-clean-final.png — replaces clean-v2 as the last frame; organizer tray, zero stickies, cabinet shut, accomplished smile). Phone flips: SC-07 v2 (`bcda7a72`) + SC-09 v2 (`a40720f0`) screens now face HIM. SC-16 first frame = extracted from SC-15’s finished clip (like SC-03←SC-02). Bible restructured accordingly (desk-gradient ledger bullet, SC-16 storyboard rewritten to the final-step clearing). Approval page round 3 republished (`9d438103`); Drive synced.

**🎬 16 OF 17 CLIPS GENERATED (2026-09-02, Seedance 2.5, 1080p 16:9, audio on / no BGM by design):** local `tap-crm-scroll/clips/` + Drive `crm-scroll-prototype/clips/`. Video job_ids: sc00 `9fe54cf7` · sc00b `e0a1c3b8` · sc01 `664fd564` · sc02 `b575aafa` · sc03 (dialogue, transcript-verbatim) `d3acf93b` · sc04 `5b2784bc` · sc05 `119b851f` · sc06 `eeebdab6` · sc07 `5cbeeb1f` · sc09 `1fe31d2b` · sc10 `c57b2048` · sc11 `e6db1ac7` · sc12 `b8710450` · sc13 `06a22c59` · sc14 `9a815f8e` · sc15 `6de4f6bc`. Chained first frames were ffmpeg-extracted + uploaded: sc00-last `21d0a952` (→sc00b), sc02-last `a8e12f27` (→sc03), sc15-last `c9262972` (→sc16). **Gotchas learned:** (1) a "IN THE DARK" preset recommendation intercepts desk-at-night submissions — resubmit with `declined_preset_id: 24bae836-2c4a-48e0-89b6-49fcc0b21612`; (2) the content filter false-flags certain image LINEAGES — SC-10 passed after neutral rewording, SC-15 only passed after regenerating its keyframe FRESH from owner-anchor (new keyframe job `9204c6f8`, saved as assets/keyframes/sc15.png; the flagged v3 lineage is sc15-old.png); (3) Seedance concurrency cap ≈ 6 — submit in waves.

**⛔ SC-16 (the finale: papers file themselves as the map-pack hits #1) IS THE ONLY CLIP LEFT — blocked by "Out of credits on plus (monthly) plan" on Higgsfield.** Ready to fire the moment credits exist: model seedance_2_5, mode omni_reference, 8s 1080p 16:9, start_image = media `c9262972` (sc15-last), end_image = job `fd2ff37e` (desk-clean-final), prompt = the SC-16 clip prompt in the bible (final-step clearing + accomplished smile), declined_preset_id as above.

**⏱ DREAM TIMELINE + FINAL AUDIT (2026-09-02, Fable 5.1) — Judge PASS after 2 REVISE:** Artifact `de923892` ("Dream Timeline"), source `tap-crm-scroll/dream-timeline.html` (generator `_timeline.py`), Drive `crm-scroll-prototype/`. Contents: the length verdict (Full ≈2:48 / **Site cut 2:08 = default** / Sprint ≈1:46 with itemized trims — Taylor wanted "a realization, not a saga"); the master timestamp sheet (23 beats, start times to the tenth, scroll %, clip trims, on-screen action, which render on which screen + how it scrolls, 6 numbered chime cues, cut-outs); the scroll-mechanics diagram with real renders (video layer → device-mask overlay that zooms out of the phone, no tracking → full-frame takeovers; the 4 monitor beats SC-00/00B/15/16 are perspective-mapped with sticky-note occlusion mattes); the final audit + open list. **Decisions recorded:** chimes are a build-laid sample with native clip audio ducked; SC-14’s monitor MUST be masked (baked spreadsheet); SC-14 desk = loose papers gone but stacks/stickies remain (0.5 s dissolve into SC-15); clips are HEVC → transcode to H.264 for web. **⚠ SC-16 SUBMISSION SPEC CORRECTED:** start_image = the SC-15 KEYFRAME (job `9204c6f8`, assets/keyframes/sc15.png) — NOT the sc15-last media (that frame is pure glow); end_image = job `fd2ff37e` (desk-clean-final). **Ad frame BUILT:** renders/out/topshelf-ad.png (2800×2000; "Find the gap that’s costing you." + CRM glass + "Get your free audit") for SC-00’s monitor. Judge lessons: (1) render HTML deliverables in a real browser before shipping — unescaped `<cue>` text vanished and a nowrap cell hid 4 columns, both invisible to text review; (2) a video-derived "last frame" can be a glow/transition frame — check it before chaining.

**⏱ 30-SECOND CUT EVALUATED + SHEETED (2026-09-02, Taylor: “needs to be about 30 seconds”):** §1b added to the Dream Timeline (Artifact `de923892`, regenerated + Drive). 19 beats, machine-summed to 30.0 s, ZERO regeneration (every beat is an excerpt of an existing clip + build fan-outs). Grammar change: read FOUR screens (AI-booked chip, 1,400 total, 1,600 PAID, #1 pin), glimpse the rest via (1) phone-to-phone hard cuts, (2) fan-outs of inbox/pipeline/scheduler as angled cards, (3) speed-ramped scroll-to-bottom with hard stops on the key line, (4) six chimes as the rhythm track; only dialogue kept = the AI’s line (sc03 audio 4.0–6.0). Dropped: website hero, handshake, goodbye, dashboard → 0.3 s flash. KEY FRAMING: 30 s = the autoplay pace + scroll length; stopping on any beat still holds the full readable ticket in the device mask. A 45 s variant (restores handshake/goodbye/receipt dwell) offered. STATUS: proposed cut for Taylor’s decision — if adopted, it becomes the locked sheet and gets the full Judge pass before the build.

**NEXT (build order):** ~~renders~~ ✅ → ~~bible~~ ✅ → ~~library~~ ✅ → ~~keyframes~~ ✅ → ~~16/17 clips~~ ✅ → ~~timeline~~ ✅ → ~~chime + bed~~ ✅ → ~~H.264~~ ✅ → ~~scroll page (device masks, monitor masks, cue kit, Play control)~~ ✅ (see the DREAM SCROLL PAGE BUILT block at the top) → SC-16 clip when Higgsfield credits exist (spec above) → progress style + Taylor review → deploy.

---

### Earlier (reference-image pipeline — the foundation; note the estimator now compresses to a "+"):
**Goal:** a cinematic scroll-driven hero for the Top Shelf marketing site — the camera flies through a *real* CRM (T.A.P. Services) across the three Blends (Capture→Operate→Grow), with the **AI Estimator as the engineered peak**. Design LOCKED: real-UI camera tour (not video), **scrollcraft** engine, T.A.P. flagship, **bathroom-remodel** hero job, all-fake/disposable data, ONE company throughout.
- **scrollcraft installed** (Nate Herk, MIT, via `skill-intake` → drop-in, pushed to config + vault). Zero-dep vanilla-JS scroll runtime, publishes `--sc-p`. Lives at `~/.claude/skills/scrollcraft`. We use ACT mode + `--sc-p`, no video, no KIE (Higgsfield/SeeDance available if we ever want a rendered beat).
- **Bones prototype** (scrollcraft + rough mockups): scratchpad `…/scratchpad/tap-crm-scroll/` → published artifact **ddd8351a** ("One Business, Every Blend"). Verified via localhost; SPEC.md has the full storyboard→scrollcraft device map.
- **Reference-image pipeline (current focus):** high-res, layout-accurate renders of the REAL Contractor Pro app (T.A.P.-branded) to replace the rough mockups. Flow: read real app views (`C:\Users\taylo\Contractor-Pro`) → faithful standalone HTML (Tailwind CDN + **midnight-gold** dark theme, `.surface` glass, Figtree, `brand-500 #CEA17A`) → **Playwright 2× capture** (`_shot.cjs`; playwright-core resolves from `C:\Users\taylo\Ledger Pro\node_modules`, system Chrome). Desktop screens assembled from the dashboard shell via `_assemble.py` (has a `dock` flag); mobile via `_mobile.py`.
- **ACT 1 DONE — 7 renders** in `tap-crm-scroll/renders/out/` + Drive `Projects/Top Shelf/crm-scroll-prototype/`: **dashboard** (with dock, full 44-call Recent), and Essentials **dock-free** (pipeline / inbox / scheduler), and mobile (incoming-call / AI-receptionist / booking).
- **DATA-BIBLE.md** = the fake T.A.P. company (customer cast, KPIs: $487k mo revenue / 312 calls / 41 open jobs; hero job Alvarez bath remodel, tiers $18.4/21.4/27.9k; **Marcus Bennett = Manager** persona in the chrome). **SHOT-LIST.md** = 17 screens mapped to scroll beats.
- **Phone-dock choreography (owner, 2026-08-31):** dock HIDDEN in Essentials → slides in at Signature (own-phone-line) + a little demo → stays open for Platinum. Essentials reference images are dock-free; dashboard + Signature/Platinum keep it.
- **NEXT:** Act 2 (AI Estimator peak — the ~8 estimator mobile screens: capture→interview→measure→scope→before/after→tiers→sign/pay), Act 3 externals (site/map-pack/ads/social), then **wire the reference images into the scroll bones** (replace the `.mk` mockups), then polish (true zoom/dive camera + motion), humanizer + Judge, deploy + update live topshelfsolutions.io pricing (still old). Working files: SPEC.md, DATA-BIBLE.md, SHOT-LIST.md, _assemble.py, _mobile.py, _shot.cjs.

## ⚠️ BUILDING THE INDUSTRY DEMO SITE? Read the demo-site handoff FIRST

If your task is the **per-industry demo websites** (one polished demo per trade, that cold outreach links prospects to): read **`_handoff/top-shelf-demo-site.md`** before you build. It has the hard requirements the Website outreach funnel depends on — the **opt-in form + TCPA disclosure** (the conversion point / build trigger), the **`/demos/<trade>` URL convention**, the find-the-gap voice, and the form→build seam. Added 2026-08-28 by the outreach session. Skip if you're not building the demo pages.

---

## ▶▶ CINEMATIC MARKETING SITE — homepage + Custom-Website page BUILT; positioning pivoted to "product-led, agency behind it" (2026-08-28, LATEST — START HERE) ◀◀

**What this is:** a brand-new cinematic **marketing website** for Top Shelf (Linear.app grammar, espresso+gold luxury theme), built as self-contained **Artifacts** first (approve), then productionized into the `top-shelf-website` Hostinger repo. NOT the CRM app.

**🔑 POSITIONING PIVOT (owner decision, LOCKED):** content-extraction of the REAL topshelfsolutions.io revealed Top Shelf is a **done-for-you agency** ("We Solve. You Grow."), NOT a CRM product. Surfaced the mismatch → owner chose **"Product-led, agency behind it"**: keep the cinematic product-forward homepage (CRM/AI demos as the hero visuals) but frame the MESSAGE as the agency — find-the-gap, the Stack of 10 done-for-you services, 3 Blends pricing, hire-vs-us. Hero = **Direction A: "Find the gap that's costing you."** (owner picked A from 3 options).

**LIVE ARTIFACTS (all viewable, private — owner must be signed into claude.ai):**
- **Homepage** → https://claude.ai/code/artifact/18c168d8-2df3-40f2-ae94-9d4813fbb06b (favicon ⭐). Hero A applied. Full Linear long-scroll: nav (mega-menus) · hero (16:9 glass monitor + phone, popup cards) · trust (8 industries) · manifesto (3 pillars) · Ch.01 CRM inbox · Ch.02 AI Receptionist · Ch.03 Money & Reputation · outcomes · steps · pricing · testimonial (placeholder) · CTA · footer. 9 "+" chips → right slide-in detail panels with enterprise `.mk*` mockups. Real metallic TS logo embedded. Src: scratchpad `topshelf-homepage.html`.
- **Custom Website page** (1st Stack solution page) → https://claude.ai/code/artifact/3f4a8a0f-6dd0-4174-9add-e5d735242125 (favicon 🌐). Verified in Chrome. Hero (browser-framed sample) · 8-industry showcase gallery · click industry → sample panel (preview+blurb+CTA) · "+" capability chips (Design/SEO/Mobile/Booking/Speed) · Audit→Design→Launch→Rank process · Map-Pack #1 SEO band. Src: `topshelf-websites.html`. **This is the TEMPLATE for all Stack pages.**
- **Website plan/blueprint** → https://claude.ai/code/artifact/c61f39a7-037a-4b72-946b-0b2f6c2045a6 (🗺️). Sitemap, section kit, page mockups, SEO interconnection, FieldPro→TS mapping. Src: `topshelf-blueprint.html`.
- **Hero directions** → https://claude.ai/code/artifact/4f514318-5a80-42cd-81d5-5f1db9e02a60 (🎯). A/B/C; owner picked **A**. Src: `topshelf-hero-directions.html`.

**DESIGN SYSTEM (locked, reused across all pages):** espresso ground #0B0906, gold #C9A86A/#E4C98A, ivory ramp; Cormorant Garamond (display, italic-gold `.em`) + Jost (UI) + mono labels. App-UI mockups scale via container-query `cqi`. Committed single dark theme. **Two reusable patterns to copy everywhere:** (1) the **`.dpanel`/`.scrim`/`.dp[data-panel]` right slide-in detail panel** (opened by `.fchip[data-panel]`/`[data-panel]` click; ×/scrim/Esc close; body scroll-lock) — copies Linear's Intake "Customer Requests +" interaction; (2) the **`.mk*` enterprise mockup kit** (framed gold-lit panels: list rows w/ avatars, phone, kanban, call screen w/ waveform+livepulse, rating dashboard, invoice) — replaced the basic `.wf-*` skeletons after owner said renders were "not Enterprise level."

**OWNER DECISIONS this thread (LOCKED — do NOT re-ask):** positioning="product-led, agency behind it" · hero=A · pricing lives ONLY on the Pricing page (3 Blends: $499 / $899 Most Popular / $1,599, with struck-through hire-it comparisons) · mention a different CRM for **non-service** businesses · main blog/Guides hub now, fill later · every section splits into its own page, linked via the "+" side-panel (visual + short blurb + button to that page) · pull real content/photos/sections/context from topshelfsolutions.io · Custom Website is a Stack sub-page WITH subpages = **8 fresh generic navigable industry sample sites** the customer can click through.

**WHERE WE LEFT OFF:** TRUE MULTI-PAGE site with hash routing — **12 routes built + verified: home · CRM · AI Receptionist · Reviews & Reputation · Booking & Calendar · Payments & POS · SEO & Marketing · Automations · Pricing · Company · Industries · Guides.** Nav FULLY wired: The Stack mega-menu = 8 items (added SEO & Marketing + Automations), Industries mega-menu all → `#/industries` hub, Company/Pricing top-links routed, footer + mobile menu all routed. New pages (2026-08-29): SEO (custom Google map-pack 3-pack mock — verified), Automations (reuses textback mkph + followups mkseq), Company (editorial, honest — GDR/TAP as the only real proof, no fabricated stats), Industries (8-trade card grid w/ icons — verified), Guides (6 placeholder guide cards, "fill later" per owner). **ONLY REMAINING = the Custom Website page (#/websites) + its 8 industry sample sites** — these are the "custom sites" the owner said to build LAST (2026-08-29: "create all the pages except for the custom sites").

**✅ BLENDS PAGE built (#/blends, 2026-08-29) — the visual "show-off" (owner-specced, repeated back + confirmed):** ONE long page, extremely visual, CUMULATIVE. Hero → **Essentials $299** section (shows off all 6 things it comes with — website-w/-basic-SEO browser preview [span] + AI call w/ waveline + reviews phone + booking slots + inbox + pipeline) → **"Everything in Essentials, plus · Signature $899"** section (6 new: invoice, estimates, softphone, workflow, map-pack, reputation) → **"All of that, plus · Platinum $2,500"** section (4 new: ads campaign, social/GBP post, premium content, support chat). 16 showcase `.bunit` cards, each = a real reused `.mk`/`.mkph` mockup + caption with a `.tlink` INTO the matching Stack page (heavy internal linking, per owner). New CSS `.btier`/`.bplus`/`.bshow`/`.bunit` in the appended `<style>`; 3 new mini-mocks (softphone, ads, social post) authored inline. Added "Blends" to top-nav + mobile + footer. Complements Pricing (Pricing = numbers, Blends = visual). Verified: hero + Essentials render great; Signature/Platinum structurally confirmed via JS (headers/prices/units/links all correct) — full scroll-screenshot blocked by this Chrome's broken viewport (window+body both cap at ~56px scroll on a 10,087px page; **note: `body{overflow-x:hidden}` makes BODY the scroll container, computed `overflow-y:auto` — scroll `document.body.scrollTop`, not `window.scrollTo`, when verifying long pages via localhost**).

**✅ BLENDS PAGE REDESIGNED via a REAL 2-seat council (2026-08-29, owner: "more separated, minor descriptions + why each is good for the customer; a layout the CEO agent and client agent can agree on"):** dispatched BOTH seats for real (per [[dispatch-every-council-seat-for-real]]) — a CEO general-purpose agent + a "Marcus, skeptical 4-truck home-services owner" agent — they CONVERGED and I built the agreement. The old `.bunit` show-off was REPLACED: (1) **tiers = separate rooms** — each `.btier` (Essentials plain / `.sig` gold-glow / `.plat` darker) opens with a big centered **`.bthd` header** (name + price + $0-down chip + one positioning sentence) and a thin **sticky `.btag`** (blend + price); (2) **cumulative made loud, not repeated** — upper tiers open with a **`.brecap` chip strip** ("You keep everything in Essentials" ✓chips) + a **`.bdiv` "plus, in Signature" divider**, then only NEW services get cards; (3) **every service = one bounded `.svc` card**, consistent anatomy **№·INCLUDED → name → 1-line description → mockup → gold-left-border `.sben` benefit line → link**; benefit copy in the owner's currency (money/time/jobs), no jargon/fake stats. **⚠️ Gotcha: my tier-header class `.bhead` COLLIDED with the invoice mockup's existing `.bhead{display:flex}`** → renamed mine to `.bthd` (watch for class-name collisions when adding CSS to this file). Verified via localhost (header + card anatomy visual; `.sben`/`.btag`/`.brecap`/`.svc` computed styles all correct). Full CEO + Marcus write-ups: this session's transcript. **OWNER PICKED THE CEO VERSION** (route `#/blends-ceo`) over the card version (`#/blends`) — 2026-08-29 — then asked to make it "more creative, less back-and-forth L/R, Awwwards/Linear ideas, more + buttons → side panels with diagrams." So `#/blends-ceo` was REBUILT (via `scratchpad/_blends_rebuild.py`, since removed) into a **varied editorial layout**, per tier: a full-width **flagship `.zrow` spotlight** → a **2-up `.btwo` split** (two `.bcard`s) → a compact **`.bstrip` of `.bmini` "+" cards** under a `.bsub` "Also in…" label. Essentials & Signature = flagship+2-up+strip; Platinum = flagship+strip. **16 "+" triggers** (7 `.fplus` chips on flagships/cards + 9 `.bmini` cards) each open the slide-in `.dp` panel with a **diagram**. Added **8 NEW panels** (website, booking, phoneline, localseo, ads, social, content, support) reusing the existing `.mk`/`.mkph` mockups; existing 9 panels reused → 17 total. **⚠ Critical wiring fix:** the panel-open handler only bound `.fchip`; rewired to a delegated `document` click listener matching `.fchip,.fplus,.bmini` (else the new triggers do nothing). New CSS `.zacts/.fplus/.btwo/.bcard/.bsub/.bstrip/.bmini/.bmt/.bpp` (no collisions, grepped). Still reachable via the in-page TOGGLE (claude.ai viewer doesn't forward URL `#hash`): `.tlink` under `#/blends` hero → `#/blends-ceo`; "← card version" in the CEO banner → `#/blends`. **NEXT: once owner signs off, PROMOTE `#/blends-ceo` to be THE `#/blends`, drop the card version + the toggle banner.**

**✅ PRICING UPDATED TO THE LOCKED LADDER (2026-08-29):** built the full **Pricing page (#/pricing)** + rewrote the homepage teaser to the owner-LOCKED 3-tier ladder (source = the 2026-08-19 handoff §"PRICING LADDER LOCKED", below in this file): **Essentials $299 · Signature $899 (Most Popular) · Platinum $2,500 (application-only)** with per-tier feature lists, terms (no setup fee · $0 down · 6-mo then MTM · per domain · no refund), a "why $0 down" section, and an honest pieced-together comparison (~$1,300–1,600/mo from `00-research-pack.md`). RETIRED: the old Essential $499 / Premium $1,599 cards + the fabricated $9,605/$7,345/$1,582 hire-out strikes. All 11 Pricing / "See pricing" links now route to `#/pricing`. Verified via localhost.

**✅ SOURCE OF TRUTH CODIFIED (2026-08-29, owner: "update the source of truth… save the old blends in a v1 Blends folder"):** (1) **`offer.py` rewritten** to the v2 ladder — Essentials $299 / Signature $899 (default pick) / Platinum $2,500 (application_only) + per-tier feature lists (`ESSENTIALS_INCLUDES` / `SIGNATURE_ADDS` / `PLATINUM_ADDS`), `UNIVERSAL_INCLUSIONS` now includes **basic on-page SEO**, `AD_ADDON_AMOUNTS` {$500,$750}, `OPERATE_RESERVE_RUNG` $599 (not live), `NO_REFUND`/`NO_CANCEL_ANYTIME`; kept every linter-consumed symbol (PLANS/AUTHORIZED_AMOUNTS/RETIRED_PLAN_NAMES/REFERENCE_CLIENTS/UNKNOWNS/plan_names/TERM) — python-validated. (2) **v1 archived verbatim** → `.claude/skills/topshelf-seo-audit/scripts/topshelf/_archive/v1-blends/` (old offer.py + README). (3) **`context.md` pricing section rewritten** to the locked ladder. (4) **Essentials now includes the website WITH basic SEO** (owner add) — on the site + offer.py. Committed: skill repo `topshelf-seo-audit` @ 17cbfe5. ⚠️ **STILL STALE: the LIVE topshelfsolutions.io** (old pricing) — a separate outward-facing DEPLOY, not done, needs its own go.

**✍️ TYPOGRAPHY FIX + RULE (2026-08-29, owner: "g/y cut off at bottom of hero"):** Cormorant's long descenders were clipped by the home-hero reveal-mask (`.hero h1 .line{overflow:hidden}` had only `.03em` bottom room) and cramped by tight line-heights elsewhere. FIXED: `.hero h1` line-height 1.0→1.06 + `.line{padding-bottom:.22em;margin-bottom:-.12em}` (descender room INSIDE the mask, negative margin keeps line spacing) + reveal start `translateY(112%→125%)`; `.shero h1` 1.02→1.09 (+`.06em` pad); `.close h2` 1.04→1.10. **RULE going forward:** every Cormorant display heading uses line-height ≥1.08 and never sits flush against an `overflow:hidden` edge; any text reveal-mask needs `padding-bottom ~.2em` + compensating negative margin. New pages inherit this automatically via the shared `.shero h1`/`.close h2`/base-`h2` classes. Verified via localhost zoom on the home + Payments heroes (g/y/p/j all clear).

**📱 MOBILE NAV added (2026-08-29):** the nav had NO mobile menu (`.navlinks` was `display:none` ≤900 with nothing replacing it — owner reported "no navigation on mobile"). Added a hamburger (`#hamb`, 3 bars→X) + a full-screen `.mobmenu` overlay (Cormorant serif links, gold `.mm-lbl` section labels, gold CTA, `#mmclose` × top-right); a small JS IIFE toggles it and closes on any link / × / Esc with body scroll-lock. Gated by `@media(max-width:900px){.hamb{display:flex}.nav .cta{display:none}}` (desktop `.navlinks` already hid there). Menu design + × verified via localhost; NOTE claude-in-chrome `resize_window` does NOT change `innerWidth` (stayed 2560) so a phone viewport can't be emulated there — the collapse itself is standard media-query CSS, trust it or test on a real device.

**🔧 `<meta charset="utf-8">` added as line 1** (2026-08-29) — the raw scratchpad file had none (the Artifact publish injects one, so the live artifact was always fine, but local serving mojibako'd —/→/★). Harmless in the artifact, required for the localhost check below.

**🔑 FULL-SITE VERIFICATION now works via localhost** (extends the waveline trick): `python -m http.server <port>` in the scratchpad, open `http://127.0.0.1:<port>/topshelf-homepage.html?v=N#/<route>` in Chrome — it's a DIRECT page (not the artifact iframe) so nav/scroll/screenshots all work AND the router reads the URL hash, so you can jump straight to any route. Bump `?v=N` to bust Chrome's cache after edits. This is how I verified all 5 pages. Kill the server by port when done. See memory [[verify-artifact-animation-via-localhost]].

**🏗️ MULTI-PAGE ARCHITECTURE (owner ask 2026-08-28: "new stand-alone pages that link to each other, each service its own page with the + side-panels, display-heavy, copy Linear.app's page structure"):** Artifacts are sandboxed → separate artifacts can't link. Solution = ONE artifact (the homepage `topshelf-homepage.html`, URL 18c168d8) with a **client-side hash router**: each page is `<main class="route" data-route="X">`; nav/footer links use `#/X`; router (2nd `<script>`) shows the matching main, force-reveals its `.reveal`s, closes any panel, scrolls top. `#/`-prefixed hashes route; plain `#anchor` still scrolls in-page. Home = `<main class="route on" data-route="home">` (visible by default, no-JS safe). Splits into real .html files at productionize (trivial). **NEW CSS** (appended `<style>` before the scripts): `.route`/`.route.on` + `routein` anim · `.shero` (centered feature-page hero + backlit glow) · `.stackrow`/`.slinks`/`.slink` (the sibling cross-link cards). Everything else REUSES the existing design system + the 9 global `.dp[data-panel]` panels + the `.mk*` mockup kit.

**LINEAR PAGE GRAMMAR (studied live 2026-08-28, copy per page):** mega-menu = 4-6 feature pages (icon + name + one-line desc) + secondary links + a "New" highlight. Feature page = eyebrow + oversized headline hero (backlit product glow) → repeating feature blocks [heading + copy + "Feature +" expandable] each followed by a big product visual; sub-features as heading-stacks, UPPERCASE-label pairs, or 2×4 mini-grids → **"Make your X self-driving" cross-link row to the sibling pages** (THE interconnect) → testimonials → closing CTA → footer sitemap (identical every page).

**✅ VOICE WAVELINE (owner tweaks 2026-08-28→29, 4 iterations):** `.vscope` = a `<canvas>` oscilloscope drawn per-frame (1 shared rAF over all 3 instances: home Ch.02 AI showcase, `receptionist` panel, AI-page block 01). Evolution: bars → SVG scroll-wave → canvas → **conversation-gated canvas**. Final look (owner-approved knobs): tall amplitude (`h*0.72*…*0.48`, scope height 58px), tight frequency (`lf 0.12±0.07`), fast (`t+=0.03`, carrier `t*7`), and a **`speechLevel(t)` conversation gate** — a 12-unit `PHRASES` timeline of talking bursts (with syllable pulsing) separated by REAL flat pauses (~38% silence, varying gap lengths = turn-taking). Each scope offset by `seed*2.3` so they differ; tiny 0.02 idle floor; reduced-motion draws one frame. All params live in the 3rd `<script>` block. To retune: pause lengths/rhythm = `PHRASES`+`LOOP`; height = `.vscope` px + the `h*0.72`/`0.48`; density = `lf`; speed = `t+=`.

**🔑 REUSABLE — how to actually SEE an artifact animation (this session's big unlock):** the claude.ai artifact viewer wraps the page in a cross-origin iframe → screenshots time out, wheel/JS can't reach in, clicks toggle zoom. FIX: drop the component into a tiny standalone `.html`, serve it `python -m http.server <port> --bind 127.0.0.1` from the scratchpad, and open **`http://127.0.0.1:<port>/…`** in Chrome. That's a DIRECT page (no iframe) → screenshots are instant AND `javascript_tool` runs in-page (I sampled `speechLevel` across the loop to prove the pauses). Add an on-page readout (`loop t / level`) since rAF throttles under CDP so timed screenshots stall on one frame — read the numbers instead. Kill the server by port after (`Get-NetTCPConnection -LocalPort <port>` → `Stop-Process`). Complements memory `browser-pane-no-composite-verify`.

**✅ AI RECEPTIONIST page built (#/ai, 2nd service page):** hero "The calls you can't take, answered." → 01 Always answers (AI call mk WITH the waveline + livepulse + transcript + booked) · 02 Books the job (mkph confirmation SMS) · 03 Nothing lost (mk call-log w/ AI summaries) → sub-feature grid (24/7 · qualifies&books · sounds human) → cross-link row → CTA. Reuses `receptionist`/`followups`/`pipeline`/`inbox` panels. Nav "AI Receptionist" + home Ch.02 learn-more wired to #/ai. Structure verified (3 balanced mains: home/crm/ai). Live-nav screenshot blocked by the claude.ai viewer's zoom-on-click quirk — implementation sound, owner to eyeball.

**✅ CRM & INBOX page built (the TEMPLATE):** hero "Every conversation. One inbox." → 3 feature blocks (01 Unified inbox = mk inbox list · 02 Speed to lead = mkph textback phone · 03 Pipeline = mk kanban) each with reused "+" chips (inbox/pipeline/textback/followups panels) → sub-feature 3-grid → "The rest of the Stack" cross-link row (→ ai/reputation/booking/payments) → CTA. Only CRM's nav link (#/crm) + the home Ch.01 "Learn more" are wired live; other Stack menu items are inert `#` until their pages exist.

**REMAINING STACK/CONNECTING PAGES to build (same template):** AI Receptionist (#/ai — reuse `receptionist` panel + mk call/waveform) · Reviews & Reputation (#/reputation — reuse `reputation`/`reviews` panels + mk rating dashboard) · Booking & Calendar (#/booking) · Payments & POS (#/payments — reuse `payments` panel + mk invoice) · SEO & Marketing (#/seo) · Automations (#/automations — reuse `followups` panel + mk workflow) · Custom Website (#/websites — port topshelf-websites.html content) · then Pricing (#/pricing, 3 Blends), Company (#/company), Industries hub (#/industries). Wire each page's nav/footer link + the sibling cross-link targets as built. Sample sites LAST (owner: "build the actual sample websites last").

**✅ DONE — homepage agency reframe (2026-08-28):** manifesto → "We don't just manage your tech — we grow what you take home" + pillars More Reputable / More Visible / Easier to Run · STEPS → Audit → Build → Grow process · pricing teaser → real **3 Blends** ($499 / $899 Most Popular / $1,599, each with the true "vs $X/mo to hire out" strike) + "$9,605/mo vs from $499" hook + "See full pricing →" · nav mega-menus **Platform/Services → The Stack / Industries** · footer → agency line + The Stack + "We Solve. You Grow." · `<title>` CRM→Top Shelf. All copy pulled from the REAL topshelfsolutions.io voice + verified numbers (fact-safe). Humanizer + fact-gate + Judge still run at Ship, not on this preview. STILL TODO on the homepage: gateway buttons inside the "+" panels (deferred — they'd link to Stack pages that don't exist yet).

**⚠️ RECONCILE BEFORE SAMPLE SITES:** a parallel outreach session added `_handoff/top-shelf-demo-site.md` (per-trade demo sites cold outreach links to, at `/demos/<trade>`, WITH an opt-in + TCPA form = the conversion point). My "8 fresh generic navigable sample sites" for the Custom-Website gallery may be the SAME artifact or need to align (URL convention, opt-in seam, find-the-gap voice). READ that handoff and reconcile before building the sample sites so the two threads don't diverge.

**NEXT STEPS (build order):**
1. ~~Finish the homepage "agency" reframe~~ ✅ DONE (see above).
2. **Build the 8 live navigable sample sites** the Custom-Website gallery links to (Home Services template FIRST, then Restaurant/Medical/Law/Retail/Auto/Salon/Your-Brand) — real click-through mini-sites, not just preview panels.
3. **Rest of the Stack pages** (AI Phone · CRM · Reviews · Marketing · Booking · Payments · Automation · Memberships · POS) using the Custom-Website template · the 6 trade/industry SEO pages + a non-service CRM page · the real **Pricing** page (3 Blends) · **Company** · **Guides** hub. All hub-and-spoke interlinked (breadcrumbs, footer sitemap, schema).
4. **Productionize** into `C:\Users\taylo\top-shelf-website` (static HTML, Hostinger auto-deploy): author against PROD tokens `--ground/--surface/--ink`, run humanizer on ALL public copy + deterministic fact-gate (build a Top Shelf `facts.json`) + Judge BEFORE go-live; mirror finals to Drive.

**CONTENT BIBLE:** `C:\Users\taylo\top-shelf-website\index.html` + `pricing.html` (extracted this session) = the source of real copy/pricing/services/photos. Reuse it; don't invent stats.

**TRAPS:**
- **Browser-pane verification is DEAD** → verify via **Claude-in-Chrome** (owner signed into claude.ai renders private artifacts). Cross-origin artifact iframe: only **coordinate clicks + scrollbar-drag (x≈1558)** reach in — find/read_page/keyboard/wheel do NOT. `Page.captureScreenshot` times out on 1st try after any action → **retry immediately**. The claude.ai viewer toggles zoom fit↔100% on clicks (shifts coordinates). See memory `browser-pane-no-composite-verify`.
- **Artifact CSP:** no external JS (no Lenis/GSAP) — motion = CSS + IntersectionObserver + `@starting-style`, gated by a `.js` class so content shows without JS. Production gets the real cinematic engine.
- Prod `site.css` tokens (`--ground/--surface/--ink`) ≠ artifact tokens (`--bg/--panel/--heading`) — author prod pages against prod names.
- FieldPro dashboard is **queue-based** (Today's jobs + Needs-attention rail), reskinned Top Shelf — source `FieldPro/resources/views/livewire/dashboard.blade.php`.

---

## ▶ CINEMATIC HOMEPAGE — brainstorm + hero mock (2026-08-27, SUPERSEDED by the block above)

**A NEW thread** — a brand-new **marketing homepage** for `top-shelf-website` (the Hostinger static site, NOT the CRM app). Distinct from the lead-finder + Reids threads below.

**⏩ UPDATE — FULL HOMEPAGE BUILT (owner said "go ahead and build out the homepage"), awaiting review.** Iterated the hero on the Artifact through the owner's steers, then built the complete Linear-style long-scroll homepage. Same Artifact URL https://claude.ai/code/artifact/18c168d8-2df3-40f2-ae94-9d4813fbb06b · source scratchpad `topshelf-homepage.html` (ephemeral). **Owner steers, in order (all locked):** (a) hero CRM screen = FieldPro's REAL dashboard (Today's jobs + Needs-attention rail — queue-based, NOT KPI tiles) reskinned Top Shelf, read from `FieldPro/resources/views/livewire/dashboard.blade.php` + partials; (b) 16:9 desktop MONITOR + a PHONE in front-right (mobile view); (c) BIGGER, Linear-app scale; (d) add Linear-style floating POPUP callout cards with text. **Sections:** nav · hero (headline top + big monitor/phone composition + 3 popup cards) · trust · manifesto (3 pillars) · Ch.01 CRM (unified-inbox glass panel, missed-call→booked story, 4-up subgrid) · Ch.02 AI Receptionist (live-call showcase: waveform + transcript + booked — the 2nd demo, BUILT from scratch) · outcomes (3 category tiles) · get-started steps · pricing teaser (NO fabricated number) · testimonial (PLACEHOLDER, flagged in-copy) · closing CTA · footer. **Tech:** app UIs scale via container-query `cqi` units; motion = CSS + IntersectionObserver reveals + hero mouse-tilt; **NO Lenis/GSAP** (Artifact CSP blocks external scripts — production gets the real cinematic engine). Reveals made robust (visible without JS via a `.js` gate). **Browser-pane verification is DEAD here** — delivered unverified visually; owner is the eyes. **Production TODO on approval:** Lenis+GSAP cinematic scroll · author against PROD token names (`--ground/--surface/--ink`) · wire into `top-shelf-website/index.html` · fact-gate + humanizer + Judge before go-live · mirror to Drive.

**Owner ask (verbatim, distilled):** "build a website for Top Shelf that uses the cinematic website builder to use the Top Shelf CRM as the homepage Hero, and on that page a couple other demos like the AI assistant… use the top shelf demo page we built the other day [the live working demos] as the material… the website design is going to be 100% new in the style of linear.app but themed as Top Shelf. I want the demos on floating glass screens, very modern, futuristic and luxurious. Start with just the homepage." Then: brainstorm-first, spin up research agents, interview me, deliver a written brainstorm to approve before building.

**Owner decisions (LOCKED — do NOT re-ask):**
1. **Build target:** standalone self-contained page first (preview + approve), THEN wire into `top-shelf-website` → Hostinger.
2. **Homepage demos = 2 ONLY:** the **CRM (hero)** + the **AI Receptionist** (second demo). Everything else (Missed-Call, Reputation, Booking, Softphone…) → a LATER separate **demo-navigation page with per-service subpages**, which INHERITS this homepage's finished style so we don't rebuild. Out of scope for this build.
3. **Demo behavior:** HYBRID — hero CRM lightly interactive / scroll-driven; AI Receptionist = cinematic auto-playing showcase + a "Try it live →" link.
4. **Page scope:** FULL flagship long-scroll.
5. **CRM re-color concern:** owner worried the reused CRM demo would clash with Top Shelf colors — VERIFIED against source it's ALREADY espresso/gold Top Shelf theme (identical tokens), so no re-color needed. If owner was picturing the real CRM app screenshots, we are NOT using those.
6. **Headline:** keep the existing angle ("never miss the call that pays you" / "your whole business, one place"), tweak later.
7. **Hero CRM screen = DASHBOARD** (KPI cards + overview: 7-day leads chart + recent-activity feed), NOT the unified-inbox view — owner steered this on the first mock.

**Approach chosen = A:** reuse the existing interactive CRM demo as the hero glass screen; BUILD the AI-receptionist showcase new (no interactive asset exists — only a static bento tile); wrap both in a new Linear-grammar long-scroll with cinematic Lenis+GSAP glass motion.

**DONE this session:**
- Full written brainstorm delivered (direction, section-by-section homepage structure, motion/tech plan, 3 approaches, guardrails).
- 3 research agents run (reports captured in-session): demo-asset inventory + integration target · cinematic/glass technique map from our skills · live floating-product-UI references.
- **Hero direction mock BUILT + published as a viewable Artifact:** https://claude.ai/code/artifact/18c168d8-2df3-40f2-ae94-9d4813fbb06b — CRM DASHBOARD on a floating frosted-glass screen (mouse-tilt, gold aurora, KPI cards + 7-day leads chart + recent-activity feed), dark-luxury Linear layout, Cormorant+Jost, espresso+gold, `@starting-style` entrance (robust w/o JS). Source: scratchpad `topshelf-hero-mock.html` (ephemeral — rebuild/republish from the design if the scratchpad is wiped).

**KEY FINDINGS (load-bearing):**
- Reusable asset: `vault/projects/top-shelf/crm-page-linear/renders/crm-demo-interactive.html` = the ONLY genuinely interactive demo (self-contained, only Google-Fonts dep, already espresso/gold) → becomes the hero CRM screen (reuse 5/5). The other 5 renders are static + depend on the render `site.css`.
- **No interactive AI-Receptionist asset exists** → build it (cinematic showcase: call comes in → AI answers w/ waveform + live transcript → books the job → confirmation SMS).
- Integration target `C:\Users\taylo\top-shelf-website`: plain static HTML, NO build step, shared `assets/site.css` + `assets/site.js`, GSAP+fonts via CDN, deploys via Hostinger GitHub-App auto-publish on push to `main`, host never deletes files. **Token-name mismatch gotcha:** production `site.css` uses `--ground/--surface/--ink-*/--hairline`; renders use `--bg/--panel/--heading/--line` → AUTHOR THE NEW PAGE AGAINST PRODUCTION TOKEN NAMES so wire-in is trivial. `/demo/` in that repo = prospect sample sites, NOT product demos.
- Cinematic stack (from `cinematic-gsap-lenis-motion-system` + `build-awwwards-quality-sites` effects-catalog): Lenis (lerp .08) + GSAP ScrollTrigger; glass recipe = `glass-dark-ui` + `css-border-gradient` + `beautiful-shadows`; sticky card-stack hand-off between demo screens; NO live WebGL shader; reduced-motion → static final states. Reference anchors: Stripe (backlit tilt), Attio (framed panels on scroll — closest CRM analog), Raycast (frosted chrome), Family (scroll screen-swaps). Linear-now hero = a live self-animating in-product demo + "FIG x.x" spec labels (borrowed the FIG label).

**Where we left off:** awaiting owner's reaction to the hero mock — approve the direction → write spec + build plan, or steer (more/less glass, gold temp, motion intensity, headline, layout).

**Next 3 steps:**
1. Owner reacts to the Artifact. On approval → write the design SPEC to `vault/projects/top-shelf/cinematic-homepage/` (commit), then a writing-plans implementation plan.
2. Build via `website-design-lead` / Funnel B (Awwwards Showcase): Direction (lock evolved glass design system + prod token names) → Build (structure + the 2 glass screens) → Animate (Lenis+GSAP) → Review (design-review-live + impeccable + Judge) → Ship.
3. At Ship: humanizer on ALL public copy + deterministic fact-gate against a Top Shelf `facts.json` (build one if none) BEFORE go-live; wire into `top-shelf-website` index.html + deploy to Hostinger; mirror to Drive.

**Traps:**
- Browser-pane verification is DEAD on this machine (won't composite → screenshots time out; in-app browser not signed into claude.ai → can't load private artifacts). Verify by handing the owner the Artifact link, or console/js on a live URL only. See memory `browser-pane-no-composite-verify`.
- Mock copy is placeholder; fact-gate + humanizer apply to the REAL homepage copy at Ship, not the mock.

---

## ▶ LEAD-FINDER SCORING — competitive scan + enhancement SPEC drafted, Judge PASS, awaiting 3 owner decisions (2026-08-27)

**A DIFFERENT thread from the Reids work below** — this is about the prospecting *pipeline* (the `topshelf-lead-finder` skill), which lives in its OWN git repo at `C:\Users\taylo\.claude\skills\topshelf-lead-finder` (NOT top-shelf-crm / top-shelf-website). Commit/push there.

**Owner ask (verbatim, across the session):** first "search the repos/skills for anything that can help evaluate who a good client would be for Top Shelf to prospect to… small businesses with no website or a poor quality website, not a franchise." Then, after a competitive scan, **"yes do all of it"** = (A) verify the public Claude-skill marketplaces hands-on, and (B) draft the two proposed enhancements as a spec.

**Where we left off — DONE:**
- **Competitive scan → the field is clear.** No public tool/skill scores local SMBs by website-presence gaps. Inspected hands-on: `Nikkk2312/ai-lead-scoring-engine` (B2B tri-dimensional — not our problem), `prantikmedhi/b2b-leads-ai` / LeadFinder AI (binary has-website filter, no scoring / no franchise exclusion), Salesably marketplace, louisblythe/Sales-Skills, AugmentClaude — all B2B firmographic/intent; two say verbatim you'd have to build the website-gap scorer custom. `topshelf-lead-finder` is genuinely ahead of its niche.
- **Spec drafted, committed, Judge-gated, published.** `specs/2026-08-27-scoring-signal-and-llm-enhancements.md` in the lead-finder repo, commit **3f02411**. Two enhancements, both preserving the deterministic doctrine: (A) a presence-only **budget (`runs_paid_ads`) + active-owner (`owner_active`) signal layer** captured at existing passes (sitescan HTML parse + Stage-2 GBP read), feeding **`work_rank` only** — never `lead_score` / `recommended`; (B) an **optional advisory LLM review pass** over the narrow modern/basic band, written to the evidence store, never a score input.
- **Judge: PASS after 1 REVISE** — confirmed determinism is genuinely preserved (traced `work_rank` through every consumer); caught 3 real defects, all fixed: unconditional `config["signals"]` access → `config.get(…, default)`; pinned the boost insertion point BETWEEN `lf_scoring.py:206` and `:214`; softened a GTM raw-HTML overclaim. Episodic engineering.md entry written (vault **4aaf0e1**).
- **Viewable spec (Artifact):** https://claude.ai/code/artifact/49321b99-1a24-4669-a393-6bb272c66043

**Next 3 steps:**
1. **Owner decides 3 open questions** (spec §5): boost cap (default +15%?); build the LLM pass now vs land the signal layer first (recommend signal-layer first — near-free); Meta-Pixel-only = full ads boost or reduced (recommend full, `confidence: med`).
2. **Build steps 1–4** (the near-zero-cost signal layer): sitescan pixel detection + `signals` block → `lf_scoring` bounded `work_rank` boost + `config.yaml` block → Stage-2 `owner_active` capture → `notes` enrichment. TDD (spec §3 lists the exact tests). Validate the +15% cap's threshold-crossing against the live 351-row Denton file.
3. **Optional step 5** — `scripts/llm_review.py` + a gated Stage-1b step, only if owner wants it.

**Traps for the next session:**
- The lead-finder skill is a **separate git repo** with its own origin — commit/push there, not in a CRM/website repo.
- Pre-existing (not from this session): `work_rank_threshold` (45) still needs recalibration against the live 351-row file (noted in the skill's own scoring-rubric); the scoring weights are calibrated on only ~6 owner approvals (an unvalidated prior).
- Housekeeping hit this session: the shared **vault** had 6 uncommitted orphan files + one stray `autostash` stash from a prior unfinished session (agents.index.md / skills.index.md / intake-queue.json / etc.). Restored them untouched and left the stash — someone should reconcile or discard them.

---

## ▶ REIDS CRM SUB-ACCOUNT — SHIPPED + verified live on prod (2026-08-26)

**Goal:** Reids Auto Connection as a sub-account (`Location`) in the Top Shelf CRM (`C:\Users\taylo\top-shelf-crm`). Reid logs into ONLY his sub-account; his customers = auto-loan applicants; the co-applicant is NOT a customer (lives inside the application only). PLUS per-client theming (Taylor added mid-build): each client's login + CRM themed from their website colours; Top Shelf keeps its dark brand.

**STATUS: DEPLOYED to `main` + LIVE-VERIFIED as Reid.** `git push origin main` (317f2d3..97a4e7f) → Railway deployed in ~80s → the guarded migration provisioned Reids on prod. Logged into `app.topshelfsolutions.io/login/reids` (reid@reidsautoconnection.com / password) via authenticated Inertia-props fetch: navy `#0e1b3a` light theme, `is_house:false`, **`clients[]:0`** (no agency leak), analytics 3820/47, exactly 3 customers (Marcus Wells / Alicia Grant / Devon Price), Devon's Application shows co-applicant Brianna Price (spouse), SSNs masked. `.dark`-over-light bug found in live verify + fixed (97a4e7f: initializeTheme yields to a client theme) → re-verified `htmlHasDark:false`. Website form LIVE on Hostinger (topshelfsolutions.io/demo/reids-auto-connection/website/get-approved.html POSTs to the intake; CORS 204 preflight OK, bad-token 404). Website file filed to Drive. **CRM checkout restored to `redesign/customer-facing`** (that WIP branch does NOT have this work — it lives on main).

**Branch:** built on `feat/reids-subaccount`, FF-merged to `main`, pushed + deployed. Taylor CONFIRMED app.topshelfsolutions.io deploys from **main**.

**DONE — TDD throughout, full suite 360 pass / 0 fail:**
- **Intake** `POST /api/applications/{token}` (`PublicApplicationController`) — resolves Location by webhook_token; ONE applicant Contact; co-applicant grouped under `payload.co_applicant` only (never a 2nd contact); SSN masked to last-4 (client AND server). Accepts the REAL get-approved field names (first/last/co_*/ssn/salary/housing/…), allow-listed. `form_submissions.funnel_id` made nullable.
- **Isolation** — `SetCurrentLocation` defaults a non-admin INTO their own location; new `EnsureAgencyAdmin` (`agency` mw) gates clients / agency-companies-store / phone* / prospecting / **webrtc-token / device-token**; `clients` switcher prop is admin-only; sidebar/switcher/context-banner de-agencied for a client login.
- **Review page** `GET /company/contacts/{contact}/application` (`contacts/application.tsx`) — applicant + co-applicant, renders payload generically (known groups + catch-all). "Application" card on `contacts/show.tsx`.
- **Analytics** — `business_profile.analytics` → "Website performance (Sample)" dashboard panel (div bars, no chart lib).
- **Theming** — `business_profile.theme` (mode+tokens) → `ClientTheme` resolver → `app.blade.php` pre-paint (inline on `<html>`, no flash) + `useClientTheme` re-skins on SPA nav + branded login `/login/{slug}`. Reids = navy `#0e1b3a` + gold `#f5a524` on white `#f6f8fc`, navy sidebar.
- **Provisioner** `App\Support\Provisioning\ReidsSubaccount` (idempotent) run by a GUARDED data migration (skips under `testing`; seeders don't run on Railway, migrations do via AUTORUN_LARAVEL_MIGRATION). Creates Reids Location (slug `reids`, webhook_token `rc_a7f3e91b8d264c05b9e2f6a1c4d78e30`, theme, analytics) + Reid user (`reid@reidsautoconnection.com` / `password`, NOT agency admin) + 3 sample customers (Marcus Wells Ch7 · Alicia Grant no-credit · Devon Price repo WITH co-applicant spouse Brianna).
- **Website form** wired — `top-shelf-website` `demo/reids-auto-connection/website/get-approved.html` branded `appForm` POSTs to the intake (SSN masked in-browser). NOT pushed. CORS already allows topshelfsolutions.io (main).

**Judge:** REVISE #1 caught a real leak — a client login shares Top Shelf's `agency_id` and `SoftphoneProvider` auto-connected → Reid would register on Top Shelf's LIVE line + get its caller-ID. FIXED (`d9d4d9d`): gated the token routes + provider only connects in the house account + regression test. **Re-review dispatched; final verdict PENDING** (write the episodic engineering.md entry once it lands).

**Commits on feat/reids-subaccount:** `5065f8f` intake · `1210257` isolation · `11a1bf8` review page · `e45f6fd` analytics · `e147fed` theming · `0689de4` real-form-fields · `47e8505` provisioner · `d9d4d9d` softphone fix.

**NEXT STEPS (deploy + verify DONE — these are follow-ups):**
1. Optional end-to-end proof: submit the LIVE get-approved form in a browser → confirm it appears as a new customer in Reid's CRM (not done unprompted — it would add a real lead to prod; the endpoint is unit-tested + CORS/route confirmed live). Delete the test lead after via the contact page.
2. **Change Reid's password** (currently `password`) before handing the login to Reid — e.g. via tinker/UI on prod.
3. Branch hygiene: `redesign/customer-facing` is now behind `main` (missing this work + the earlier main-only booking/prospecting). Whoever owns that WIP branch should rebase/merge `main` into it before it's ever considered for deploy — deploying it as-is would ship a CRM without the Reids sub-account.
4. Generalising "each client themed": the theming engine is data-driven (business_profile.theme), but only Reids has a theme + the provisioner is Reids-specific. For the NEXT client, add a small per-client theme/onboarding path (or a settings UI) rather than another bespoke provisioner.

**Gotchas:** the intake token `rc_a7f3e91b8d264c05b9e2f6a1c4d78e30` is hardcoded in BOTH the provisioner and the website form — must match. Full GHL nav for a sub-account was ALREADY there (app-sidebar hardcodes it for non-house; only Phone + Prospecting are `isHouse`-gated). **Isolation lesson (mirror to Contractor Pro / FieldPro if they get client logins):** gating agency DATA routes isn't enough — a client shares the agency_id, so token-mint routes + always-mounted providers (softphone WebRTC) leak unless also gated / house-scoped.

---

## ▶ FOLLOW-UP: toggle → granular Client Access + Reids site chrome — ALL DEPLOYED (2026-08-26)

**Client Access (DEPLOYED + LIVE, CRM `d86cae6`):** evolved the coarse Full/Basic tier into granular per-page control. Sidebar → Agency → **Client Access** lists clients; click one → enters it → its Access page: an on/off switch per CRM page (Dashboard locked "always on") + **Basic CRM / Full CRM** presets. Source of truth = `business_profile.nav_disabled` (absent = full, so new sub-accounts stay full); the Clients-page quick toggle + the `plan` label stay in sync (`LocationController@updatePlan` writes both). A guarded backfill migration (`2026_08_27_000002`, Judge REVISE→fixed) gives any existing plan='basic' client an explicit nav_disabled on deploy so it doesn't regress to full nav. `ClientAccessTest` (10), Judge **PASS**. Verified live: `/company/settings/access` → 200, lists 4 clients (a new **Summit Roofing Co** appeared — added by the parallel session). Open option: Basic hides nav only, doesn't route-gate (fine for a nav tier; add gating if it becomes a paid gate).

**Reids demo site chrome (DEPLOYED, top-shelf-website `ad0a579` → Hostinger, LIVE):** header + footer + the under-hero `.trust` banner now navy — done via the `.section-blue` CSS-variable override so all `var(--text*)` chrome auto-switches to legible light; removed ALL "call a real person" copy (deleted the bottom "Prefer to talk it through? Call a real person…" phone CTAs on 5 pages + reworded 11 inline "a real person" mentions to us/we/Reid's team; 0 left); `styles.css?v=6→v7`. Playwright + live-curl verified; filed to Drive.

---

## ▶ FOLLOW-UP: per-client Full/Basic toggle + customer-facing redesign — BOTH DEPLOYED to main (2026-08-26)

**⚠️ A PARALLEL SESSION pushed 8 "v1 REST API" commits to main** during this work (API-key auth + Contacts/Pipelines/Tasks/Conversations/Invoices/etc. CRUD under `Api/V1/*`, `routes/api.php`, `bootstrap/app.php`, Invoice model). Handled: rebased the toggle onto their work (zero file overlap, clean), 409 suite green together. Their work is intact.

**Toggle (DEPLOYED, `b9df789`):** owner asked "toggle each client Full CRM vs Basic, one by one." `Location.plan` ('full' default so new sub-accounts stay full; 'basic' = core nav {Dashboard,Texts,Inbox,Contacts,Reputation,Settings}). Agency-gated `PATCH /agency/companies/{location}/plan` (validated + same-agency IDOR guard). Per-client Basic|Full switch on the Clients page. Sidebar filters basic clients. TDD `ClientPlanTest` (6). **Judge PASS (1st).** Route-gating of Full features (hard paid tier) is a noted future option — Basic currently hides nav only (no data-isolation risk, routes stay location-scoped).

**Customer-facing redesign (DEPLOYED, `a210d88`):** owner approved the redesign/customer-facing renders and said "make the calendar the booking page, don't apply the consultation (funnel) page yet but keep the design, everything else deploy." Ported the 6 approved pages (welcome/book/invoice/portal/review/sign) onto main. **funnel.tsx EXCLUDED** (kept main's; the redesigned consultation page stays on `redesign/customer-facing` for later). **Calendar.php kept as main's** — the month-calendar book.tsx works on main's already-deployed booking backend (both use the `{key,date,slots:[{iso,label}]}` shape); Playwright-verified the calendar + slot-picking work. Self-verified (no Judge — a port of pre-approved + pre-humanized pages; controllers unchanged so props match; 409 tests). Live home-hero verify was polling at handoff time.

Local dev note: reset the LOCAL admin pw back to taylor@topshelf.test/password (had drifted). CRM checkout restored to `redesign/customer-facing`.

---

## ▶ CRM BUILD SPEC — Reids sub-account in the Top Shelf CRM (2026-08-24 — the original spec; NOW BUILT per the section above)

**Goal:** in the Top Shelf CRM (`C:\Users\taylo\top-shelf-crm`, live at **app.topshelfsolutions.io**), give Reids Auto Connection its own sub-account so Reid logs into only his CRM and sees his customers (the auto-loan applicants), their applications, and sample analytics. Taylor confirmed the model with a diagram.

**The model (GoHighLevel-style, Taylor CONFIRMED it):** Top Shelf = the **Agency** (Taylor sees all clients) → each client is a **sub-account** (in code = a `Location`) → the client's own customers are **Contacts** inside that sub-account. Reid attaches to the Reids `Location` and is scoped to only its data.

**TWO refinements Taylor added (must honor):**
1. **A newly-created sub-account must have the FULL GoHighLevel-style nav + features** — not a stripped-down client view. Provision Reids with the complete client feature set (Dashboard, Contacts, Conversations/Inbox, Calendars, Opportunities/Pipeline, Reputation/Reviews, Automations, Sites/Funnels, Reporting, Settings — whatever `app-sidebar.tsx` exposes to a non-`isHouse` location). Ensure creating a sub-account doesn't yield an empty/limited nav.
2. **The co-applicant is NOT a customer.** ONE application submission = ONE primary Contact (the original applicant = the "customer" that shows in Reid's customers list). The co-applicant's info is stored INSIDE the application payload and shown ONLY on the application/review page as "co-applicant." Do NOT create a second Contact for the co-applicant; do NOT list them as a customer.

**⚠️ DEPLOY — CONFIRM BEFORE PUSHING:** repo is checked out on `redesign/customer-facing` (5 ahead / 2 behind `origin/main`; origin default = main). It's a PROD CRM. **Ask Taylor which branch `app.topshelfsolutions.io` deploys** (main vs redesign/customer-facing) before pushing — building on the wrong branch = not live. Deploy = Dockerfile build; `composer.json post-autoload-dump` runs `php artisan migrate --graceful` on deploy (migrations auto-run; seeders do NOT — so seed demo data via a guarded DATA migration so it lands automatically). Railway project = "Top Shelf" (id 90dd2f37-11d2-486f-9610-230348fcdc4f). Can't run the app locally (no local Postgres) → verify by deploy-then-screenshot app.topshelfsolutions.io (log in as Reid).

**ARCHITECTURE (mapped by an Explore agent — trust these, re-verify file contents before editing):**
- Stack: Laravel 12 + Inertia + React 19 + TypeScript + Tailwind + shadcn/ui (`@/components/ui/*`), lucide-react. Location pages hardcode `/company/...` URLs (NOT `route()`). Pages auto-register by path via glob (`Inertia::render('location/contacts/show')` ⇒ `resources/js/pages/location/contacts/show.tsx`).
- Tenancy: `Agency` → `Location` (fillable: agency_id,name,timezone,business_profile,sending_domain,status,webhook_token,is_house) → tenant data. User↔Location via `location_user` pivot (`->users()->attach($id,['role'=>'admin'])`); User has agency_id + is_agency_admin. `app/Http/Middleware/SetCurrentLocation.php` sets current location from session; `app/Models/Concerns/BelongsToLocation.php` global-scopes every tenant model by `location_id` (+ auto-fills on create). Login → `/company` = `location.dashboard`. Enter a client: `POST /agency/companies/{location}/enter` (`LocationController@enter`).
- Contact model: fillable first_name,last_name,email,phone,company_name,source,dnd,portal_token; `customFieldValues()` morphMany; scoped by location_id. Routes under `Route::middleware('location')->prefix('company')` — `location.contacts.index/show` = `/company/contacts[/{id}]`, fed by `ContactController@index/show` → `Inertia::render('location/contacts/index'|'show')`.
- MIRROR TARGETS: **sample analytics** → `LocationDashboardController@show` + `resources/js/pages/location/dashboard.tsx` (add an `analytics` prop + div-based stat/bar widgets; NO chart lib installed — mirror the existing `is_house` outreach panel's plain-div bars). **Public intake** → `PublicWebsiteLeadController@store` (route `website-lead` in `routes/api.php`, throttled, no session auth, uses `Contact::withoutGlobalScopes()->where('location_id',…)` + explicit location_id on create) and `PublicFunnelController@submit` (the find-or-create Contact + `FormSubmission::create(['payload'=>$data])` pattern). **App data storage** → `FormSubmission.payload` (JSON) — but `funnel_id` is a non-nullable FK, so add a migration to make it nullable (or add a small `type` col). **Application review page** → clone `ContactController@show` + `contacts/show.tsx` (3-col card grid, tabs, gold-top-border header, cards = `border-border bg-card rounded-xl border p-5`). **Seeding** → `DatabaseSeeder` (Agency/Location/User+pivot) + `DemoContentSeeder` (`app(CurrentLocation::class)->set($loc)` → `Contact::firstOrCreate(['email'=>…],$p)` → `set(null)`; no Contact factory). **Nav** → `resources/js/components/app-sidebar.tsx` (`toolItems: NavItem[]`, hardcoded `/company/...`, some gated by `isHouse`). CORS `config/cors.php` (`paths=['api/*']`) — add the website origin `https://topshelfsolutions.io` to allowed_origins for the form POST.

**BUILD STEPS:** (1) migration: make `form_submissions.funnel_id` nullable. (2) data migration (idempotent/guarded): create Reids `Location` under the Top Shelf agency, attach a Reid `User` (scoped, NOT agency-admin) with a known demo password, ensure full GHL nav shows for it; seed 2-3 sample **primary** Contacts (customers) each with a `FormSubmission` application payload — make ONE of them include co-applicant fields in the payload (to show the co-applicant-only rendering). (3) `PublicApplicationController@store` + `POST /api/applications/{token}` (resolve Location by webhook_token; find-or-create the PRIMARY contact; store full app incl. co-applicant in FormSubmission.payload). (4) wire the website Get-Approved form (`top-shelf-website` repo `demo/reids-auto-connection/website/get-approved.html`, currently a demo submit) to POST there + CORS. (5) `ContactController@show` add `application` prop (latest FormSubmission payload) + a route/page `GET /company/contacts/{contact}/application` → clean review page (primary applicant + co-applicant section). Add an "Application" card on the contact detail linking to it. (6) sample analytics on the dashboard. Then deploy (right branch) + verify on app.topshelfsolutions.io logged in as Reid.

**Sample customers (seed):** e.g. primary applicants like a Ch.7-discharged buyer, a first-time/no-credit buyer, and a repossession buyer WITH a co-applicant (spouse) — each with realistic application fields (name, DOB, masked SSN, DL, email, phone, state, address, employer, income, bankruptcy type) in the payload; the co-applicant appears only inside that one application.

---

## ▶ REIDS SITE — blue sections + BBB badge + contact refresh + cache fix LIVE (2026-08-24)

Live: https://topshelfsolutions.io/demo/reids-auto-connection/website/ · Source: `top-shelf-website` repo `demo/reids-auto-connection/website/`.

**Design round DONE + live-verified in Chrome (desktop):**
- **Blue feature sections** to break up the white: new `.section-blue` class in styles.css scopes the deep-navy palette so children render light-on-blue (same trick as the staff dashboard). Applied to homepage "How it works" + story band, the WHOLE get-approved page ("application on a blue background" per owner), and the contact page.
- **BBB A+ badge** floats top-right of the hero photo. Owner wanted the real BBB logo — I CANNOT reproduce the trademarked BBB torch, so built a readable designed badge: a navy CIRCLE mark with gold "BBB / ACCREDITED" next to "A+ Rating" + "Better Business Bureau · 18 yrs". ⚠️ For the real logo, owner provides Reid's OFFICIAL BBB Accredited Business seal and we swap it in (`.bbb-badge__mark` is the placeholder). Scoped `.bbb-badge` CSS is in index.html `<head>`.
- **Contact page refresh** (cc31891): details panel was pinned LEFT (max-width without margin-inline:auto) — centered it + the heading, made both sections blue.
- **Trust tag** = "18 yrs / Veteran-owned business". Owner confirmed 18 is accurate and will reconcile the 2009 founding/GBP himself — do NOT touch that.
- **Design audit** (owner asked): walked homepage + get-approved in Chrome; contrast/buttons/layout all pass, no real issues. Mobile + inner pages rely on the intact responsive CSS.
- **CACHE ROOT-CAUSE FIXED** (4038db0): site `.htaccess` revalidated CSS/JS/img/PDF but NOT html, so edited pages showed stale to returning visitors (owner kept seeing old copies). Added `html?` to the no-cache FilesMatch — fixed for all of top-shelf-website.

**⏭ NEXT — CRM intake build (owner ask, verbatim):** "add this customer to the Top Shelf CRM so they have their own dashboard and they can see the analytics of the website and so when a customer fills out the application that information will populate in Reids Auto Connection CRM page as a customer for them (Not Top Shelf)… the user can click on a card that says something like 'Application' and it pulls up a new page with all the customer's information laid out cleanly."
- **Analytics (owner decision):** SAMPLE/placeholder only for now (so Reid sees what the dashboard would look like), NOT real tracking.
- **CRM app:** `C:\Users\taylo\top-shelf-crm` (Laravel 12). Bones already exist: **Location** = a client sub-account (create one for Reids), **Contact** = a customer/lead, **FormSubmission** = intake, plus Pipeline/Opportunity/CustomField. Auth/tenancy is per-Location.
- **Build steps:** (1) create a "Reids Auto Connection" Location + a login scoped to only its data (Reid's dashboard); (2) sample-analytics widgets on that dashboard (mock pageviews/leads/conversion); (3) a PUBLIC token-guarded intake endpoint that turns a website application POST into a Contact under Reids' Location with the full application saved; (4) wire the get-approved form to POST there (today it's a demo submit + a Cognito embed); (5) Contacts list + Contact detail with an "Application" card → clean full-application review page. Deploys on Railway.
- **Not done this turn on purpose:** substantial cross-app build (CRM Laravel app + website wiring) and the session was very long — best built with fresh focus. Plan above is ready to execute.

---

## ▶ REIDS SITE — follow-up edit rounds since the redesign, all DEPLOYED LIVE (2026-08-24)

Live: https://topshelfsolutions.io/demo/reids-auto-connection/website/ · Source: `top-shelf-website` repo `demo/reids-auto-connection/website/` (push `main` → Hostinger auto-deploy). Three owner-driven rounds after the base redesign below, each committed + live-verified:

1. **Financing photo swapped** (commit `0e64316`) — the "Approved, not judged" story-band photo read as AI / badly framed; replaced with a better-composed owner-with-keys shot (soul_2, cropped to drop a plate). Note: Hostinger re-optimizes JPGs on deploy, so live bytes/md5 differ from the source file — the image itself is correct.
2. **"Talk to a real person" → "Call us today"** (`576bebd`) — header phone label on all 5 pages + the contact phone block (7 spots). Value-prop "a real person" sentences left intact.
3. **Hero + theme + co-applicant + wording** (`5268789`, judge-agent PASS after 1 REVISE):
   - Hero h1 → **"Drive a newer car today. / Watch your credit climb."**, CTA **"Get Approved Today"** (dropped "Bad credit is a chapter" + "Start My Fresh Start").
   - Theme recentered on **"The Fresh Start Vehicle Program"** across pages + the chatbot answer.
   - get-approved.html: **intuitive "Add a co-applicant" toggle** (id `coToggle`) reveals a full second-applicant fieldset (name/DOB/SSN/DL/email/phone/relationship/employer/income) with a same-address toggle + Remove; co-fields carry no `required` so they never block the primary submit. Removed the dead "joint applicant?" dropdown. Scoped CSS in the page `<head>`, JS in the page's inline `<script>`.
   - Copy: **"Reid has given"** (not "gotten"); **18 yrs** (not 15/"Fifteen") on badges + About story; fixed a redundant About fly-in bullet; humanizer pass (kept the warm em-dash voice per voice-calibration). Judge REVISE caught two spelled-out "Fifteen years" (grep for "15" missed them) + the chatbot program name; all fixed + grep-verified.

**⚠️ OPEN for owner review (flagged, not resolved):** the trust badge now says **18 yrs** (framed as Reid's experience) but the business founding is still **2009** everywhere (schema `foundingDate`, "est. 2009", owner letter). 2026 − 2009 = 17, not 18. Asked the owner whether to move founding to 2008 or keep 18 = experience + 2009 = the business. Reconcile once he answers.

**Next:** owner reviews the live site; apply further copy/photo tweaks he wants; resolve the 18yr-vs-2009 founding question.

---

## ▶ REIDS SITE — light-theme redesign, inventory removed, location stripped, owner photos added, DEPLOYED LIVE (2026-08-23)

**Owner ask (verbatim):** "please get rid of the inventory page completely, nothing links to it, absolutely gone, also make it a light theme, and use images of minorities and single mothers in cars smiling, maybe some holding their keys up… happy they just got a car… around the website in areas that make sense. Also please get rid of the location everywhere on the website, no address, no lewisville, no texas."

**WHICH SITE:** the **v2 full site IS the demo-hub website** — `topshelfsolutions.io/demo/reids-auto-connection/website/`, source in repo **`top-shelf-website` at `demo/reids-auto-connection/website/`** (push `main` → Hostinger auto-deploy, ~5s). This CORRECTS the stale "v2 Aurora — NO live URL recorded" note in the section below (an intervening vault commit reverted an earlier fix). The hub `…/reids-auto-connection/` links to this website + the SEO/GBP audit.

**✅ DELIVERED + LIVE (commit `07a8403`, judge-agent PASS 1st):**
- **Inventory gone:** deleted `inventory/` + `inventory-page.js`, removed the home inventory section + every nav/hero/footer/CTA link, pruned sitemap. Live `/inventory/` = 404. KEPT `inventory-data.js` (the staff dashboard depends on it — it's not "the inventory page").
- **Light theme:** rewrote `styles.css` :root to crisp white (`--bg #F6F8FC`) + navy ink (`--text #0E1B3A`), gold accents kept; fixed all inline dark colors (theme-color, story-band `#fff`/`#C7D3E1`, get-approved white emphasis, validation red). Cool `#EDF1F8` section bands.
- **Location gone everywhere:** all 6 customer pages verified CLEAN — address, Lewisville, Texas, ZIP, every city, DFW, Dallas Love Field, the Google-map iframe, JSON-LD **address AND geo-coordinates (lat/long)**, areaServed city list, and the chatbot's location keyword+answer. Recast as phone/online, nationwide + fly-in. Kept phone (972) 436-1600 + the `reidsautoconnection.com` schema url.
- **Photos:** 4 generated (Higgsfield `soul_2`), placed on home hero, home story band, About, Financing — a Black woman keys-up in the driver's seat, a Latina single mother holding her smiling child (the mom+kid moment), + two more happy owners with keys. Files `assets/img/owner-{hero,story,about,financing}.jpg`. Story was regenerated + left-cropped because the model kept baking in a legible license plate.
- **Staff pages:** dashboard/login kept working by SCOPING the original dark palette to `body.dash-body` (dashboard.css) + login's `body` so the customer light theme doesn't bleed into the internal tool.

**Interview answers (owner, this session):** photos = "Mix of both" (mostly solo adults, 1 mom+child) · theme = "Crisp white + navy."

**Conversion open items (unchanged, still pending):** design direction for the paid build, real domain (reidscars.com) + canonical/schema swap, confirm hours/ZIP/legal-name/financing, inventory source decision, upgrade "Dawn" chatbot to live Claude + real lead delivery.

**Next 3 steps:** (1) Owner reviews the live redesign; iterate on copy/photos if wanted (e.g. About photo has a faint Audi wheel emblem — offered to swap). (2) On approval, proceed to the paid-conversion build (domain + inventory + chatbot). (3) Optionally mirror final to Drive `Projects/Top Shelf/Reids Auto Connection/` (code stays in the repo).

---

## ▶ REIDS AUTO CONNECTION filed into Top Shelf as a conversion client (2026-08-22)

**Owner ask (verbatim):** "bind this conversation to top shelf, give me the webpage that we made for reids auto connection. Also please put all of the project information from the Project Reids Auto Connection into a reids auto connection folder in top shelf… Reids auto connection is a business that we are trying to convert."

**Context:** Reid's Auto Connection was a standalone website project built BEFORE Top Shelf. It's now a business Top Shelf is converting (delivered demo → paid engagement). Reid M. Anderson, Lewisville TX used-car dealer whose whole identity = subprime/bankruptcy auto financing ("Ch 7 & 13 Fresh Start", $0 down); ~4.7★/~200 reviews; BBB A+, veteran-owned since 2009. Best reputation, worst (old) website = the Top Shelf angle. Full brief: `Documents/Reids-Auto-Connection-Strategy-Brief.md`.

**✅ DELIVERED — consolidated the whole Reids project under Top Shelf (Drive), non-destructive:**
- Copied entire Drive project `Projects/Reids Auto Connection/` → **`Projects/Top Shelf/Reids Auto Connection/`**. Verified **65→71 files** (+`index-revamp.html` + 5 homepage `concepts/` pulled from the v1 repo, which weren't in the Drive snapshot). Standalone project left intact.
- Contents: `Documents/` (strategy brief + BOTH audit PDFs incl. the Top Shelf SEO+GBP audit 2026-07-25), `Website/` (v1 + revamp + concepts), `Website-v2-Aurora/`, `HANDOFF.md`, `CODE.md`, `Manager/`, `renders/`.
- Wrote `README.md` orientation doc at the folder root (business facts, live links, repos, open items). Placed at Top Shelf **ROOT** (not `Prospects/`) — most discoverable, right for a client being converted; offered to relocate.

**Website links (given to owner):**
- **Live v1 "Daybreak":** https://lawngreen-wildcat-658951.hostingersite.com — confirmed HTTP 200. Repo `taylor-os/reidsautoconnection-website`, push `master` → Hostinger auto-deploy.
- **Revamp preview:** https://lawngreen-wildcat-658951.hostingersite.com/index-revamp.html (HTTP 200, not promoted to homepage).
- **v2 "Aurora":** repo `taylor-os/reidsautoconnection-v2-website` + Drive `Website-v2-Aurora/`. **NO live URL recorded** — verified GitHub Pages 404 + nothing in repo. Built but never confirmed deployed.

**Conversion open items (from brief/notes, unchanged):** (1) which design ships (promote revamp / pick Aurora / present to Reid); (2) point real domain (reidscars.com) + swap placeholder canonical/schema/sitemap URLs; (3) confirm hours (Mon–Sat 10–7?), ZIP (75057 vs 75067), exact legal name, financing mechanics; (4) inventory source — #1 blocker: static sample cards vs self-managed app (paid upgrade); (5) upgrade "Dawn" chatbot to live Claude-powered + real lead delivery (paid phase).

**Next 3 steps:** (1) Owner decides the design direction for the conversion. (2) If v2 Aurora is wanted as a clickable option, deploy it to a Hostinger subdomain. (3) On go, run the paid-conversion build (domain + inventory decision + chatbot upgrade). Session bound to Top Shelf; Drive-only filing so far (not git-synced).

---

## ▶ AUTOMATIONS REBUILD — BUILT, Judge PASS, push proposed (2026-08-24, LATEST)

**Shipped on branch `feat/automations-recipes-edit` → fast-forward merged to `main` @ `83f093e`.** Push proposal filed at `_outbox/push/automations-recipes-83f093e.json` (repo `top-shelf-crm`, branch `main`) — awaiting owner Approve → Railway auto-deploys.

**What was built (all against real engine node types + real triggers — no dead stubs):**
- `app/Support/WorkflowRecipes.php` — 6 one-click recipes (new-lead instant text, new-lead 3-touch, ask-for-review after invoice.paid, invoice-paid thank-you, win-back on client.inactive, appointment-booked confirmation).
- `WorkflowController@store` — optional `template` seeds the recipe graph; blank = Custom Automation (trigger-only canvas). `index()` passes the gallery.
- `WorkflowController@compose` + `POST workflows/compose` — plain-English → steps via the bound `AiEngine`; **HTTP 422 graceful degrade when `CLAUDE_CODE_OAUTH_TOKEN` absent** (the open 401 still blocks the live AI path — box works the moment the token lands).
- `index.tsx` — recipe-card gallery (search + category filter) + "Custom Automation" button/card + explicit **Edit** action on every automation (recipe-made AND custom), so trigger timing / message wording is editable after build.
- `builder.tsx` — **freeform React Flow canvas kept unchanged** (owner req: keep live drag-around); added a collapsible "Describe it and I'll build it" AI box docked below it.
- Tests: `WorkflowRecipeTest` + `WorkflowComposeTest`. **20 workflow tests green; `vite build` succeeds.** Judge = **PASS**.

**Honest gap surfaced to owner:** the render's "missed-call text back" recipe was omitted — the engine has NO missed-call trigger. Adding one (call.missed domain event from the softphone call_logs → trigger) is the clean follow-up to make that recipe real.

**Next 3 steps:**
1. Owner Approves the push → verify live on https://top-shelf-production.up.railway.app/company/workflows (seed login taylor@topshelf.test / password): recipe cards create pre-wired flows, Custom Automation opens the freeform canvas, Edit reopens any automation, AI box shows the graceful "not set up yet" until the token works.
2. Unblock the AI box: resolve the `CLAUDE_CODE_OAUTH_TOKEN` 401 (see CRM archive handoff) — then compose() goes live with zero further code.
3. Optional: add a `call.missed` trigger so the flagship "Missed-call text back" recipe can ship; consider `whereDate`-free indexes if recipe volume grows. Pre-existing tsc type warnings in builder.tsx (addEdge/router.patch) are esbuild-ignored and were left untouched (surgical) — clean up separately if desired.

---

## ▶ AUTOMATION BUILDER — direction CHOSEN + Custom page rendered (2026-08-24)

**Owner's decision (after reviewing the 3 renders below):** combine them.
- **Main automations screen = R1** (recipe cards for the pre-builts) + a **"Custom Automation"** button in the header (echoed as the last card).
- **"Custom Automation" button → a new page** = the **R2 linear step builder** (trigger → steps → "Add a step" picker, drag-reorder, Yes/No branch lanes) **with an AI "Describe it and I'll build it" box docked BELOW the step buttons** (plain-English textarea + example chips + "Build my steps"; runs on the Claude subscription; nothing sends until publish).

**✅ Rendered both (verified on screen, publish confirmed HTTP 200):**
- Board (main screen + Custom page): https://web-production-cd5ad.up.railway.app/preview/b/b_36be5d9696
- Original 3 concept renders: https://web-production-cd5ad.up.railway.app/preview/b/b_f702ed89d5
- Files: `/tmp/ts-renders/render1-recipes.html` (main), `render4-custom.html` (Custom page) + PNGs. **⚠️ /tmp is wiped on session resume** — the HTML is ephemeral; only the published gallery bytes persist. Copy to Drive `Projects/Top Shelf/renders/` if they should last.

**Next 3 steps:**
1. Owner signs off on the Custom-page render (or requests tweaks: dark mode, mobile, different step palette).
2. Build for real — Plan → tdd-workflow → diff-scoped two-lens review → Judge. **Main screen** = add recipe-template picker + "Custom Automation" nav to `workflows/index.tsx`. **Custom page** = new page rendering the linear builder over the existing React-Flow nodes/edges data model (keep the data model, change presentation + insert UX), plus the AI compose box calling the P10 Claude-CLI path (same subscription engine as the inbox "✨ Draft"). Recipes = a seed set of prebuilt graphs.
3. AI-compose depends on `CLAUDE_CODE_OAUTH_TOKEN` being live (still the open 401 blocker — see the CRM handoff archive). R1+R2 need NO AI, so ship those first; AI box degrades gracefully until the token works.

---

## ▶ AUTOMATION BUILDER — 3 redesign renders for owner review (2026-08-22)

**Goal (owner ask, verbatim):** "make the automation live wire map easier to use… it is overwhelming. Create 3 renders of how you can make it easier."

**Diagnosis (grounded in `resources/js/pages/location/workflows/builder.tsx`, 1102 lines):** overwhelm is structural — (1) new automation drops you on a blank React-Flow canvas with one lone Trigger node; (2) 20 cryptic step types in one "Add step" dropdown (Go to Workflow / A/B Split / Update Field / Webhook / AI Draft…); (3) manual wire-dragging with unlabeled left/right branch handles — so easy to make a dead flow they added a guard alert (builder.tsx:342-357); (4) dense ID/cron/`{{merge_tag}}` config; (5) no templates.

**✅ DELIVERED — 3 concept renders (HTML mockups + 1440px PNGs), verified on screen, match the app's shadcn look:**
- **R1 Recipe Gallery** — "New automation" opens a searchable gallery of plain-English recipes (Missed-call text back, New lead → 3-touch follow-up, Ask for a review). Kills the blank canvas.
- **R2 Linear Step Builder** — replaces the node graph with a top-to-bottom vertical list; drag to reorder; + between steps; branches = labelled Yes/No lanes (can't disconnect); merge tags become tappable chips.
- **R3 Guided Composer** — describe it in plain English → assistant lays out steps to review; no metered API (Claude Code subscription). Manual builder still one click away.

**Files:** `/tmp/ts-renders/render{1,2,3}-{recipes,linear,guided}.html` + `render{1,2,3}.png` (on the Taylor OS server, NOT in any repo, NOT in Drive — still just scratch).

**Recommendation given:** ship **R1 entry + R2 builder** (neither needs AI → fits the no-API-key constraint), layer R3 later.

**✅ UPDATE (2026-08-23): now published to the production /preview gallery.** The `:3737` local preview app was still down, so instead of waiting, published straight to the PRODUCTION gallery: **https://web-production-cd5ad.up.railway.app/preview/b/b_f702ed89d5** (behind the preview login) — all 3 renders side by side with caption strips. This supersedes the "could not publish" note above; Taylor has the link and was asked to pick a direction. Still no judge-agent gate (ideation set, not a shippable deliverable) and still nothing built into `builder.tsx`.

**Next 3 steps:**
1. Owner opens the link above and picks a direction (or R1+R2 combo, or further iteration). Nothing else proceeds until this happens.
2. On selection, build for real into `builder.tsx` / `workflows/index.tsx` (Plan → tdd-workflow → diff-scoped two-lens review → Judge). R1 = a template/recipe picker in the create flow; R2 = the bigger lift (linear layout engine over the existing nodes/edges graph — keep the React-Flow data model, change the presentation + insert UX).
3. Once a direction is picked, copy the winning render(s) from `/tmp/ts-renders/` to Drive `Projects/Top Shelf/renders/` if they should persist past this session.

---

## ▶ HOMEPAGE + "SEE IT WORK" DEMO PAGE — built for review, agents backed, Judge PASS (2026-08-22)

**Goal:** owner approved the wireframe (a lighter homepage + ONE dedicated demo page built as a guided process). Build BOTH fully (desktop+mobile) for evaluation; demos show a whole job live; backed by CEO+customer agents. NOT deployed — these are evaluation renders; deploy is the next step.

**✅ DELIVERED — 2 private Artifacts:**
- New homepage → https://claude.ai/code/artifact/8bfb3d0a-a777-4f1a-b025-54b057888cd5
- "See it work" demo page → https://claude.ai/code/artifact/3e28a2a9-ba05-4cba-a401-8d798933e4f7
Source in Drive `Projects/Top Shelf/renders/homepage-concepts-2026-08-20/` (src/ + dist/ + kit.js + kit.css + build.py). Cross-links between the two artifacts are hard-wired to those URLs via build.py (`.home_url` / `.demo_url`).

**What's built:** Homepage = hero live call demo (AI/text-back toggle, captions) · trust band HIGH ("what's yours stays yours" + honest CRM-is-the-subscription caveat) · accountable-human line · pain line · 3 outcome tiles · passive review demo · CRM teaser → demo · pricing ($499/$899/$1,599 + honest terms) · a working lead-capture form at #audit (thank-you on submit). Demo page = a guided **6-step two-sided tour** (call→CRM→create/schedule→invoice→pay/auto-Paid→review→5★ Google; owner CRM + customer phone side-by-side on customer-facing steps; click-through + auto-play + Back/Replay/Next) then the full clickable 12-screen CRM (Basic/Full × desktop/mobile). Sample business "Delgado Plumbing & Air" / owner "Mike" / fake customers.

**Engine (all in `ts-build/kit.js` + `kit.css`):** demos mount via `data-demo="phone|crm|review|crmteaser|tour"`; `tour()` is the new 6-step controller; the 12-screen CRM is `crm()`. Assembled by `build.py` (inlines kit + logo data-URIs + the cross-link URLs). Verified functionally + visually in Playwright on localhost at 390 & 1300px (in-app browser pane still won't composite on this machine — Playwright is the reliable verify/screenshot path).

**Agents/Judge:** BOTH persona seats BACKED the build; their 2 blockers were fixed first — (a) a testimonial placeholder that RENDERED VISIBLY on the page → removed (kept an honest small-local-team accountability line; NO fabricated testimonial); (b) dead-end CTA → a real capture form. Judge PASS (1st).

**⚑ Owner still owes / NEXT (post-approval):** (1) a REAL named local testimonial (name·trade·town·result) — both agents' #1 remaining trust lever; never fabricate. (2) Deploy into the real `top-shelf-website` repo at clean paths (`/` and `/see-it-work`) and WIRE the capture form to the real CRM lead endpoint (the site already has one). (3) humanizer pass on the final public copy. (4) re-verify cross-links after deploy.

---

## ▶ PROSPECTING SCORING RETOOL + BUCKET DASHBOARD — Phases 1+2 SHIPPED, Phase 3 pending (2026-08-21)

**Goal (owner):** redo the lead-finder scoring so website quality no longer dominates the SCORE — it sets a **contact bucket** instead, and the score measures fit for what Top Shelf actually sells (get-found + stay-in-touch). Weak/no site = **must_contact** (free site = foot in the door, reach out no matter what); good site = **system_sell** (still pursue — they may lack the system or overpay); unknown = **needs_review**. Everyone graded the SAME way. Then show the buckets on the CRM Prospecting tab as grouped sections. (Interview locked: rebalance-to-core-offer-fit + 3 buckets + grouped-sections dashboard.)

**✅ PHASE 1 — scoring** (`taylor-os/topshelf-lead-finder`, pushed `25a6ad0`): website removed from the opportunity score; opportunity = get-found (ranking 0.45 + GBP 0.25) + stay-in-touch (chat 0.30) via `_OPP_W`. `contact_bucket()`: must_contact (`_ALWAYS_CANDIDATE_SITES = _WEAK_SITE - {unknown}`, now derived) / system_sell (modern) / needs_review (unknown) / excluded (dq). `recommended` follows the bucket. `work_rank` restored to the **need × ability-to-pay** intersection (`opp_factor × pay_factor`; dead longevity/contact terms removed). Carried to the CSV column + the app push (`score_reason`). Docs (rubric/csv-schema/SKILL) synced — the rubric was 2 model-generations stale. Commits `bf099fd`+`19aa105`+`25a6ad0`, **115 tests, test-first.** Owner invariants pinned (6 approvals stay must_contact+recommended; unknown held). NOTE: desktop finder repo was 6 behind origin at session start — PULLED first, so this sits on the real current model (resolves the earlier stale-model discrepancy).

**✅ PHASE 2 — dashboard** (`taylor-os/top-shelf-crm`, `833644b` on main, **DEPLOYED**): migration `2026_08_20_000001_add_contact_bucket_to_prospects` (nullable string); `Prospect::contactBucket()` derives from website_quality for pre-column rows (mirrors the Python), stored value wins once the scorer writes it; `ProspectController::present()` surfaces it; `prospecting.tsx` now GROUPS into labelled, ranked sections + bucket filter chips with counts. `ProspectingBucketTest` (7) + **full suite 334 green**, frontend builds clean. Built on a clean `feat/prospect-buckets` off origin/main, FF-merged to main, pushed; checkout restored to `redesign/customer-facing`. **Railway deploy = SUCCESS 2026-08-21T03:39Z** (migrations auto-run on boot, `AUTORUN_LARAVEL_MIGRATION`). **LIVE: https://app.topshelfsolutions.io → Prospecting.**
  - CRM prod = the **top-shelf** Railway service (id `61e63827`, project "Top Shelf" `90dd2f37`, env production `ef37211d`), domain `app.topshelfsolutions.io`. DEPLOY.md's `top-shelf-production.up.railway.app` is an old alias.

**⏳ PHASE 3 — headless auto-resolve of `needs_review` (NOT built).** Render bot-blocked / JS-shell (`unknown`) sites in a headless browser (dev-browser/agent-browser on the Oracle box — already used there for GBP reads) and re-run the sitescan grader on the rendered DOM, so most `unknown` rows re-bucket to must_contact/system_sell BEFORE the owner sees them; only true WAF/CAPTCHA/dead residual stays needs_review. Cheap second pass on the unknown subset only.

**How this started:** "use the ceo + business-owner agents to evaluate the prospect-finding automation" → CEO(Business-Strategist)+owner-persona eval (Judge PASS; both seats converged on: dormant winners.yaml/no conversion loop, oversized queue, stale website grade) → branded slideshow → "audit the new scoring model" → "redo it, website→bucket" → this build.

**Next 3 steps:** (1) Owner uses the live board; if the 45 work_rank line needs re-tuning (values shifted when dead terms came out) it's `work_rank_threshold` in the finder `config.yaml`. (2) Build Phase 3 when greenlit. (3) Re-hunt a city to replace the derived buckets with the scorer's own written values.

---

## ▶ HOMEPAGE REDESIGN — 3 interactive "show-don't-tell" concepts delivered (2026-08-20)

**Goal (owner):** 3 redesigned homepage concepts for topshelfsolutions.io that make the CEO + customer persona agents happy and make the customer BELIEVE us. Keep the color theme + logo; everything else fair game. Trust · info quick/easy · understand fast · don't over-complicate. Crucially: **SHOW it works with live demos** (automated review-request text · CRM basic AND full · the other main services) instead of "trust me bro." Interactive HTML (owner picked), not image renders.

**✅ DELIVERED — 3 private Artifacts (open logged in):**
- A "Watch It Work" (hero IS a live demo): https://claude.ai/code/artifact/8deb68e9-9cfd-4589-8d19-21bdc82e9e56
- B "One Lead's Journey" (narrative timeline): https://claude.ai/code/artifact/81e86943-c2b5-464b-9419-e059cc19ffc2
- C "All In One Place" (control-room tabs): https://claude.ai/code/artifact/7a2c95a0-e8c4-40b7-b5ae-fafe27929dcc
Source filed to Drive `Projects/Top Shelf/renders/homepage-concepts-2026-08-20/` (dist + editable src/kit.js/kit.css/build.py). Redeploy an Artifact by republishing the same dist file path (URL persists); scratchpad wipes, so rebuild from the Drive copy. **v2 (2026-08-20, owner feedback):** AI receptionist reworked from a chat-thread look to a REAL in-call screen (two talking heads with active-speaker swap, live captions, running call timer, waveform, call controls) — text-back path unchanged; CRM now has a **Desktop/Mobile device toggle** for BOTH Basic and Full (mobile = phone-framed app w/ card list, bottom tab bar, horizontal pipeline on Full). All functionally verified in-browser (0 console errors); same 3 Artifact URLs redeployed. NOTE: demo auto-play is IntersectionObserver-gated so it won't fire in the non-compositing in-app pane — verify by clicking the mode toggle; it plays fine in a real browser.

**Architecture:** ONE shared JS demo kit (`kit.js`/`kit.css`, rendered into `[data-demo]` mounts) embedded in 3 distinct lean shells; assembled self-contained via `build.py`. The 3 demos, threaded as one lead's journey: (1) call → **AI receptionist** answers + captures, with a toggle to **missed-call text-back** for AI-averse customers → saved to CRM; (2) **CRM Basic⇄Full** toggle; (3) automated **review-request** text after the job. Brand kept exactly (#0B0906/#C9A86A, Cormorant+Jost, real gold TS logo as webp data-URI). Pricing shown = CURRENT live/offer.py tiers ($499/$899/$1,599) — NOT the newer $299/$2,500 ladder still uncodified below; revisit if that lands. NO fabricated stats/case studies — proof is the "Sample"-tagged demos.

**Persona review (both dispatched for real) SPLIT the winner:** CEO → **A** (demo-in-hero = instant proof); customer "Mike" → **C** (five-bills-vs-one comparison + click-it-yourself control tipped him; A's auto-play felt "salesy"). Both agreed the fixes, applied to all 3: review-gating→service-recovery, "Sample" tags, honest 6-mo-term line by price, amplified "you keep the numbers either way / real person not a call center" line, C hero reframed off "one screen you operate"→"we run it for you," CRM "we run it, you don't have to" reassurance. **Judge PASS (after 1 REVISE** — caught review-GATING prose surviving in A/B section copy + a gap-not-leak "leaking customers" slip in B; both fixed, re-judged PASS).

**✅ OWNER PICKED CONCEPT A — "Watch It Work" (2026-08-20).** That's the site to build. (CEO/Mike had split A-vs-C and suggested a hybrid; owner chose A as-is.) **Post-selection demo refinements — all in the shared kit, all live at the same URLs:** v2 — AI receptionist reworked from a chat-thread look to a REAL in-call screen (two talking heads + active-speaker swap, live captions, running call timer, waveform, call controls); CRM gained a Desktop/Mobile toggle for both tiers. v3 — CRM **Desktop view now renders inside a laptop frame** (the CRM is the laptop's screen), mirroring the mobile phone frame; also fixed the call-caption area to scroll instead of clip (a `min-height:0` flex-overflow bug — the whole flex fill chain in `.tsp-view`/`.callui`/`.crm-mob` now carries min-height:0). **v4 (2026-08-20): adopted the owner's polished FieldPro "Command Center" reference (artifact cf7e449d) as the CRM** — rebuilt as a full clickable app: 12 screens (Dashboard/Clients/Jobs/Schedule/Phone/Inventory/Email/Reviews/Automations/Reports/Connections/Settings; Basic = first 4 + Settings, Full = all 12), clickable rail on desktop + bottom-nav & More menu on mobile. Deliberate alterations (told owner, can revert): retinted FieldPro's midnight-teal → Top Shelf warm-black + #C9A86A gold + Figtree font (scoped `.fp-scope`, no collision with marketing CSS); "Taylor"→"Mike"; business = Delgado Plumbing (consistent w/ call+review demos). Desktop CRM scales to fill the 16:9 laptop; dashboard mark-done→Request-review still fires the review demo. **Next steps:** (1) Build Concept A into the real `top-shelf-website` repo (NOT cloned here — pull on a desktop session): wire the real free-audit form, run the **humanizer** pass on the visible copy (not yet done — concept draft), keep the demos. (3) Open trust upgrade neither demo fills: one REAL named local (plumber/HVAC) testimonial — owner supplies, never fabricate. (4) This ALSO clears the standing FTC "REAL RESULTS" blocker (below) by REPLACING claims with demos.

---

## ▶ PROSPECT-FINDING PIPELINE — CEO + owner eval → branded slideshow (2026-08-20, LATEST)

**Goal (owner):** have a CEO agent + a business-owner agent evaluate the **prospect-FINDING automation** (the `topshelf-lead-finder` pipeline that discovers/scores businesses — distinct from the marketing-site eval below), say what's good / what to improve / why — **no changes, no renders** — then turn it into a **Top Shelf-branded HTML slideshow he can follow easily.**

**How:** mapped "ceo/business-owner agent" onto the pipeline's OWN two reviewer personas (`topshelf-seo-audit/scripts/topshelf/judges.py` — a Top Shelf CEO + a cold owner). Dispatched BOTH for real ([[dispatch-every-council-seat-for-real]]): Business Strategist (CEO seat) + a general-purpose owner-operator persona. Read the finder end-to-end (`SKILL.md`, `scoring-rubric.md`, `config.yaml`, `lf_scoring.py`, `flags.py`, `winners.yaml`, `csv-schema.md`, `lf_cli.py`) + skimmed audit/sample/outreach. Fact-checked every load-bearing claim vs source ([[verify-agent-findings-before-acting]]); corrected 2 agent overstatements. **Judge PASS (1st).** Episodic log: `growth-marketing.md` (committed vault `a0e8176`).

**Findings that matter — both seats converged on the top 3 (highest signal):**
1. **No proof the ranking predicts a sale; the learning loop has never fired.** `winners.yaml` is empty (`{}` everywhere) → winner_boost + Step-6 feedback have never run; rubric §7 admits the weights are an unvalidated prior; nothing scores **retention** (a Won that churns month 2 is unprofitable). → *Fix: instrument — stamp rank/gap-shape/vertical/city at first contact, record outcome + 90/180-day survival, feed RETAINED wins into the boost.*
2. **"Recommended" is hollow + the queue is oversized.** Weak-site override forces Recommended for 7/8 grades; real triage `work_rank ≥ 45` = **95/351** Denton rows, and the count drifts up as contacts enrich (reach=0 at discovery). → *Fix: fixed top-N by work_rank (~10–15 = real build capacity), rename `recommended`.* (Nuance: NOT auto-spend — a manual "Approved" gate sits before the builds.)
3. **The website grade rides stale into the pitch.** Set once at discovery, reused by the audit ("do not re-fetch"); `unknown`/JS-shell can grade weak and get pitched with only a prose reminder to look. → *Fix: date-stamp scans + re-run if old; treat unknown/js-shell as "needs a human look".*
- **CEO-only:** ICP is really "weak site" not "strong presence + weak site" (no website-independent ability-to-pay signal); attended-browser GBP read is the true scale ceiling (not tokens); model categoricals (franchise/gbp_status) have no verification layer; live demo pages hotlink non-consenting businesses' Google photos (legal/fragility). **Owner-only:** rerun footguns (silent no-op w/o `--rerun`; re-score on thinner data); `G:\`/Chrome hard deps → half-scored city looks done.
- **Two fix-first picks (complementary):** Owner = cap the weekly queue NOW (cheap); CEO = close the conversion+retention loop (the only change that becomes a moat). Do both, in that order.

**✅ DELIVERED — branded slideshow:** 12-slide deck on the REAL Top Shelf design system (espresso `#0B0906`/gold `#C9A86A`, Cormorant Garamond + Jost, embedded TS logo from `top-shelf-website/assets/img/email/*`). Keyboard/swipe/dot nav + progress bar; findings use an Issue→Why→Fix frame with CEO/Owner/Both tags. Private Claude artifact (redeploys same URL): **https://claude.ai/code/artifact/fedd9220-64da-49a4-977e-8a928e156399** . Local source: scratchpad `prospecting-review-deck.html`. Verified render via DOM/console (12 slides, logos+fonts load, no errors) — in-app browser pane won't composite here, so no screenshot ([[browser-pane-no-composite-verify]]). Nothing in the pipeline was changed.

**🔴 DISCREPANCY TO RECONCILE (verified from source this session):** the **desktop** `~/.claude/skills/topshelf-lead-finder/scripts/lf_scoring.py` I evaluated is the **pre-2026-08-11 5-section model** — winner_boost + reachability + longevity all PRESENT, weights **30/20/30/10/10**, traction `log10(rc+1)/2.0`, `unknown` INSIDE `_WEAK_SITE`, `work_rank = 100·wg·buy_signal`. This **contradicts the "▶ START HERE (2026-08-11)" section below**, which says a rework (killed those 3 inputs, weights 30/30/40, traction `/2.85`, `unknown` excluded, `wg` dropped from work_rank) was built + pushed to the `taylor-os/topshelf-*` skill repos. So either the desktop clones are STALE (server rework never pulled) or that rework isn't on the desktop branch. **Decide which is canonical before acting — some recommendations may already be addressed server-side.** My eval reflects the LIVE DESKTOP code.

**Next 3 steps:** (1) Owner reviews the slideshow → pick which findings become build tickets; the two headline actions are cap-the-queue (config `work_rank_threshold`/top-N) and instrument-the-loop (recording only — winner_boost machinery already exists). (2) **Reconcile the scoring-model discrepancy above** — `git -C ~/.claude/skills/topshelf-lead-finder log`/`pull` and diff against the 08-11 claims to see which model is live where. (3) Optional: file the deck to `Top Shelf\Documents` on Drive / export a PDF if the owner wants a static copy.

---

## ▶ MARKETING-SITE TWO-LENS EVAL — CEO + customer, evaluation only (2026-08-20)

**Goal (owner):** have a CEO/strategist agent and a business-owner/customer agent go over the MAIN pages of the live marketing site topshelfsolutions.io (Home, Why Us, Pricing, Contact/Free-Audit) and say what's really good and what should improve. **No renders — evaluation only.** Delivered in chat; Judge PASS (1st).

**How:** all 3 screenshot paths were down at once on this desktop (in-app Browser pane won't composite · Playwright hit Cloudflare 403 · Claude-in-Chrome not connected) → captured the 4 pages' real copy + design system via live DOM/CSS instead, disclosed the limit. Both lenses dispatched for real (Business Strategist + Persona Walkthrough "Mike, DFW plumber burned by Hibu"), grounded in the captured content. Full writeup was in scratchpad `topshelf-eval.md` (ephemeral) — key findings preserved here.

**The finding that matters:** both lenses independently RANKED the same #1 issue — the anonymous, unlabeled **"REAL RESULTS" case studies** (index.html ~L452–469) undercut the site's whole honesty positioning (CEO: credibility + FTC exposure; customer: BS-detector trips at peak intent). **This is the SAME item already tracked below as the 🔴 FTC hard blocker (D1) — this eval is independent corroboration, not a new item.** Both also converged on: (2) the 6-mo term needs a visible safety-net/guarantee, not just an honest explanation; (3) the hero stats (62%/$49K/97%/73%) need a source line; (4) the offer needs a risk-reversal. CEO-only adds: no About/team/proof-of-existence; "you own everything" headline overreaches the CRM/automations fine print; generalist-across-8-verticals vs "demand a specialist"; $499 Essential is the anchor but every dramatized gap lives in $899 Signature (make Signature the visual default). Customer-only adds: "Ten Services / One Flywheel" = overload + the one buzzword that lost him; reassure at the phone field ("we'll text the report; only call if you ask").

**Net:** messaging/positioning/honesty-framing/brand are genuinely strong; the gap is PROOF — the site claims honesty + results but doesn't yet SHOW them. Design system confirmed: warm-black #0B0906 / gold #C9A86A / Cormorant Garamond serif + Jost sans / square corners — a cohesive "top-shelf spirits" look.

**Next 3 steps:** (1) if owner wants action: rewrite the "REAL RESULTS" section (one real named client OR transparently-labeled "illustrative math" + the same "not a guarantee" disclaimer already on the Gap Finder quiz) — this is BOTH the eval's #1 and the standing FTC gate before first outreach send; lives in `taylor-os/top-shelf-website` (NOT cloned on the Taylor OS server — pull on a desktop session). (2) draft a risk-reversal guarantee for the offer + add source lines under the hero stats. (3) optional polish: trim the homepage "Ten Services" to 2–3 hero services + drop "flywheel"; add a phone-field reassurance line on contact.html.

---

## ▶ PRICING LADDER LOCKED — 3-tier Blends, honest brainstorm (2026-08-19)

**Goal (owner):** keep the $299 Essentials Blend exactly as-is; brainstorm strategy + a realistic, value-based price for the OTHER two tiers (Signature + Platinum), grounded in Top Shelf's real services and the à-la-carte cost benchmarks. Honest brainstorm only — **no code, nothing codified** (owner: "I don't want anything coded anywhere"). Ran grow-pricing + grow-offers, then a REAL 2-seat council (per [[dispatch-every-council-seat-for-real]]): a CEO agent + a skeptical-plumber customer agent — both dispatched for real, not synthesized.

**✅ LOCKED LADDER (owner decisions, 2026-08-19 — do not re-ask):** a clean **3-tier** good-better-best (capture → operate → grow):
- **Essentials $299/mo** (unchanged) — free site + AI front desk (chat/voice/text-back) + reviews + booking + unified inbox + **basic** CRM.
- **Signature $899/mo** (hold — proven, ~40% off the ~$1,300–1,600 à-la-carte stack; the "Most Popular" hero) — adds **full CRM** (estimates/invoices/**card payments**), **own softphone line**, deeper automation, **local SEO**, GBP optimization, done-for-you review responses.
- **Platinum $2,500/mo** (RAISED from the old $1,599; scope-capped; **application-only**) — adds managed ads (**one** platform; 2nd +$500–750), weekly GBP posts, paid social, premium SEO + content, priority/dedicated support. **Ad spend billed separately** (pass-through, stated minimum).
- **Terms, all tiers:** $0 up front · no setup fee · **6-month minimum then MTM** · per domain · **no refund**.

**Owner's 4 explicit calls (2026-08-19):** (1) **3 tiers now**; keep the **$599 "Operate" rung** (Essentials + get-paid tools, no SEO) in reserve — the council's convergent fix for the $299→$899 gap; launch only if/when real deals stall there. (2) **Platinum $2,500** confirmed. (3) **NO cancel-anytime — keep the 6-month minimum** (owner reason: the term is how Top Shelf absorbs the up-front build so the customer pays **$0 down**). This OVERRIDES both council seats, who wanted the minimum dropped as the #1 trust lever. (4) **NO money-back guarantee.**

**⚠️ SUPERSEDES the older "House Blend" pricing discussions below** (Phase 3 §2026-08-11 + Essentials-Blend §2026-08-17). Now dead: the separate "House" name, the no-website-at-$299 rule (Essentials $299 KEEPS the website per the 08-17 lock), the $149 option (held at $299), cancel-anytime, and the 30-day guarantee. Ladder is now **3 tiers** — **$499 Essential retired**, **Premium→Platinum repriced $1,599→$2,500**. The Essentia**ls**/Essentia**l** naming collision is resolved by retiring the $499 tier.

**Council convergence worth keeping (both seats, independently):** (a) real cannibalization risk is DOWNWARD — Essentials at $299 is so loaded (free site + voice AI) prospects may never climb → frame tiers by JOB, make local SEO the visual hero of Signature's delta; (b) Platinum is a dentist/established-shop tier, NOT trades-outbound (plumber = hard no at any price) → price high + application-only; (c) with no track record, risk-reversal normally beats social proof — but owner chose the $0-down/6-mo-term trade instead, so the trust load now rests on **the live demo (hear the AI answer a call) + GDR/TAP as capability proof + framing the term as "we eat the build cost so you pay nothing up front."** CEO also: fence Signature's SEO scope + Platinum's content cadence so open-ended service work doesn't blow up a solo's capacity (~15 hrs/mo per Platinum client caps at ~6–8 clients).

**✅ BUILT:** a 2-slide branded visual checklist (Top Shelf espresso/gold) — at-a-glance cards + full inclusion matrix — as a private Claude artifact (editable, redeploys same URL): https://claude.ai/code/artifact/c858cf7c-3536-4049-a6a0-7cf51861315b . Local source: scratchpad `top-shelf-blends.html`. Stamped "working draft — not in offer.py / live site."

**Next 3 steps:** (1) When owner gives an outward-facing go, **codify the new ladder in `offer.py`** — a full restructure, not just a $299 add: Essentials $299 (rank 0) · keep Signature $899 · retire Essential $499 · rename Premium→**Platinum $2,500**; update AUTHORIZED_AMOUNTS; then mirror to the live pricing page. NOT done — needs explicit go (touches the money source-of-truth + live site). (2) Iterate the artifact deck on owner feedback (candidate edits: add the $599 rung as a ghosted column; put features in customer words). (3) Keep the $599 "Operate" rung spec ready; launch only on the stall trigger.

---

## ▶ CRM ARCHITECTURE-FIX PASS — suite was RED on Linux/CI, now GREEN (2026-08-18, LATEST)

**Goal (owner):** review CRM codebase architecture, fix what's broken, and test each fix right after so nothing silently breaks.

**What was actually wrong:** the suite was green on the case-insensitive dev machine but **red on Railway/CI** (case-sensitive Linux). Two root causes, both fixed + tested:
1. **No `config/inertia.php`** → Inertia's default testing page path `resources/js/Pages` (capital P) couldn't resolve the repo's real lowercase `resources/js/pages` on Linux, 500-ing every `assertInertia(...->component('...'))` file-existence check (6 tests). FIX: published `config/inertia.php` with paths corrected to lowercase.
2. **4 stale tests** in `DashboardTest` + `TenancyFoundationTest` still encoded the *pre-restructure* altitude model (login→`/agency`, `agency/dashboard`). Commit `4cc83e8` had moved login→`/company` (house account), renamed agency list to `/clients`→`agency/clients`, and made `SetCurrentLocation` default to the house company. Updated assertions to the shipped architecture (incl. rewriting "sub-account unreachable" into a real "defaults to house company" invariant).

**Result:** full suite **323 passed** (was 40 failed on Linux: 30 needed a frontend `npm run build` first, 10 were the two real defects). Swept all 43 `Inertia::render()` targets — every one has a matching page component. Committed locally `3d1cf00`; **push proposal waiting** at `vault/_outbox/push/top-shelf-inertia-case-fix-3d1cf00.json` (owner Approve to push `main`).

**Next 3 steps:**
1. Approve the pending push so the green suite lands on `main`/CI.
2. **Confirm CI builds frontend assets before `php artisan test`** — else the 30 Inertia-render tests show false reds in CI (they need `public/build/manifest.json`).
3. Finish the fix batch below (owner said "Yes" to A+B fixes and D deletion; hold `CallOverlay`).

## ▶ §0.10 SWEEP — A+B FIXES + D DELETION ALL DONE (2026-08-18, LATEST)

**Batch complete + PUSHED to `main` (`0f80a2f`, Railway auto-deploys the CRM).** Full suite **327 passed**. Owner approved the push directly in-session because the Taylor OS Approve button was broken; pushed with the env's GITHUB_TOKEN (note: this env is NOT credential-scrubbed, contrary to the old handoff claim). Both outbox push proposals removed (landed). **Origin repointed** `taylor-os/top-shelf` → `taylor-os/top-shelf-crm` (GitHub renamed the repo; push followed the redirect, remote set-url updated). Owner also said: don't rebuild/refresh the phone APK until all work is finished — a CRM push doesn't touch the APK anyway. Commits: A1 `098ac30`, A2 `25970ea` (conversation reply reuses closed thread — dropped `status` from firstOrCreate match at ConversationController + WorkflowEngine), A3 `1f40e88` (name-only contact no longer dedups to a stranger), B1 `c691813` (shared `Contact::findByPhone`), B2 `75aecc5` (inbox `->limit(100)`/contacts `limit(500)`), D `0f80a2f` (~975 LOC dead starter-kit removed). **`CallOverlay.tsx` HELD** — owner to confirm whether the incoming-call UI is meant to be wired up (delete if not). **C (architecture refactors: WorkflowEngine dispatch map, Telnyx signature dedup, CallFlow extraction, ProspectPipeline) still NOT approved — offer separately.**

**Cross-surface sync done (2026-08-18):** `vault/projects/top-shelf/context.md` (the durable business/technical brief, distinct from this save-state) rewritten with a matching dated section — repo-rename pointer, the A/B/D fix list, the CallOverlay + C open items, and the Approve-button/direct-push governance note. Both files now agree; nothing further to reconcile here.

<details><summary>original findings/spec (delivered)</summary>

## ▶ §0.10 SWEEP — findings delivered, fixes IN PROGRESS (2026-08-18)

Ran the combined dead-code + correctness + architecture review (3 parallel read-only agents, findings verified against source). Owner approved fixing A+B (test-first) and deleting D dead code except CallOverlay. **Toolchain note:** vendor/ + node_modules/ are NOT committed; on a fresh clone run `composer install && npm ci && npm run build && cp .env.example .env && php artisan key:generate` before `php artisan test` (SQLite in-memory). GateGuard hook fires before every Bash/Edit — present the 4 facts and retry (or set `ECC_GATEGUARD=off`).

**✅ A1 DONE + committed `098ac30`:** invoice numbering collision. Added `Invoice::nextNumber(int $locationId)` (max-based, not count) at `app/Models/Invoice.php`; both `InvoiceController::store` and `WorkflowActions/CreateInvoiceAction` now call it. Test `test_next_number_avoids_collision_after_a_deletion` in `CreateInvoiceNodeTest`. 14 invoice/payment tests green.

**REMAINING (each: write failing test → fix → run that filter → commit):**
- **A2 — closed-conversation thread split.** `MessageDispatcher::sendToContact` matches `firstOrCreate` on `(contact_id, channel)` but `ConversationController.php:169` and `WorkflowEngine.php:307` add `'status'=>'open'` to the MATCH array → a send against a closed thread makes a 2nd conversation. Fix: drop `'status'=>'open'` from the match at both sites (keep it only as a create-default, as MessageDispatcher does). Test in `ConversationsTest`/`TextsAndInboxTest`: close a convo, send → same conversation id reused.
- **A3 — name-only contact returns a stranger.** `ContactController.php:73-76`: when email+phone both empty the dedup query is `Contact::query()->first()`. Fix: only run dedup when email or phone present (e.g. wrap the `$existing` lookup in `if (!empty email || !empty phone)`), else skip straight to create. Test: two name-only contacts both persist.
- **B1 — inbound-SMS full-table phone scan, duplicated.** `RecordTelnyxMessage.php:103` + `ConversationController.php:192` both `Contact::whereNotNull('phone')->get()->first(fn…toE164)`. Minimal: extract one shared `contactForNumber(string $e164)` (the controller already has a private `contactForNumber`) and have both use it; add a `ponytail:` note that the real ceiling is a normalized E164 column (migration) if volume grows. (Efficiency, not a bug — lower priority.)
- **B2 — inbox unbounded.** `ConversationController.php:89` `->get()` all SMS convos. Fix: `->paginate(25)` mirroring Contacts index; the React `location/inbox` / `conversations/index` page may need to read `.data`. Verify the page still renders (build + a feature test).
- **D — delete ~975 LOC dead starter-kit** (proven 0-ref by the audit): `resources/js/components/ui/{select,alert,badge,icon,toggle,toggle-group}.tsx`, `components/{app-header,appearance-dropdown}.tsx`, `layouts/app/app-header-layout.tsx`, `layouts/auth/{auth-split-layout,auth-card-layout}.tsx`, `pages/dashboard.tsx`, `components/ui/placeholder-pattern.tsx`, `app/Console/Commands/CheckLogin.php`. **HOLD `resources/js/components/softphone/CallOverlay.tsx`** (243 LOC) — unreferenced but may be unfinished incoming-call UI; owner to confirm intent. After deleting, `npm run build` (catch any stray import) + full `php artisan test`.
- **C (architecture, NOT approved yet)** — deferred: WorkflowEngine dispatch map (C1, highest leverage), Telnyx signature-verify dedup (C2), voice-webhook `CallFlow` extraction (C3), ProspectPipeline service (C5). Offer separately.

**Verify at the end:** full `php artisan test` green; then commit each fix separately and add a push proposal to `vault/_outbox/push/`.

</details>

---

## ▶ ESSENTIALS BLEND — the main outreach engine · CRM BUILD SHIPPED (Judge PASS, 2026-08-17, LATEST)

**Goal (owner):** make the "find local businesses with NO website → auto-build them a free demo → call/text → one-click onboard → recurring $" model (from Jason Wardrop's "Build Once, Sell Forever" video, `PBS73aETisw`) Top Shelf's **main outreach process**, branded **"Essentials Blend."** THIS task = research + brainstorm + deliverables ONLY, no CRM code (owner: "not building anything yet").

**✅ DELIVERED + pushed (`top-shelf-crm` `5e1c82d`, all in `docs/essentials-blend/`):**
- `essentials-blend.md` — full teardown: the 5-beat process (Find→Wow→Reach→Close→Onboard), verbatim scripts, stats (1-in-3 no website, 62% don't answer, 18s onboard, $2,800/mo value), both tools' exact behavior, pricing, a **mapping of what the CRM already owns vs. what's new**, the implementation + operate brainstorm, risks, and the full transcript.
- `slides.html` — 9-slide Top Shelf deck (dark espresso). `find-prospects.html` + `onboard-client.html` — the two CRM page mockups in the real design, inside a CRM shell showing a new `Prospecting ▸ Essentials Blend ▸ Find Prospects / Onboard Client` sub-nav.

**Owner decisions (4-Q interview 2026-08-16 + refinements 2026-08-17 — don't re-ask):** (a) Find Prospects = NEW focused sub-page, KEEP the existing board; (b) Onboard page = instant-provisioning ONLY for now; (c) **Essentials Blend price LOCKED at $299/mo** (2026-08-17; codify in offer.py + site before customer-facing); (d) finder shows the auto-built demo INLINE; (e) entry CRM = basic **Essentials CRM** (contacts from the website chatbot / AI receptionist / online forms) — the full-suite CRM + payment processing + softphone are a HIGHER, pricier tier (deliberate anti-cannibalization lever); (f) owner wants the offer DISPLAYED on the marketing website → brainstorm added as **§8** of the doc (offer.py→site mirror first, Blends page + Essentials landing, slot into the CRM-page-Linear redesign).

**🟡 PRICING DIRECTION — owner is going with (ii); two flags remain.** The $299 Essentials Blend LEADS WITH A FREE WEBSITE (the video's wedge), which contradicts the **Phase 3 council rule (2026-08-11, below): a $299 tier must NOT include the website** (the free website is what $499 Essential sells). As of **2026-08-17 the owner is proceeding with (ii): free-website-at-$299 stays, and tiers are differentiated by CRM/feature DEPTH instead** — Essentials gets the basic Essentials CRM; the full-suite CRM + payments + softphone are up-ladder. Cleaner anti-cannibalization lever than website-presence. STILL OPEN: (1) $499 Essential likely needs repositioning now that $299 also includes a website; (2) the "Essentia**ls**" $299 vs "Essentia**l**" $499 one-letter naming collision needs a distinct name.

**⚠️ Fact status:** $299 tier is NOT in `offer.py` (authoritative: Essential $499 / Signature $899 / Premium $1,599). Marked "proposed" throughout. Must be added to `offer.py` + live pricing page before any prospect-facing use.

**🔨 CRM BUILD (2026-08-17, IN PROGRESS on `top-shelf-crm`).** Owner said build it for real (website deferred). Plan: `docs/superpowers/plans/2026-08-17-essentials-blend-crm.md`. **Honest scope (owner, option A):** provision the **8 real** services for real; the **3 not built per-client** — `voice_ai`, `chat_widget`, `missed_call_text` — are catalog `status:pending`, never rendered/provisioned active (feasibility map found: voice AI receptionist = voicemail only; no in-CRM chat widget; missed-call text-back is agency-only).
- ✅ **Foundation @ `2bfe73a`:** `NavItem.items[]` + collapsible `nav-main` + Prospecting parent (Find Prospects / Onboard Client / Pipeline board, house-only); `config/essentials.php` = the 11-service catalog (single source for UI + provisioner); `location_services` table + model.
- ✅ **Backend core @ `26924e3`:** `EssentialsProvisioner::onboard()` creates a client `Location` + admin, seeds contact / Sales Pipeline(+5 stages) / calendar / Free-Audit funnel / 3 published workflows (contact.created nurture · review.requested · message.received→ai_reply), enrolls the contact, writes 11 `location_services` rows (honesty gate by construction; **never charges**). `EssentialsController` find/onboard/provision + routes `location.essentials.*` (house-guarded). **12 Essentials tests pass.**
- ✅ **Un-broke the test suite:** `tests/Feature/AssistantTest.php` had a `private seed()` illegally narrowing the framework's public `seed()`, fataling ALL PHPUnit discovery → renamed `seedFixtures()`. Now **319 pass**; 4 pre-existing failures surfaced (`TenancyFoundationTest`+`DashboardTest`, all legacy `/agency` altitude — chip `task_648ccbbe`, NOT this feature).
- ✅ **Pages @ `a818af5`:** `resources/js/pages/agency/essentials-{find,onboard}.tsx` mirror the mockups in real CRM components, wired to real props/routes. Honesty upheld — the 3 pending services render "Activation pending", never active (the build deliberately did NOT copy the mockup's fake all-11-live). `HandleInertiaRequests` shares the `provisioned` flash so the onboard result reaches the page.
- ✅ **Verified:** full suite **319 pass / 4 pre-existing** failures only (TenancyFoundation+Dashboard `/agency` drift, chip `task_648ccbbe`); 12 Essentials tests green; `npm run build` + `tsc --noEmit` clean (0 new errors); app boots (`top-shelf` preview → :8125), serves built assets with no errors, React/Inertia renders, 3 routes registered, auth-gating redirects unauthed→login. **NOT verifiable here:** an interactive authed UI walkthrough — the in-app browser pane won't composite / hold the login session (env limitation, not the feature); authed page behavior is proven by `EssentialsPagesTest`. Live outbound (Telnyx/Resend/AI) is prod-only (no local keys).
- ✅ **Judge PASS (1st, 2026-08-17):** the judge-agent ran the suite + production build itself (319 pass / 4 pre-existing; honesty gate verified at all 3 layers; no charge; scope-honest). 2 non-blocking notes: (1) onboarding marks the `website` entitlement active but does NOT itself publish the client's public site — that's the separate Build-demo/sample-builder flow; one-click site-standup = a follow-up; (2) `offer.py` still needs the $299 line (outreach artifact, outside this CRM deliverable). Live authed UI click-through = CAN'T-VERIFY (the in-app pane won't hold the login session).
- **FOLLOW-UPS (separate projects):** codify Essentials $299 in `offer.py` (+ mirror to the site); build the 3 pending services (per-client Telnyx numbers + voice AI, embeddable chat widget, per-client missed-call text); §8 website display. Also the stale `/agency`-altitude tests (chip `task_648ccbbe`).

**Judge:** independent judge-agent dispatched over all 4 deliverables at end of session — verdict recorded in the episodic log / reported to owner.

**Next 3 steps:** (1) **Finish the CRM build:** review+commit the two Inertia pages, run the full suite, boot the app and verify onboarding end-to-end, then Judge + episodic log. (2) **Codify Essentials Blend $299 in `offer.py`** (new entry PLAN rank 0; add $299 to AUTHORIZED_AMOUNTS; inclusions = free website + AI bundle + Essentials CRM; NOT full CRM/payments/softphone) + mirror to the live site — the gate for anything customer-facing. (3) Pick a distinct name (Essentials vs Essential collision); build the 3 pending services as a separate integration project (per-client Telnyx numbers + voice AI, chat widget, per-client missed-call text); then the §8 website display.

---

## ▶ NEW WORKSTREAM — CRM Page + Service Demos (Linear-inspired) · RESEARCH DONE, RENDERS IN FLIGHT (2026-08-12)

**Goal (owner):** study linear.app, then design a new **CRM page** (with an *interactive* product demo) + a set of **per-service interactive demos** (review mgmt, Google review answering, business softphone, automated booking, calendar, AI receptionist + more) for the Top Shelf **marketing site**. Point of every demo: make a home-service owner *feel* how it helps them (what is it / how does it work / how does it help me). Research now, **build later**. He wants renders of the sections/pages.

**Where it lives:** all deliverables in `vault/projects/top-shelf/crm-page-linear/`. NOT touching the CRM app repo — this is for the marketing site `taylor-os/top-shelf-website` (NOT cloned on this server; pull it when we build).

**✅ DONE this session:**
- Captured linear.app myself (playwright): home + /plan + /build + /customer-requests full-page shots in `crm-page-linear/linear-shots/`.
- 4 research agents → `01-linear-structure.md` (IA/layout DNA + replication skeleton), `02-linear-motion-assets.md` (how the motion/assets are built + cheap CSS-first playbook + 11 effect components), `03-crm-page-and-demo.md` (CRM page section order + the "Day in the Life of a Lead" interactive demo, approach = self-built scripted click-through), `04-service-demos-catalog.md` (13 demos / 6 pages, shared component kit, build priority).
- **`00-PRODUCTION-PLAN.md`** = the master synthesis (design system "Linear grammar in a Top Shelf skin", site map, CRM page order, demo script, service catalog, shared kit, 4-phase build order, guardrails). READ THIS FIRST on resume.
- **Renders** in `crm-page-linear/renders/`: shared `site.css` (the espresso/ivory/gold design system) + 5 HTML mockups being built by agents: `crm-page.html` (✅), `crm-demo-centerpiece.html` (✅), `services-hub.html`, `service-speed-to-lead.html`, `service-reputation.html`.

**STATUS 2026-08-13:** Research pack + `00-PRODUCTION-PLAN.md` + all 5 HTML renders (+ full-page PNGs) = **DELIVERED, Judge PASS** (after 1 REVISE — vault race had deleted the spine; recreated). Then **OWNER DECIDED** the flagship demo = a GENUINELY CLICKABLE self-built replica app in a **desktop-monitor frame** + a separate **mobile phone-frame** version (Desktop/Mobile toggle) — NOT live backend/iframe, NOT paid embed — and approved building the first slice. → **BUILT + Judge PASS (1st): `renders/crm-demo-interactive.html`** (single self-contained 45KB file, vanilla JS, all 8 interactions wired, both device shells premium). Preview boards (24h TTL): renders = `/preview/b/b_aa778bab80`; clickable demo = `/preview/b/b_b5bd04ee8d`.

**Key decisions (don't re-litigate):** demo = **clickable self-built replica app** in device frames (monitor + phone), free-click modules + a playable 6-step "Day in the Life of a Lead" story. IA = **Services Hub + 6 Linear-style feature pages** cross-linked to the CRM page. 3 build-first heroes: Missed-Call Text-Back, Reputation Triage, AI Receptionist. No fabricated stats; "find the gap" not "leak"; humanizer + Judge before ship.

**Next 3 steps:** (1) Owner reviews the clickable demo (board above) → confirm the monitor framing (currently elegant/subtle; chunkier is an easy tweak) + any content changes. (2) Pull `taylor-os/top-shelf-website`; build **Phase 0** (promote `site.css` tokens + `GlowFrame`/`DemoStage`) then **Phase 1**: the real CRM page embedding this demo + Services Hub shell + the 3 hero service demos. (3) Phases 2–4 per `00-PRODUCTION-PLAN.md` §8; harmonize demo domain `app.topshelf.co` → real `topshelfsolutions.io` at build.

**⚠️ Env notes:** vault here is NOT git and the **brain handoff gets pulled fresh on session restart** (my mid-session narrative edits were reverted once) — durable record is the git-versioned `.claude` memory `topshelf-crm-page-linear-demos`. Also `/tmp` backups do NOT survive a restart. Parallel subagents writing the same vault subtree silently deleted sibling files mid-build (keep the vault copy, re-check before Judge).

---

## ▶ Home-Service Blend GTM · PHASES 0–1 DELIVERED, both owner decisions RESOLVED (2026-08-13) — next: codify into `offer.py` + live site, then Phase 2

**✅ PHASE 0 + PHASE 1 DELIVERED & Judged.** Cost-research pack at `vault/projects/top-shelf/home-service-blend/00-research-pack.md` (Judge PASS after 1 REVISE) + $899 Signature Blend one-pager at `.../01-signature-onepager.md` (Judge PASS after 1 REVISE). Honest à-la-carte finding: **~$1,300–1,600/mo (typical) reconciled to ~$1,475/mo** stitching together separate CRM + VoIP/AI-receptionist + website ($2k–8k one-time, now folded in) + SEO/reviews, **vs. $899/mo flat with Signature**, math sums exactly (200+50+149+50+750+125+150=1,474).

**✅ BOTH OWNER DECISIONS FROM THE ONE-PAGER, ANSWERED 2026-08-13 (do not re-ask):**
- **(a) YES** — add the website chat assistant + online-booking widget to the Signature Blend. Chat assistant priced into the comparison table at $50/mo (from the research pack); booking widget listed as a feature, kept out of the cost table (usually bundled/near-free elsewhere, would pad the comparison).
- **(b) YES** — Signature's "Review Management" now means **requesting reviews + writing the responses + monitoring** (owner: "that will be with the requesting reviews and will be incorporated into 'Review Management'"), not just automated requests.
- Both applied to `vault/projects/top-shelf/home-service-blend/01-signature-onepager.md` this session.

**Contract terms locked (owner, 2026-08-13):** **no setup fee**; **6-month minimum initial term, then month-to-month** — the floor exists to guarantee some return on the setup work done up front. Applies to the Signature Blend GTM; carry into `offer.py`/sales copy when codified.

**🔴 NOT yet codified — `offer.py` and the live site do not match the one-pager.** The authoritative package source (`offer.py`, `AUTHORIZED_AMOUNTS`-linted) still lacks the 3 new Signature inclusions (chat assistant, booking widget, review-response writing), and neither does the live topshelfsolutions.io pricing/solution pages. Deliberately deferred — an outward-facing/authoritative-source change, done as its own careful pass rather than folded into an already-long session. **Do this before trusting the one-pager against the rest of the sales stack, and before Phase 2 outreach quotes these inclusions as real.**

**Next 3 steps:** (1) Add the 3 new inclusions to `offer.py`'s Signature entry (no price change) + mirror into the live pricing/solution pages. (2) **Phase 2 — targeting + 5-touch outreach scripts** (recommended fresh session, per Home-Service Blend roadmap). (3) Remember the **FTC "Real Results" hard blocker** (see the "▶ START HERE" section below) still gates any real outreach send, independent of Phase 2 progress.

**Note:** vault is NOT git on this server → this handoff can't be pushed from here (rides vault autosync elsewhere). Repo `main` unchanged (`dc09316`).

---

## ▶ PHASE 3 IN PROGRESS — Convert / pricing (2026-08-11)

**On Phase 3 ("Convert": interested prospect → fast yes → in the door → upsell).** OWNER REFRAME (do not re-litigate): ~$200/mo total cost, NOT worried about early churn, wants a FAST low-friction yes on a cheap entry, get in the door, upsell what you can't see from outside. Lightweight convert, not a heavy MSA/kill-fee. Deliverable so far is a PROPOSAL — nothing coded/changed yet.

**GROUNDED IN THE REAL CATALOG (read `topshelf-seo-audit/scripts/topshelf/offer.py` + live site).** The real "Blends": **Essential $499/mo** (free custom 5-page website + logo + GBP optimization + basic SEO + automated reviews + missed-call text-back; "vs $1,582/mo to hire out"), **Signature $899** (MOST POPULAR — adds CRM + AI receptionist 24/7 + <5min follow-up), **Premium $1,599** (adds ads/social/GBP posts). Website is FREE on every Blend. 6-mo term then MTM. `offer.py` is the linter's source-of-truth (`AUTHORIZED_AMOUNTS`) — a $299 price it doesn't list fails the build.

**OWNER DIRECTIVE:** the FIRST price a prospect sees = **$299 with a few services** (a new tier BELOW the $499 Essential floor).

**Council re-run — BOTH seats REAL Agent dispatches this time** (CEO `a1f64056e495927f2`, customer/plumber `a2ddcd6b207d2a683`; see [[dispatch-every-council-seat-for-real]]). Verdict:
- **$299-first is a GO — HARD RULE: the $299 must NOT include the website.** That omission is what stops it cannibalizing $499, makes it the only tier deliverable solo at volume (config not build), and anchors the line UP (a decoy that pushes toward Essential/Signature) instead of dragging it down. Both seats agreed (plumber: "drop the website, capture my calls").
- **Recommended: "House Blend" $299/mo** · nothing up front · **month-to-month, cancel anytime** (drop the 6-mo term on the starter) · includes **missed-call text-back (LEAD WITH THIS)** + lead-to-text + GBP cleanup + automated reviews · **no website**.
- **Anchoring move (CEO):** say $299 first in the SALES SCRIPT, but keep **Signature $899 the visual hero** on the pricing page (House = small "start here →"). Copy fix: "free website on every plan" → "free website on every **Blend**".
- **⚠️ HONEST TENSION owner must decide:** at $299 the plumber does NOT whim-yes — he goes to "prove it." His whim-yes is **$149** with just text-back. So $299 = proof-to-close (higher ticket), $149 = frictionless (lower ticket). To close at $299 he demands 3 closers: (1) live demo — watch the text-back fire before paying; (2) a real tradesman reference — **can't be honest yet (0 outside customers) → use GDR/TAP + the demo**; (3) a 30-day money guarantee ("doesn't text a missed call → pay nothing").
- **Vertical:** $299-first is built for TRADES; for DENTISTS it reads small-time — lead them with Signature. Salon feels the missing website more.

**Next 3 steps (Phase 3):** (1) owner answers 5 decisions: House $299-no-site MTM? · hold $299 vs drop to $149? · add 30-day guarantee? · pricing-page: lead $299 vs Signature-hero? · make it real? (2) if yes: add House to `offer.py` (or its linter rejects $299) + update pricing page + "every Blend" copy — **outward-facing money change, needs explicit owner go**; (3) build `lo_convert.py` convert engine (per-prospect/vertical drafts, GDR/TAP proof only, never-send + humanizer + Judge).

**Open question:** hold $299 for ticket, or add the $149 for velocity? (customer seat: $149 = yes-without-thinking).

---

## ▶ PHASE 2 — outreach engine COMPLETE (2026-08-11)

**We are on Phase 2 ("the approach": deliverables + outreach + send-automation decision).** Phase 1 remediation (B1–B6 + scoring, below) is done. This session rebuilt the **cold first-touch outreach** from a website-only template into a condition-specific, easy-win-first engine — owner's explicit ask: "don't come off as generic one-size-fits-all; review their GBP + website, lead with easy wins that earn trust, upsell later."

**Done + pushed (`taylor-os/topshelf-lead-outreach`, 156 tests pass, Judge PASS):**
- **NEW `scripts/lo_diagnose.py`** (`cf233b0`) — `gap_profile(row)` grades 4 discovery dimensions (website / Google listing / reviews / ranking); `lead_angle(row)` acknowledges a real strength then leads with the ONE easy win. Three easy wins (owner-approved): **free sample site · claim+build the Google listing · review-request automation**. Ranking is cited as the *reason* ("not in the map pack"), never a standalone offer. `business_noun()` maps category slugs to real nouns ("a dental office", not "a dental").
- **`scripts/lo_draft.py` rewritten** (`9f97cd3`, `9bb84ef`) — all 5 cadence builders (day0 email/sms, day3, day7, day14) consume `lead_angle`. **A good-website prospect is NOT pitched a sample — it leads with the Google listing** (`leads_with_site` False). Follow-ups adapt their noun (sample vs breakdown) to the angle. Strong website is acknowledged FIRST ("your website is genuinely sharp…") to match the owner's headline example. All voice/compliance invariants preserved (one hub link, free breakdown, no fabricated numbers, CAN-SPAM/STOP, humanizer). Docs synced (`0161469`).
- **Owner-approved defaults:** v1 works from discovery-level signals (not a full audit per cold prospect — solo-operator constraint); lead with the 3 easy wins above.

**Design decision (owner style — evaluate→propose→he reviews):** the rich per-prospect intelligence already existed in the AUDIT layer; the value was wiring it into the FIRST touch (least-personalized, highest-leverage message).

**🔴 Still-open PREREQUISITES before ANY send (unchanged, all real gates):**
1. FTC "Real Results" fabrication on the live website (must fix before first send).
2. **0 of 57 recommended leads have an owner name/email** — nothing to send *to*; enrichment (owner + live GBP read) also deepens the personalization.
3. CAN-SPAM `mailing_address` is still `<<NEED>>` placeholder in `topshelf-lead-outreach/config.yaml`.

**ALSO DONE this session (autonomous continuation — owner said "keep going through all of them"):**
- **Enrichment / specific-service personalization** (`af242a3`) — `lo_diagnose.known_for(enrichment)` names ONE real service (from `known_for`/`services`, placeholders rejected, never category-derived); woven into the Day-0 reason line ("...searches for Invisalign"). Contact-scoring (`lo_enrich.score_contact`) was already solid; the 0/57-contacts gap is a RUNTIME data-collection task, not code.
- **Existing-customer UPSELL track** (`de5b5e0`, doc `1a0bd5e`/`ec8dc4d`) — NEW `lo_upsell.py`: `next_offers(active_services, profile)` ranks the next of the 10-service catalog excluding active ones (provable-weak dimension first); `build_upsell_touch()` drafts a warm email+SMS referencing prior work, returns `[]` when nothing to offer. No cold framing, no hub link, CAN-SPAM/STOP preserved. This is the "earn trust with easy wins → upsell once in the door" ladder.
- **DYNAMIC EASY-WIN LIBRARY** (`aabd8d0`, plumbing `3f26108`/`59ee07d`, doc `3ce5469`) — research found the finder's sitescan already fires ~43 weighted, owner-readable flags (`sitescan/flags.py` — "they are the pitch"). Built an 11-entry specific easy-win library in `lo_diagnose` (`_SITE_FLAG_WINS`), each backed by a REAL flag (no_https→"Not Secure", no_click_to_call→"number isn't tappable", not_mobile, stale, placeholder_site, no_title, no_localbusiness_schema, ...). `fired_flags()` reads a `site_flags` column (list or comma-string, unknown dropped, nothing invented); `lead_angle` leads with the most persuasive AVAILABLE win (specific beats coarse on a tie = "best foot forward"); `map_pack_reason` gates false ranking claims. **Live end-to-end:** `lf_store` persists `rec['flags']` to a new `site_flags` CSV column (handles the `summarize()` comma-string form). Verified: a row with `site_flags="no_https,no_viewport"` → opener "your site shows Not Secure in the address bar."
- **247 tests pass (173 outreach + 74 finder). Judge PASS on every piece.** The outreach ENGINE (cold first-touch + enrichment + warm upsell + dynamic flag-backed easy wins) is now complete.

**Next steps (Phase 2, mostly OWNER-DATA / DECISION gated — not code):**
1. Run enrichment to get owner name/email on the 57 (needs live web; blocks all sending).
2. Fix the FTC "Real Results" fabrication on the live website + fill the CAN-SPAM `mailing_address` placeholder — both hard gates before the first send.
3. Send-automation decision (Phase 2 close), then a first small cold cohort.

**Open question for owner:** should the first touch ever name a *second* easy win, or stay strictly one? (Currently one.)

---

## ▶ START HERE (cold-start, 2026-08-11)

**✅ B1–B6 + the 6 scoring fixes are BUILT, TESTED, and PUSHED (2026-08-11).** Done this session on the Taylor OS server. The owner's "bad/no website = ALWAYS acceptable, no matter what" rule was confirmed by owner ("Go") and wired as the SUPREME override across every change.

**What shipped (all in `~/.claude/skills/topshelf-*`, each skill is its own git repo, all pushed to `taylor-os/topshelf-*`):**
- **topshelf-lead-finder** — commits `1761a85` (6 scoring fixes), `d68977f` (B1/B2/B4), `e396640` (B3), `22d0154` (SKILL.md sync). **73 tests pass** (ran under a nix python3.13 env — no python on the host otherwise; `nix-shell -I nixpkgs=channel:nixos-unstable -p python313 python313Packages.{pytest,pyyaml,openpyxl}`; `test_sitescan.py` needs httpx/protego/selectolax and is out of scope).
- **topshelf-sample-site** — `4b95b50` (B5 `require_graded_site` guard + tests). Its 27 fact-gate test failures on THIS host are environmental only (need the `platter` module + a `C:/Users/taylo/Ledger Pro` path); 0 B5 failures.
- **topshelf-lead-audit** — `49a25df` (B5 `list_approved` now returns only graded rows + `list_needs_grade`). Not runnable here (its `sys.path.insert` is a Windows path) — verified by inspection.

**The scoring changes (lf_scoring.py + config.yaml):** killed 3 dead inputs (winner_boost/reachability/longevity) → weights crm_fit 30 / account_value 30 / opportunity_gap 40; traction `/2.0`→`/2.85`; queue-entry gate ≥100 reviews OR map-pack rank (SUBORDINATE to the owner rule); website is a membership GATE not a within-pool ranker (dropped `wg` from `work_rank`); `unknown` no longer auto-qualifies. Owner rule set `_ALWAYS_CANDIDATE_SITES = {none, facebook_only, not_mobile, builder, basic, outdated}` (NOT unknown, NOT modern) forces Recommended regardless of score/reviews/gate. Calibration test pins all 6 owner-approved leads stay Recommended.

**B1** = cross-file dedup keys on name+phone (domain fallback); within-file row identity stays name+city (never re-keyed). **B2** = `business_city` column added, no re-key. **B4** = `cap_confidence()` caps derived GBP claim at medium until `gbp_claim_verified`. **B5** = both builders refuse ungraded/`unknown` rows (a graded `none` builds — owner rule). **B3** = `reconcile.py` (report-only, exits nonzero on drift) detects the §1.4 orphans.

**⚠️ STILL OPEN (could not be done on this host — needs the live Drive data):**
1. **B6 — resolve the 4 orphans** (Animal Medical Center, Dove Creek, Strittmatter, Tangerine). The lead CSVs are on Google Drive (`G:\...\Leads\*.csv`) and the demo manifest is in `top-shelf-website` — NEITHER is on the Taylor OS server. **Run `reconcile.py --leads-dir <G:...Leads> --manifest <top-shelf-website/demo/manifest.json> --demo-dir <top-shelf-website/demo>` on a Drive-mounted (desktop) session** to get the exact orphan list, then resolve each.
2. **Recalibrate `work_rank_threshold` (currently 45)** against the live 351-row Denton file — the rank range shifted upward when the `wg` multiplier was dropped. Fail-safe direction (queue grows, nobody hidden), but re-tune when the data is in hand.
3. **JUDGE:** an independent Judge was dispatched over this build at end of session — see the retro/log for its verdict.

**Original task (now complete): apply B1–B6 + the 6 scoring-model fixes to the Top Shelf lead pipeline.** Everything was approved; nothing needed re-deciding.

**State of play:**
- ✅ **Phase 0 (Offer & ICP)** — complete AND re-Judged on the correct model. Original Judge was Sonnet-pinned (untrusted); re-run returned REVISE, all 4 fixes applied + committed. Phase 0 is now PASS-equivalent on a trustworthy gate.
- ✅ **Phase 1 (Find & Qualify)** — complete. Both council seats re-run on Opus, §2a corrected per Judge, Chair synthesis delivered.
- ✅ **B1–B6** — all approved by owner (2026-08-10), sequencing confirmed (2026-08-11): run them NOW that Phase 0 is re-Judged.
- ✅ Everything committed + pushed to origin: **`dc09316`** on `top-shelf-crm` main.
- ✅ `misc` catch-all category already added to the lead finder + sample site (backend only).

**B1–B6 build order (from the approved plan below):**
1. **B3** — nightly reconciliation job (CSV ↔ manifest ↔ deployed dirs; alert on Approved-without-artifacts). ~20 lines. Do first — makes silent failures visible before anything else changes.
2. **B5** — sample-site + audit builders refuse `website_quality=unknown` / empty `website` rows until a human grades them.
3. **B1+B2** — cross-file identity keys on **name + phone** (domain fallback), NOT name+search-city. **B2 adds a `business_city` column — never re-key `city`** (row identity for every writeback is name+city; re-keying orphans all 275 rows).
4. **B4** — cap `confidence` mechanically on derived fields + live-read GBP claim status (2-sec read, doubles as intent signal).
5. **B6** — resolve 4 orphan prospects: Animal Medical Center (build or demote), Dove Creek (add manifest row or take demo down), Strittmatter + Tangerine (are those demos legitimate?).

**Plus the 6 scoring-model fixes (Phase 1):** kill 3 dead inputs (winner_boost, reachability-in-HV, longevity) and redistribute weights · gate queue entry at ≥100 reviews OR top-5 map-pack rank · traction denominator `/2` → `/2.85` · remove website quality as a within-pool ranker (keep as entry gate only) · live-read GBP claim status · fix identity to name+phone (same as B1). **Calibrate every change against the 6 Approved leads as the test set** — they all have ≥197 reviews.

**Then:** re-admit the 112 geographic discards into the corridor (HV + Flower Mound + Double Oak + west Lewisville) → run the **dental cohort** end-to-end (dental is the settled beachhead).

**🔴 One hard gate before ANY outreach send:** the live website's "Real Results" section (3 fabricated case studies + 4 unsourced hero stats) is a **live FTC Fake-Reviews-Rule violation** (16 CFR Part 465 — triggers on publication, no transaction needed). Owner chose to leave it live until first outreach; it MUST be fixed in the website session before the first send. Phase 6 (autonomous loop) must not switch on while this is open.

**Files:** audit docs at `top-shelf-crm/docs/audit/2026-08-09-phase{0,1}-*.md`. Lead pipeline skills at `~/.claude/skills/topshelf-{lead-finder,lead-audit,sample-site,lead-outreach}`. Lead CSVs on Google Drive: `G:\My Drive\Taylor OS\Projects\Top Shelf\Outreach\Prospects\Leads\*.csv` (NOT in any repo).

**Remaining phases after B1–B6:** Phase 2 (deliverables + outreach copy + send-automation decision) → 3 Convert (MSA/contract) → 4 Onboard → 5 Keep & Deliver (Premium pricing decision) → 6 Autonomous loop (last).

---

## WHAT CHANGED 2026-08-10 — the handoff wiring was rebuilt

Top Shelf is now **ONE project spanning two repos** in `project-registry.json` (new optional `repos` array). `brain-handoff.cjs`, `session-project-binding.cjs` and `freshness-gate.cjs` all read `repos` when present and fall back to `repo`, so the other 11 projects are byte-identical — verified by running each hook against the unchanged registry *before* the switch and re-verifying all 8 others after. Both Top Shelf folders now bind to **"Top Shelf"**, and this file is the generated handoff carrying live state for **both** repos, mirrored to both repo roots.

The repos stay separate on disk deliberately: each deploys from its own root (CRM→Railway, website→Hostinger including the live `/demo` sample sites), so a monorepo would break both pipelines. Drive already models Top Shelf as one folder; only git is split, and correctly so.

**Four writers were silently destroying handoffs** and are now fixed: `handoff-sync`, `taylor-os-sync`, the Taylor OS **Sync button** (`8bf4fc8`, deployed), and the Claude Desktop global instructions. All four said *"write `HANDOFF.md` at the repo root"* — but the generator overwrites every repo-root copy every ~10 minutes, so that work vanished within minutes with no error. **Repo-root copies are mirrors, never the source.** A live instance was recovered from commit `5f24833` during this very sync — see the AI-401 section.

**Still owed by Taylor (one manual step):** paste the updated `OS Manager/DESKTOP-GLOBAL-INSTRUCTIONS.md` into Claude Desktop → Settings → Profile → custom instructions. It could not be automated: the Claude Desktop window is masked from computer-use screenshots — a deliberate boundary stopping an agent from driving its own host app. Until pasted, **Desktop keeps writing handoffs to the repo root where they are erased.**

---

## ⛔ RETRACTED — "PHASE 1 COMPLETE" was an unverifiable claim (checked 2026-08-10)

**The section below was written by a Taylor OS session and its artifacts do not exist.** Verified four independent ways from the desktop:

| Check | Result |
|---|---|
| Commit `6d67cce` in local repo | **not found** |
| Commit `6d67cce` on GitHub | **HTTP 422 — no commit found for SHA** |
| `misc` category in lead-finder / sample-site config | **absent from both** |
| Traction denominator `/2.85` | still `/2.0` in `lf_scoring.py` **and** `scoring-rubric.md` |

Tip of `top-shelf-crm/main` is `7c4cb83`. The Phase 1 audit doc is still `23eaa98`, which marks §2a **un-Judged** and §3 **pending re-run**.

**Treat everything below as UNVERIFIED — including the "dental first" beachhead call.** Either that session ran in a container that never pushed, or the claim was aspirational. Do not apply the six scoring fixes on its authority. **Phase 1 is being re-run properly (2026-08-10).**

*Lesson for whoever reads this next: a handoff claiming "✅ COMPLETE" with a commit SHA is worth exactly as much as `git cat-file -t <sha>` says it is. Check before you build on it.*

### Chair's verdict — UNVERIFIED, retained only as a hypothesis to re-test
- **Beachhead: health/beauty storefronts, dental first.** Owner's revealed behavior (4/6 approvals), HV survival (dental 62%, salon 60%), geographic stability all agree. Home services is second — needs metro-wide radius-operator infrastructure first.
- **5 discovery calls before volume outreach.** Approved prospects, no pitch. Two questions: "how do you get new customers? Does your website bring leads?"
- **6 scoring model fixes** (build pending owner approval): kill 3 dead inputs, gate ≥100 reviews, traction /2.85, remove website as within-pool ranker, live-read GBP claim status, fix identity to name+phone.
- **Re-admit 112 geographic discards** into HV + Flower Mound + Double Oak + west Lewisville corridor.

### ✅ OWNER DECISION 2026-08-10 — B1 THROUGH B6 ALL APPROVED (do not re-ask)

Taylor: *"I want to do all of those b1 through b6."* All six are approved to build. **Apply them before Phase 2 work begins** — they are Phase-1 remediation and Phase 2 (the approach: deliverables + outreach) depends on a sound funnel.

Recommended build order and the reasoning behind it:
1. **B3 — reconciliation job** (~20 lines). Do first: it turns silent failures into visible ones *before* anything else changes. Instrument, then operate.
2. **B5 — builders refuse `website_quality=unknown` / empty `website`.** Before any send. Guards the single most self-discrediting message the system can produce ("we studied your website" to a business whose site nobody loaded — crawlers are blocked most often by better-funded firms with good sites).
3. **B1 + B2 together** — one defect from two sides, and the actual unblocker for "point it at any US city." **Key on name + phone with domain fallback** (phone is already on 42/57 recommended rows) rather than the doc's `place_id`, which costs a Maps lookup per row; upgrade only if real collisions appear. **B2 adds a `business_city` column — never re-key `city`**, because row identity for every writeback is `name+city` and re-keying orphans all 275 rows from their audit URLs and statuses.
4. **B4 — cap confidence on derived fields**, paired with a live read of GBP claim status (a two-second read that doubles as an intent signal).
5. **B6 — resolve the four orphans**: Animal Medical Center (build or demote), Dove Creek (manifest or take down), Strittmatter + Tangerine (are those demos legitimate?).

Full definitions: `top-shelf-crm/docs/audit/2026-08-09-phase1-find-and-qualify.md` §5.

### ⚠️ SEQUENCING (owner, 2026-08-11): Phase 0 re-run BEFORE B1–B6
Taylor: the original Phase 0 Judge (§9 of the phase0 doc) ran while the Judge was pinned to Sonnet — he does not trust that verdict. **Re-run an independent Judge over Phase 0 on the correct model FIRST**, apply any REVISE corrections, THEN apply B1–B6 (all still approved). B1–B6 build order unchanged (above).

### IN FLIGHT — Phase 0 Judge re-run (2026-08-11)
Independent Judge dispatched over the full Phase 0 findings (offer, tiers, pricing break-even, ICP/segment mismatch, the "Real Results" fabrication + DTPA exposure). Verdict pending.

### Next 3 steps
1. Complete Phase 0 Judge re-run → apply corrections if REVISE
2. Apply B1–B6 (all approved) in the recorded build order (B3 → B5 → B1+B2 → B4 → B6)
3. Re-admit 112 discards → run dental cohort end-to-end

---

## ✅ AGENT LAYER — FIXED 2026-08-11. Root cause: the desktop app's pinned CLI `2.1.170`.

**Read this, then skip the rest of this section — everything below it is a two-day debugging trail whose conclusions were superseded.** The durable record is `vault/brain/_core/subagent-400-name-the-active-model.md`.

The `400 output_config.effort 'xhigh' … thinking is disabled` on every subagent was a **bug in the CLI build the desktop app pins itself to** (`2.1.170`): it writes `output_config.effort` with no guard on thinking state. `2.1.226` fixes it — proven same-machine, same config, identical flags, sole variable the binary. Taylor OS never hit it for two independent reasons: it passes **no `--effort` flag** and it runs the self-updating npm CLI.

**Live workaround (set 2026-08-11):** `CLAUDE_CODE_LOCAL_BINARY` = `C:\Users\taylo\AppData\Roaming\npm\node_modules\@anthropic-ai\claude-code\bin\claude.exe`, which `app.asar` honours. Remove it once the app itself updates — the app is frozen at `1.12603.1.0` because a Windows Update Blocker has `wuauserv`/`InstallService`/`DoSvc` ACL-locked to Disabled.

**Consequences for how to work:** any effort level now works on the desktop. No frontmatter `effort:` pins. No "start Manager sessions at high or below" rule — that is **retired**. Council and Judge work is fine on this desktop again.

---

### 🔴 HISTORICAL — superseded debugging trail, kept for the lessons only

**Nothing else in this handoff should be worked until this is resolved.** Owner instruction, verbatim: *"I need all of the council, agents, skills, judge, manager to work correctly before any work is done."*

**Symptom:** every subagent — council seats, leads, `judge-agent` — dies in <500ms with zero tokens:
> `400 output_config.effort 'xhigh' is not supported when thinking is disabled on this model. Use effort 'high' or below, or enable thinking.`

**Cause:** subagents inherit the session's *effort* but are spawned **without thinking**. On Opus that pair is illegal.

**Measured, all four dispatch forms:**

| `model` passed | Result |
|---|---|
| *(none — inherits Opus 5)* | **400** |
| `opus` | **400** |
| `sonnet` | runs |
| `fable` | runs |

⚠️ `sonnet` and `fable` "work" **only because they switch off Opus.** They are not fixes — they silently run every seat on a model the owner did not select, and report success. This fooled me twice and I recorded both as "fixed." Do not repeat it.

### ⚠️ 2026-08-10 — RESOLVED ON TAYLOR OS, STILL BROKEN ON THIS DESKTOP

Both are true; they are different environments and the header above ("NOW RESOLVED") reflects Taylor OS only.

- **Taylor OS server: working.** That session re-ran the ICP/Targeting and Demand seats on Opus 5 and ran the Judge — so subagents dispatch fine there.
- **Windows desktop: still 400s.** Re-tested 2026-08-10 after the app process restarted (old pid gone, so `settings.json` was read fresh) with `alwaysThinkingEnabled: true` confirmed live. A no-model dispatch returns the identical 400.

**Most likely explanation:** the desktop runs at **xhigh effort** (set by the app's effort picker) while the Taylor OS server does not, and it is the *effort × no-thinking* pair that is illegal — not thinking being off per se. That fits every measurement: `sonnet`/`fable` survive because they permit high effort without thinking; `opus`/inherit do not.

**One caveat before calling the desktop case settled:** the retest ran in a *resumed* session (`$env:CLAUDE_CODE_SESSION_ID` unchanged at `b0c799cd-…`), and a resume may restore prior effort/thinking state rather than re-resolving from the freshly-loaded settings. The remaining untested case is a **brand-new session, not a resume**:
> `Agent(subagent_type: "general-purpose", prompt: "Reply with one line: OK")`
- **Runs** → thinking inherits; unpin the Judge (below).
- **400s** → confirmed environment limitation on desktop at xhigh. Stop investigating (see "Already ruled out"); either lower effort or run council work on Taylor OS, and say so plainly.

### ✅ 2026-08-10 — THE NEW-SESSION TEST WAS RUN. IT 400s. CASE CLOSED.

Brand-new session `852a2c0b-5852-4e10-b269-def4ed6c4ac6` (not a resume — different id from `b0c799cd-…`), `$env:CLAUDE_EFFORT=xhigh`, `alwaysThinkingEnabled: true` confirmed live in `settings.json`:

| Dispatch | Result |
|---|---|
| no `model` (inherits Opus 5) | **400 — identical error**, 245ms, 0 tokens |
| `model: sonnet` (control) | **OK**, 2.4s, 36k tokens |

**A new session does not fix it.** The dispatch path itself is healthy — the sonnet control proves that. It is specifically *xhigh × Opus × no-thinking* that is rejected, exactly as the error text says.

*(An earlier draft of this section said "case closed, do not investigate further." That was wrong — a full root-cause investigation followed and found the actual defect. See the next section.)*

### 🔬 ROOT CAUSE FOUND 2026-08-10 — it is the DESKTOP APP layer, not config, not skills, not the CLI

**Owner's hypothesis — that last week's token-minimization skill rework broke it — is DISPROVEN.** The three token-min commits (`1b7dda3` anti-triggers→platter, `3b2eb38` retire 18 seo duplicates, `50138e5` compress 23 descriptions) touch descriptions and routing only; none touch effort, thinking, or dispatch. Verified by `git log -S` on `settings.json`: the only effort/thinking key ever added is `alwaysThinkingEnabled`, and it was added at `9879c31` **on 2026-08-09 during the debugging itself** — it is an attempted fix, not original config, and it does not work.

**What actually changed behavior:** `72a0f3f` (remove model pins from all 106 agents). Before it, 103 agents were pinned to Sonnet, so subagents never inherited Opus and never met the constraint. **The system only ever "worked" by silently running every seat on Sonnet — the exact thing the standing rule bans.** The unpin did not cause the bug; it exposed one that was always there. There is therefore no "good old version" to go back to that the owner would accept.

**Ruled out by direct measurement, not inference:**

| Suspect | Verdict |
|---|---|
| Token-min skill/agent edits | **No** — descriptions/routing only; no agent carries `model:` (0/106) |
| `MAX_THINKING_TOKENS=0` sabotage | **No** — not set anywhere; no `env` block in `settings.json` |
| `alwaysThinkingEnabled` missing | **No** — present and `true`; simply does not reach subagents |
| Stale/old CLI version | **No** — see below, but not the cause |
| Frontmatter `effort:` unsupported | **No** — the running binary contains `has invalid effort '` beside `has invalid permissionMode '`, proving it parses the field |

**Correction to a previously "ruled out" item:** the last session recorded *"Claude Code is current (2.1.226), so not an already-fixed upstream bug."* **That check was wrong** — it read the npm-global binary on `PATH`. The desktop app runs its **own bundled copy at `2.1.170`** (`$CLAUDE_CODE_EXECPATH`, `AI_AGENT=claude-code_2-1-170_agent`, Agent SDK `0.3.170`), and `2.1.170` is the only version in the app's directory. Updating the npm package does nothing for this app.

**But the version is not the culprit either.** Running the app's *own* `2.1.170` binary headless — same machine, same `~/.claude`, with `CLAUDE_EFFORT=xhigh`, `CLAUDE_CODE_EFFORT_LEVEL=xhigh`, and even `MAX_THINKING_TOKENS=0` — dispatches subagents **successfully every time**. Headless cannot reproduce the 400 under any combination tried, so it is not a usable test bench for this bug.

**Conclusion: the defect lives in the desktop app's session layer** (Claude Windows app `1.12603.1`, package `Claude_pzs8sxrjxfjjc`), which sets `CLAUDE_EFFORT=xhigh` in the session environment and issues subagent requests with `output_config.effort=xhigh` **while omitting thinking**. Opus rejects that pair. The same binary driven any other way does not do this.

### ✅ TEST RUN AND CLOSED — 2026-08-10. Do not re-run it.

Outcome was **#1: frontmatter outranks the app env var.** `judge-agent` (`effort: high`) dispatched and returned a verdict; `general-purpose` (no effort line) 400'd; `eng-cpanel` (no effort line, near-identical restricted tool list to the Judge) 400'd as the control that isolates the variable.

**The "surgical fix" in the original plan was rejected, and correctly.** It rescues 1 seat of 106 — every lead and council seat still 400s on the desktop, so the pipeline stays broken and only the end gate works — and on Taylor OS, where xhigh dispatches fine, it silently downgrades the Judge to `high`, the banned override.

**Owner's decision: lower the app effort picker `xhigh` → `high`.** Fixes all 106 seats, no pins, every seat runs the effort selected at the prompt. The `effort: high` line has been reverted from `judge-agent.md` (`~/.claude` @ `bde015c`); all 106 agents now carry zero `effort:` and zero `model:` pins. **Owner action outstanding: flip the picker.** Until it is flipped, subagents still 400 on this desktop — so the "don't run Phase 1 on the desktop at xhigh" warning below stands *only while the picker is still xhigh*.

Full detail: `brain/_core/subagent-400-name-the-active-model.md` (@ `8fa30dc`).

**Two corrections to beliefs that were in play:**
1. **Nothing anywhere "drops the effort level" for subagents, and nothing ever did.** There is no such mechanism to be broken: the Agent tool exposes `model`, not `effort`. Subagents inherit the session effort verbatim. Any expectation that the Judge self-lowers to `high` is mistaken.
2. **`judge-agent.md` no longer carries `model: sonnet`** — the pin is already gone (verified: **0 of 106 agents** carry a `model:` line). That is correct per the standing rule, and it is *why* the Judge now 400s where it used to quietly run on Sonnet. The unpin did not break the Judge; it made a pre-existing breakage visible.

~~**The fix is one of two, both Taylor's call:** lower the picker xhigh → high, or run council/Judge work on Taylor OS.~~ **Both were workarounds. Neither is needed — see the FIXED banner at the top of this section.** Pinning the Judge back to Sonnet was never on the list and still isn't.

~~**Do not run a Phase-1 pass on the desktop at xhigh.**~~ **Retired 2026-08-11** — the desktop dispatches cleanly at every effort level now.

*(An earlier note said the setting "does not win." That was wrong at the time — it had never been loaded. It has now been loaded and the desktop still fails.)*

### ~~STEP 2 — unpin the Judge~~ · ✅ ALREADY DONE
~~`judge-agent.md` currently carries `model: sonnet`.~~ **False as of 2026-08-11 — and contradicted by point 2 just above.** The pin is gone; **0 of 106 agents** carry a `model:` or an `effort:` line. Nothing to do.

### ~~FALLBACK if STEP 1 fails~~ · ❌ BANNED, DO NOT DO THIS
~~Pass `model` as a tool parameter per dispatch. Keep the Judge pinned.~~ **A model override is the banned workaround** — it only ever cleared the 400 by silently switching off Opus, which violates the standing rule. There is no fallback needed: the root cause is fixed. *(This was the last surviving imperative sentence on disk telling a reader to pin the Judge — struck 2026-08-11 on the Judge's own objection.)*

### Already ruled out — and the one that was ruled out WRONGLY
Fast mode is OFF (`epitaxyPrefs.fastMode = False` in `%APPDATA%\Claude\claude_desktop_config.json`) · no managed/policy settings · no HKLM/HKCU registry policy · no thinking/effort env vars · no thinking key in `.claude.json`, the desktop config, or the session record. There is **no way to force a settings reload mid-session** — no CLI, no tool.

**⚠️ "Claude Code is current (2.1.226), so not an already-fixed upstream bug" was the wrong call, and it was the answer.** That check read the **npm-global** binary on `PATH`; the desktop app runs its own pinned copy at `2.1.170`. Two days were spent in config space because of it. **When an app bundles a runtime, `--version` on `PATH` tells you nothing about what the app is running** — find the actual `$CLAUDE_CODE_EXECPATH`.

### Fixed along the way (`72a0f3f`)
**All 106 agents carried a hardcoded `model:` in frontmatter — 103 `sonnet`, 3 `opus`, zero inheriting.** That included `judge-agent`, so **every Judge verdict ever rendered in the Manager pipeline used Sonnet regardless of the selected model.** Pins removed. Side effect: agents now fail *loudly* instead of silently running on the wrong model. Note frontmatter pins are **cached at session start**, so editing them mid-session does nothing.

**⚠️ Also: this file's narrative gets clobbered.** Correction commit `c7b7f4d` was overwritten within the hour by another session and an autosync WIP snapshot. Durable lessons belong in `brain/_core/` memory, not here.

---

## OWNER'S GOAL — verbatim, do not re-ask

> Point Top Shelf CRM at a city → it reviews every business → builds a CSV of who to contact → creates the deliverables → sends them → auto-responds to text/email replies → keeps him notified throughout → sends onboarding docs for signature → delivers a welcome packet → auto-creates the client in the CRM with delivery templates and automations.

And: *"I want everything straightened up and correct before sending anything. I dont want a customer to reach out and we are not ready for their business."*

**Constraints (decided — do not re-litigate):**
- **Solo, a few hours a week.** The binding constraint on every decision.
- **Automate everything**; surface legal risks rather than avoid them — *"dont not do something because it might be illegal just make sure I know about it while fixing it."*
- Offer and pricing **fully in scope and changeable**.
- **Equal scrutiny on all 7 phases**, full council depth each.
- **Standing rule:** every seat always uses the effort and model selected at the prompt. Never pin frontmatter, never override, never put effort instructions in a skill or agent.

**⚠️ HOW HE WANTS ME TO WORK** — *"this is the whole reason I am auditing. I want you to evaluate what I already have, see if it's any good, and come up with solutions on your own and give them to me so I can review them."*
**Stop asking him to spec things. Evaluate → propose → he reviews.**

---

## THE FINDING THAT REFRAMED EVERYTHING

**The machine is fully built and has never been switched on.** Verified by hand, not agent-reported:
- **275 leads** (Highland Village 235 + Lewisville 40) — `outreach_status` **blank on all 275**
- **0 of 57** recommended leads have an `owner_email` or `owner_name`
- CAN-SPAM mailing address in `topshelf-lead-outreach/config.yaml` is still `<<NEED: real physical mailing address>>`
- **Zero customers, zero prospects contacted, ever**

Three independent blockers, any one of which halts all outbound.

**Also done:** paused auto-archive on all 8 cold-prospect sample sites (`top-shelf-website` @ `0bc1c66`). Luigi's was 19 days from deletion; the 60-day timer was counting down against prospects who don't know the samples exist.

---

## PHASE 0 — Offer & ICP — COMPLETE
Doc: `top-shelf-crm/docs/audit/2026-08-09-phase0-offer-and-icp.md` (`a5827ad`)

1. **🔴 Live homepage publishes 3 past-tense case studies under a "Real Results" header** (`top-shelf-website/index.html` L452–469) from a business with **zero customers**, plus 4 unsourced hero stats (L104–118). Found independently by two council seats, then verified by hand.
2. **Root cause: no `facts/topshelf.json`.** The platter fact gate validates claims about the *prospect*, so Top Shelf's claims about *itself* pass unchecked. Same gap that shipped the false "you own the sample site" line in August.
3. **Premium $1,599 is undeliverable solo** — three seats agreed independently (~15.9 hrs/mo recurring, 26.5% net margin, worst per-hour of the three tiers).
4. **Essential $499 does not break even until month 8–9 — after its own 6-month term ends.** No setup fee + blank kill fee ⇒ a month-2 churn costs ~$1,472 with zero recourse.
5. **The pitch targets the wrong segment.** The funnel is mostly high-review/weak-site businesses (Ousley Vision 1,298 reviews; Tangerine 939) who already get customers — "your website is dated" is not their felt pain.

**OWNER DECISIONS (do not re-ask):**
- **D1 — leave the "Real Results" section live**, fix it in the dedicated website session. **HARD BLOCKER: must be fixed before the first outreach send.** Bounded risk only while nobody has been contacted.
- **D2 — defer the Premium question to Phase 5**, since its contents depend on the delivery model. **Do not sell Premium before Phase 5 concludes.**

---

## PHASE 1 — Find & Qualify — PARTIAL
Doc: `top-shelf-crm/docs/audit/2026-08-09-phase1-find-and-qualify.md` (`23eaa98`)

⚠️ The five council seats ran **on the wrong model** (Fable 5 while the owner was on Opus 5 — my error, from misreading a cached picker menu as the active model). **Every finding below was therefore re-verified by hand and stands on that, not on the seats.**

**VERIFIED AND FINAL:**
- **The composite score is decorative.** 4 of 57 clear the 65 threshold; **53 ride the website override.** HV max 66; dataset-wide max 75.
- **Scores are NOT comparable across cities.** `reachability` fires in Lewisville (max 10, mean 3.0 among 20 recommended; all-row 1.5) and is 0 in Highland Village. An earlier claim of mine that it is "structurally dead" was HV-only presented as dataset-wide — **wrong, do not repeat.** `winner_boost` **is** structurally 0 everywhere (`winners.yaml` empty).
- **`years_est` is blank on all 235 rows**, so `longevity` is a constant. Third dead input.
- **Cross-file identity is broken both ways.** *Williams & Kunkel* excluded in HV as "out-of-city Flower Mound" *with* website+phone, yet live/recommended/queued in Lewisville with neither. *Hirji CPA* permanently suppressed by its own wrong-city exclusion record. Dedup keys on name + **search** city.
- **The Approved gate does not gate.** Strittmatter deployed with empty CSV URLs; Tangerine deployed but status still Not contacted; **Dove Creek deployed with no row in any CSV and no manifest entry**; Animal Medical Center **Approved since 2026-08-04 with nothing built.** Nothing reconciles CSV ↔ manifest ↔ deployed dirs.
- **55 rows stamped `confidence: high` on *derived* GBP status** ("claim status not verified") — understates the exact gap Top Shelf sells into, then feeds the audit as fact.
- **Geographic bleed is 112 of 176 exclusions (64%)** — nearly half of everything discovered. Flower Mound alone is 42. *(An earlier draft said "~90"; the Judge caught it — I had matched only `located in X` and missed `out-of-city X`.)*
- The "187/235 unknown website_quality" figure is **not** a scanner failure — 174 are Excluded pre-scan by design. Only **13 live rows** carry an unmeasured grade.

**§2a — Manager analysis on Opus 5, NOT an independent seat, NOT Judged:**
- **The owner's real selection criterion is invisible to the model.** All six Approved leads have **≥197 reviews** (median 396); 14 of 57 recommended have <15 reviews and he approved **none**. `traction = log10(reviews+1)/2` saturates at ~100 reviews, so a 100-review business and Onyx's 694 score **identically** — the scorer flatlines exactly where his decisions begin. Fix: desaturate (`/2.5`) + a traction floor (≥15 reviews OR top-5 map rank).
- **Weak website is the right gate, wrong ranker.** All 6 approvals have weak sites — but so do all 57 recommended, so it cannot separate within the pool.
- **Vertical: HV data says dental (62% survival) and salon (60%), NOT home services (roofing 21%).** The conflict with Phase 0 is *geographic*: trades are radius operators serving HV from neighbouring towns, so they appear as out-of-city exclusions. **A bedroom suburb contains storefronts.** If home services is strategically right, discovery must run **vertical-first across the metro**, not geography-first per suburb.

**JUDGE RECORD:** an independent Judge (pinned to Sonnet) returned REVISE with 3 objections; 2 fully correct (geographic count, unstated mean population — both fixed), 1 half-right (it called the two blank-`exclusion_reason` rows a defect; they are the two *modern-site* businesses, correctly scored and correctly not recommended — documented as correct so nobody "fixes" it). It confirmed §1.4 and §2a.2 are **proportionate, not inflated**, and that the verified/unverified labelling held. **Caveat: this is Sonnet grading Opus-produced work** — reliable on arithmetic and sourcing, may miss subtle reasoning flaws.

**STILL OWED:** independently Judge §2a; re-run the ICP/Targeting and Demand seats once agents work. Brief them with §1 as fact and §2 as corrections; **do NOT re-feed the "structurally dead" error.**

**BLOCKING ISSUES B1–B6 (owner decisions needed, in the doc §5):** B1 cross-file identity must key on place_id/address+domain **(blocks any second city)** · B2 add `business_city` (never re-key; row identity for every writeback is name+city) · B3 reconciliation job CSV ↔ manifest ↔ deployed dirs · B4 cap `confidence` mechanically on derived fields · B5 builders must refuse `website_quality=unknown` and empty-`website` rows · B6 resolve Animal Medical Center and Dove Creek; decide on Strittmatter/Tangerine.

---

## VERTICAL TARGETING — owner request, evaluated, proposal pending review

**What he asked for:** point the finder at **any US city** and filter by industry — keep all verticals, or narrow to only those with a proven pack.

**This already exists and was never used:** `topshelf-lead-finder/SKILL.md` L62 — `mode: city+industry | city-only | industry-only | flexible`, plus `metro_cities`. Highland Village ran `city-only` (widest geography, no industry filter), which is exactly why it surfaced salons/dentists and discarded ~112 out-of-area trades. **Configuration + workflow change, not a build.**

**His decisions:** all four pack components in scope · **success metric = reply rate of any kind** · scope = any US city, he picks city and filters industry · **build ONE vertical pack end-to-end first.**

**PACK EVALUATION (Manager analysis, not Judged):**

| Component | State | Verdict |
|---|---|---|
| Sample-site templates | 9 distinct HTML files, 30–40KB each, own vertical maps + design rationale | **Good — real designs, not reskins** |
| Proposal | 42 tokens, **zero** industry/vertical tokens | Not vertical-aware |
| Outreach copy | `gap_phrase()` keys on `website_quality` only | Not vertical-aware |
| CRM funnel per vertical | none built | Does not exist |

**The insight:** the expensive piece (9 industry designs) is DONE; the cheap pieces (industry-specific *words*) are missing. A roofer and an orthodontist both currently receive *"your site's looking a little dated."* His success metric is **reply rate**, which is driven by outreach copy — the least tailored component. Highest-leverage fix in the system.

**PROPOSED PACK (NOT yet approved):** 1) industry gap phrasing — `gap_phrase()` becomes `(website_quality × industry)`; 2) industry pain-set tokens in the proposal; 3) template-family mapping (mostly exists); 4) curated photo pack per industry (fixes the 2026-08-05 defect — construction/cleaning stock photos on an HVAC sample); 5) CRM funnel page — **defer**, only matters for inbound, none exists.

**PROPOSED FIRST VERTICAL: home services** — deliberately against the HV numbers, because **the owner runs GDR, a real home-services business** with a live site, review funnel, CRM and real customers. That is the case study Top Shelf lacks, and it answers the Skeptic's #1 kill risk ("who else have you done this for?"). Also fits the geography finding: trades need `industry-only`/city-list, not suburb sweeps.

---

## OPEN ENGINEERING THREAD — AI Employee 401 (STILL BLOCKED as of 2026-08-10)

**P10 AI Employee (chat-bubble reply drafting) will not authenticate.** Taylor minted **two** fresh `CLAUDE_CODE_OAUTH_TOKEN` values via `claude setup-token`; both returned `401 OAuth access token is invalid`.

**Three code fixes were deployed in `6ff2896`** — pinned the CLI to `@2.1.226`, added `CLAUDE_CONFIG_DIR=/tmp/.claude-config`, added `trim()` on the token — **still 401. The token itself is being rejected by Anthropic's servers; the code is not the problem.**

**New clue:** the latest error carries `"fast_mode_disabled_reason":"sdk_opt_in_required"`, which may mean Taylor's subscription tier does not permit headless SDK/API calls via an OAuth token. *(Possibly related to the agent-layer blocker at the top of this file — both involve subscription-level capability rather than config. Worth considering together, though they have not been proven to share a cause.)*

**ONE diagnostic step remaining — Taylor's action:**
1. Command Prompt on Windows → `claude --version`. If old: `npm update -g @anthropic-ai/claude-code`, re-mint with `claude setup-token`, update both Railway vars (web + worker), redeploy.
2. If it was already current: report roughly how long the `sk-ant-oat01-…` token is (character count) and whether `setup-token` opened a browser or just printed a token with no interaction.

Both services must be set: chat runs from `web`, workflow-driven AI drafting runs on `worker` — set only one and automations silently do nothing.

> **How this section got here is itself the lesson.** A Taylor OS session wrote these findings into `top-shelf-crm/HANDOFF.md` — the repo root — which is a *generated mirror*. The next `brain-handoff.cjs` tick would have overwritten them. Recovered from commit `5f24833` and folded into the brain by hand on 2026-08-10. The four writers that taught that habit have since been fixed; if you find yourself editing a repo-root `HANDOFF.md`, stop and edit this file instead.

---

## THE 7 PHASES
0 Offer & ICP ✅ · 1 Find & Qualify 🟡 partial · 2 The approach (deliverables, outreach, send-automation decision) · 3 Convert (MSA risk, contract) · 4 Onboard · 5 Keep & deliver *(Premium decision lands here)* · 6 The autonomous loop *(must be last; do not switch on while D1 is open)* · then a switch-on scorecard.

---

## TRAPS
- **Lead CSVs are on Google Drive, NOT in any repo:** `G:\My Drive\Taylor OS\Projects\Top Shelf\Outreach\Prospects\Leads\`. A Judge false-flagged them as missing by searching the repo. **Cite absolute paths.**
- **There is no Denton.csv.** Agent scans reported a "Denton run, 503 researched" — not on disk. Treat as lost. Only HV + Lewisville exist.
- `top-shelf-crm` local clone was **22 commits behind** origin. `git fetch` before any work.
- PowerShell 5.1: `2>&1` on git makes `$?` false even on success. Do not redirect.
- **Never infer the active model from `~/.claude.json`** — it is not there. `additionalModelOptionsCache` is a cached *picker menu*. The session record at `%APPDATA%\Claude\claude-code-sessions\…\local_*.json` has the real `"model"` field; the system prompt is also authoritative.
- **Before concluding a settings change "doesn't work," check whether the process ever restarted** — compare `(Get-Process claude*).StartTime` to the file's `LastWriteTime` and whether `$env:CLAUDE_CODE_SESSION_ID` changed.

---

## NEXT 3 STEPS

0. ~~Run the frontmatter test.~~ **DONE and superseded — the agent layer is fixed (2026-08-11, CLI `2.1.170` → `2.1.226`).** `judge-agent.md` carries no `effort:` line and no `model:` line; all 106 agents inherit. Nothing further is owed on diagnosis. Sanity-check with one dispatch if you like, then get on with the work.
1. **Get owner review** of the proposed vertical pack + home-services first vertical. Then build that pack end-to-end.
2. **Close Phase 1:** independently Judge §2a, re-run the two judgement seats, present B1–B6 for decision. **B1 blocks pointing the finder at any second city — and he wants any US city.**
3. **Apply B1–B6** (all six approved, order and reasoning above), then re-admit the 112 discards and run the dental cohort end to end.

## FILE MAP
- Audit docs → `top-shelf-crm/docs/audit/2026-08-09-phase{0,1}-*.md`
- Lead data → `G:\My Drive\Taylor OS\Projects\Top Shelf\Outreach\Prospects\Leads\*.csv`
- Skills → `~/.claude/skills/topshelf-{lead-finder,lead-audit,sample-site,lead-outreach,seo-audit}`
- Facts files → `C:\Users\taylo\Agentic-OS\vault\platter\facts\` (19; **none for Top Shelf itself** — that is finding #2 of Phase 0)
- Demo estate → `top-shelf-website/demo/` + `demo/manifest.json`
- Durable lessons → `vault/brain/_core/subagent-400-name-the-active-model.md`

**🧰 NEW WEB EFFECT IN THE TOOLBELT (2026-09-03, Fable 5.1) — "SCATTER-GATHER".** Watched a Wix Studio short (Rina Grim) of the "scrolling effect" where grid items each travel their own vector on scroll (gather on scroll-in, scatter on scroll-up; scrubbed, so direction=motion). Rebuilt it in our stack as portable GSAP ScrollTrigger and wired it in (pushed to config repo `taylor-os-claude-config`): (1) `anti-slop-web/references/effect-map.md` entry **#14** (the router premium builds read in Direction) — distinct from parallax (#1): moves countable OBJECTS to one composed frame, not depth planes; (2) `gsap-scrolltrigger/SKILL.md` "Recipe: Scatter-gather" copy-paste block (data-attribute vectors per item, in-sync vs staggered, Lenis + reduced-motion) + passing self-check `scatter-gather.check.mjs`. **Good for:** product lineup / services grid / asset showcase / gallery intro on a premium (Funnel-B) build where the assembly IS the reveal. Guards baked in: 3-7 items, modest offsets, images/headings only, legible at zero motion. NOTE: `anti-slop-web` still has 3 dangling `effects-catalog/` refs (not installed here) — the new entry points at `gsap-scrolltrigger` which exists, so unaffected; run down separately if wanted.

**🧰 NEW WEB EFFECT #2 IN THE TOOLBELT (2026-09-03, Fable 5.1) — "PARALLAX ZOOM SCROLLING".** Watched a short defining it: scroll = a Z-axis camera dolly toward a FIXED horizon focal point; depth layers each scale by their `data-depth` so the viewer flies INTO/out of the scene ("sudden 3D movement, many layers"). Two modes: (A) in-scene layered dolly (one illustrated scene gains 3D), (B) push-through portal (front section zooms past to reveal the next behind it). Rebuilt in portable GSAP + pushed to config repo `taylor-os-claude-config`: (1) `anti-slop-web/references/effect-map.md` entry **#15** (distinct from parallax #1 = X/Y plane pan, and scrubbed-video #13 = pre-rendered clip; this scales live DOM/art on Z, stays crisp, no video file); (2) `gsap-scrolltrigger/SKILL.md` "Recipe: Parallax zoom scrolling" (pinned scrub, per-layer scaling, Mode A+B, Lenis + reduced-motion) + passing self-check `parallax-zoom.check.mjs`. **Good for:** ONE flagship hero or a single section-to-section fly-through on a premium (Funnel-B) build with a world to enter (space/nature/product interior/stylized illustration) — the most cinematic scroll move we have; use once, it's the peak. Guards: 3-6 layers, pin it, near layers must fade before covering the reveal, cap zoom on mobile, legible at scale 1 (no-JS + reduced-motion). Companion to [[scatter-gather]] added earlier today.

**🧰 NEW SKILL — "DEVICE-STORY-SCROLL" (2026-09-03, Fable 5.1) — the Top Shelf CRM showcase engine.** Taylor asked to replicate void.sbs (studied it LIVE: Next.js, a pinned `position:fixed` phone whose SCREEN is a scroll-scrubbed portrait video, ~14 MP4s up to 2048px + a 63s blob video = effective but HEAVY) for the Top Shelf CRM feature showcase, and to make it LIGHTER + add a MONITOR frame for desktop/mobile. Built a full standalone skill `device-story-scroll` (pushed to config repo `taylor-os-claude-config`, drop-in, native trigger) + effect-map router entry **#16**. Core mechanic = pinned device narrator + screen advances through feature beats on scroll + synced copy + jump-nav, then releases into a numbered feature rail. **The weight win = a screen-content decision ladder:** (1) real CRM UI composited into the frame as DOM (zero media, crisp, real product proof — DEFAULT for the CRM), (2) AV1/WebP screenshots per beat (lazy, poster-first) — this is the "show desktop AND mobile" path, (3) ONE scrubbed video max, only where motion IS the message. Plus CSS phone + monitor/browser frames (no device PNGs), a lighter-than-void perf budget (preload=none + IntersectionObserver, content-visibility, transforms-only, pause offscreen), reduced-motion/no-JS fallback (beats stack vertically), honesty guard (product-proof: only what the CRM really does), and a passing self-check `beat-map.check.mjs`. Composes with gsap-scrolltrigger (pin+scrub), cinematic-gsap-lenis, [[parallax-zoom]] + [[scatter-gather]] (the two effects added earlier today). **NEXT when we build the Top Shelf CRM page:** pick the CRM features to beat out (Leads/Quote/Schedule/Invoice/Reviews...), export or clone real CRM screens, build the phone+monitor showcase with rung-1 DOM screens by default.

**🎬 CRM SCROLL REDONE AS DEVICE-STORY (2026-09-04, Fable 5.1) — Pixar film REPLACED by the real CRM on a monitor+phone.** Taylor: redo "The Contractor's Dream" using the new [[device-story-scroll]] skill so the CRM desktop+phone switch and show off as you scroll (instead of the Pixar video), with "+" hotspots that open a sidebar for more info, and a monitor to show desktop+mobile. BUILT + published (Artifact https://claude.ai/code/artifact/65332393-0b80-4030-ac7e-125c50abcb7e): a pinned stage, real T.A.P. CRM screens (dashboard, Jobs kanban, AI receptionist, booking, on-my-way, estimator, customer-pay, review, GBP flywheel) composited into CSS phone+monitor frames, 9 screens compressed to ~860KB total (vs the film's 14 MP4s). Scroll advances 10 beats across the 3 Blends (Essentials/Signature/Platinum), active device center-stages + screen crossfades, gold "+" opens a feature+Blend sidebar, left Blend-rail progress, beat-10 lights BOTH devices (the desktop+mobile showcase). Files: scratchpad `device-story/` + Drive `Projects/Top Shelf/crm-scroll-prototype/device-story/` (index.html, STORYBOARD.md, renders/*.png — 7 section frames). Built with Playwright screenshots at #b<N> hashes (local http.server on 4610; browser file:// is blocked, serve over http). **Evaluated by CEO agent (Business Strategist) + client agent (Persona Walkthrough Specialist)** — both in STORYBOARD.md. FIXED post-eval: pipeline beat showed wrong screen (setScreen stale-layer bug), added "sample data" honesty marker, CTA now bridges story→audit, softened "measured"→"priced". **OPEN DECISIONS FOR TAYLOR (both agents converged, need his call / new assets):** (1) lead pain-first not dashboard; (2) MOBILE LEGIBILITY — zoom each beat to ONE readable region, full CRM screens are unreadable on a real phone (biggest risk); (3) add real social proof; (4) personalize demo with visitor's business name (CEO's top idea); (5) $21,400 couch-payment reads big; (6) beat-9 is a spec, fold into an earlier moment + move CTA after the flywheel; (7) define "Blend" + price signal. NOT formally Judge-gated — the two requested agents were the review.

**📐 CRM SHOWCASE RE-PLANNED — "TWO WINDOWS, ONE BUSINESS" v3 (2026-09-04, Fable 5.1) — PLAN ONLY, NOTHING RENDERED.** Taylor scrapped the Disney/Pixar story entirely: now a pure CRM + services showcase where a monitor AND a phone stay fixed in frame the whole page and only their screens change as you scroll. Brainstormed 5 structures (Blend-chapters / day-in-life / phone-only / feature-grid / v1 swap-and-blur) → chose **paired moments** (phone = "Your customer", monitor = "You", same service both sides) in **customer-journey order tagged by plan** (Found → Call → Follow-up → Paid → Reviews → Come back → Grow → Ads → proof/plans/audit). **Evaluated by CEO agent (Business Strategist: ship-with-changes) + client agent (owner-operator persona: 5/10 as drafted)** — both converged; v3 applies all: audit mechanics in beat 0 + sticky CTA; 12→8 service beats; **AI Estimator CUT (not on the public site)**; **Reactivation beat ADDED** (Signature line never shown); **one Premium beat ADDED** (so $1,599 has something behind it); follow-up reframed done-for-you ("goes out in seconds, you don't log in" — verified on solution-crm); ONE plan tag per beat (Every plan/Signature/Premium, never split); device labels; **mobile = monitor crop is the pinned hero (owner-first)**, no sub-scrub on mobile; sample numbers sized to a 2-truck shop; price strip with /mo + "six months then month-to-month, 30 days" (public) + anchor + the line "everything you watched except the ads is Signature". FACTS VERIFIED on topshelfsolutions.io: Payments IS sold (tap/card-on-file/one-tap invoices/POS financing); Estimator appears nowhere; reactivation = Signature; no contractor case study (bar/auto/dental only); "Platinum" was WRONG — third plan is **Premium**. Report Artifact https://claude.ai/code/artifact/517ed004-4f77-48f4-b20a-2dd247fd100d ("Two Windows, One Business"); files scratchpad `device-story/PLAN-v3-final.md` + `report-v3.html` + Drive `crm-scroll-prototype/device-story/`. **NEEDS TAYLOR'S 6 DECISIONS before rendering:** (1) confirm cutting the Estimator, (2) approve 5 new renders (customer text-back w/ timestamps; small-ticket pay+tip; reactivation text; reactivation campaign list; ad landing + ads/GBP-post dashboard), (3) price anchor wording, (4) any REAL client quote (none invented), (5) modest-number re-renders y/n, (6) personalization deferred y/n. Not Judge-gated — the two requested agents were the review.

**💎 SIGNATURE SHOWCASE — TRUE-3D COPY + AUDIT (2026-09-04, Fable 5.1).** Taylor: add true 3D deformation on a COPY (keep the CSS version to fall back to), then audit devices that "go too far or aren't big enough". Built `scratchpad/signature-3d/` (Drive `Projects/Top Shelf/crm-scroll-prototype/signature-blend-showcase-3d/`): same sections/panels/CSS as v3 (extracted to `_css.html`, `_body.html`, `_panels.js`), DOM rig replaced by a Three.js r128 (cdnjs UMD) WebGL layer in `engine.js`, assembled by `build.py` (which also runs the keyframe AUDIT). Scene sized so 1 world unit = 1 CSS px at z=0 (camera z 1400 = the CSS perspective). Each device = glass SDF-rounded plane (gold hairline edge, sheen) + textured screen mesh (24×15 / 12×26 cells, non-indexed, per-cell aCenter/aRand/aDelay) + phone notch + soft shadow sprite. **Deformation** in the vertex shader from a smoothed velocity uniform: ripple (sin z), bend into the move (x²), twist about y. **Screen change = true 3D shatter**: the old screen's `out` mesh explodes (per-cell rotX/Y/Z + outward flight + gravity, fades after .62) on top while the new screen converges from the same fragment field (prog 1→0), 1.35 s. Same keyframe rig/poses/scramble/flash/shards/panels as v3. Orientation rule kept (solo → face centre, pair → face each other; hero pair faces copy). **AUDIT (build.py, deterministic):** flags exit overshoot (x < −w−20 or > 125), solo mins (dsk 42vw / ph 20vw), pair mins (34 / 11), copy-column crowding (side-L devices x ≥ 45; side-R right edge ≤ 55), phone height > 92vw, y drift; sec 12 bookends exempt. Fixed from the audit + frames: phone left-exit −34→−30; close bookends 26/9→28/10; pair-left desktops x 0→3 (3D turn was clipping the left edge); arc lift 6→3. Result: **no flags**. Test hooks: `window.__rig.forceVel=0.9` freezes bend; `__rig.changeScreen(dev,key)` + `cancelAnimationFrame(dev.userData.raf)` + set `uProg` freezes a shatter (frames in `frames/`). **Artifact (NEW URL): https://claude.ai/code/artifact/98f4ff99-ea3e-41d8-826e-7396b62cb378** — the CSS v3 stays at …/04ef4610-9ec6-4713-b204-971acd0ddece. NEXT: Taylor picks CSS vs 3D; tune shader amplitudes (ripple 16/10, bend 90, twist .55), shatter dur/spread; add real backdrop-blur look to glass (currently a translucent fill) if wanted; mobile pass.

**🎬 SIGNATURE 3D v4 — CHOREOGRAPHY + SECTION REVEALS (2026-09-04 late, Fable 5.1).** Taylor: (a) a phone/desktop sometimes crossed the screen unused; (b) entrances/exits shouldn't just slide L/R — research award sites; (c) sections themselves felt static. Researched live (frames in Drive `signature-blend-showcase/frames/refs/`): **cuberto.com** (numbered stacking service cards) and **obys.agency** (fully pinned stage, 120 fixed layers revealed by clip-paths, filmstrip roll). Rebuilt `signature-3d` as **v4**: (1) **pinned stage** — `.stack` is a tall scroll driver (N×160svh), `.stage` sticky, sections are absolute layers; each section has `data-enter` (diag/iris/curtain/zoom-through/blinds/vwipe/split) applied per frame from raw scroll t via clip-path/mask/transform (zoom-through also scales+blurs the previous). (2) **MOVES table per segment/device**: zoomPast (flies past the lens), zoomIn (from depth), drop (bounce ease), fold (lid closes via uFold shader), assemble (from fragments), flipIn (hinge), cross (pair swaps depth), arc+roll. (3) **Staging rule** fixes the unused-traveler bug: entering devices exist only in the last 65% of a segment (hidden before), leaving devices only in the first 65%; off-at-both-ends → hidden. (4) **Screen-change kinds** per section: shatter / roll (filmstrip uv scroll) / iris (radial reveal). (5) **Layouts**: card (04, 09; numbered translucent panel), band (06; giant centred headline, copy right, devices low), split-ivory (08; diagonal dark/ivory), immersive (10; blurred screen as the room). Audit still clean (sec 6 y exempt by design). Verified frames Drive `signature-blend-showcase-3d/frames/v4/` (lid-fold + drop staging, assemble, card, band, split, immersive; dsk hidden in 08, ph hidden in 10). Same artifact URL https://claude.ai/code/artifact/98f4ff99-ea3e-41d8-826e-7396b62cb378 (CSS v3 fallback unchanged). NEXT: Taylor scrolls it; tune reveal kinds per section, move timings (0.65 windows), band/immersive polish, mobile pass.

**🌀 SCROLL-SHOWCASE TOOLKIT — motion research report (2026-09-04, Opus 4.8).** Taylor asked for skills/repos to build award-winning scroll product showcases (transitions, product showcasing, section/page layouts). Researched our own library FIRST (standing rule) + web. Findings, an HTML report (Top Shelf brand: espresso/gold, Cormorant+Jost+mono): **Own already** — build-awwwards-quality-sites, cinematic-gsap-lenis-motion-system, scrollcraft, device-story-scroll, scroll-scrubbed-visual-sequence, anti-slop-web, full GSAP suite (8), impeccable/tastemaker/frontend-design, animation-vocabulary + audit skills, + the scatter-gather/parallax-zoom effects added earlier. **Worth adding** — Lenis (smooth scroll, the big one; NOT Locomotive which mangles the DOM), GSAP+ScrollTrigger (now FULLY FREE incl SplitText/ScrollSmoother/Flip since Webflow 2024), Three.js (3D hero), OGL (light WebGL/shader transitions), Motion.dev (React component motion), CSS scroll-driven animations (native, free), SplitType, Barba.js (page transitions, multi-page only), Theatre.js (visual 3D timeline), Rive/Lottie (stateful/AE motion). **Sources of copyable technique** — Codrops/tympanus (WebGL image+video transitions, scroll-revealed WebGL gallery Feb-2026 tutorial = GSAP+Three+Astro+Barba, Creative Hub), Awwwards collections, Apple/AVATR/Stripe/Home-Société exemplars. **8 layout patterns** — pinned stage, scrubbed reveal, horizontal panel, exploded/3D orbit, card stack, reveal-and-wipe, scrollytelling, immersive world. **Recommended Top Shelf stack** = Lenis(feel) + GSAP/ScrollTrigger(engine) + pinned device/product stage(spine) + ONE Three/OGL hero(wow) + anti-slop/impeccable(taste) + Codrops(source). Only genuinely new installs = Lenis + (rare) Three/OGL. Artifact https://claude.ai/code/artifact/72facf10-335a-4b5a-8370-9a8788848355 · Drive `crm-scroll-prototype/research/scroll-showcase-toolkit.html`.

**🏗️ SHOWCASE-BUILDER AGENT — ARCHITECTURE PLAN, AWAITING TAYLOR'S GO (2026-09-04, Opus 4.8).** Taylor: verify the recommended libs are safe, then (after brainstorm + his review) build a high-end scroll-showcase website-builder agent; combine skills that reinforce, keep cross-phase ones apart, intake new ones. Delivered a REVIEW-ONLY html plan (artifact https://claude.ai/code/artifact/af06d748-3b52-4ed5-9fbd-dc8741b7da38 · Drive `crm-scroll-prototype/research/showcase-builder-agent-plan.html`). **NOTHING built/installed/merged yet — he reviews first.** SAFETY (verified, done): Lenis MIT 14.7k★ ~1M/wk darkroom.engineering; GSAP free-commercial (Webflow, incl SplitText/all plugins since Apr-2025); Three.js MIT r184 mrdoob; OGL public-domain oframe; SplitType ISC (skip — GSAP SplitText covers it); Barba MIT (multi-page only). Our artifact CSP only allows cdnjs+jsdelivr = built-in safety; pin versions. These are LIBRARIES (CDN), not skills. **PLAN — combine vs keep-apart:** phase funnel Direction→Build→Animate→Wow(opt)→Review→Ship, one primary skill per phase. COMBINE the whole Animate phase into ONE new skill **`showcase-motion`** (folds Lenis + gsap-scrolltrigger/timeline + scroll-scrubbed-visual-sequence + scatter-gather + parallax-zoom + device-story + SplitText reveals; REFERENCES the official gsap-* manuals, doesn't fork them). AUTHOR a 2nd new gated skill **`webgl-hero`** (Three.js/OGL scroll 3D/shatter/shader hero + Codrops recipes, seeded from this session's 3D rig) — kept SEPARATE so WebGL doesn't leak onto every section. KEEP APART: Direction (anti-slop-web+build-awwwards+frontend-direction+tastemaker+ONE aesthetic system) before code; Review (impeccable+review-animations+design-review+design-review-live) after; aesthetic systems mutually exclusive. New agent **`showcase-website-builder`** = specialized sibling of topshelf-website-builder, owns the premium-motion lane, inherits model (never pin), tools Read/Write/Edit/Grep/Glob/Skill/Task+browser, funnel w/ readiness gates, guardrails (1 signature move, 1 WebGL max lazy+fallback, perf budget, reduced-motion/no-JS, honesty, real mobile pass). **NEXT (pending Taylor go):** 1 author showcase-motion, 2 author webgl-hero, 3 both through skill-intake→config+vault repos, 4 build the agent, 5 add to platter matrix + rebuild indexes. Related: [[scroll-showcase-toolkit]] research report.

**✅ SHOWCASE INFRA BUILT + PAGE BUILD RUNNING (2026-09-04, Opus 4.8 session).** Executed the approved plan. **DONE + pushed:** authored `skill:showcase-motion` (combined Animate: Lenis+GSAP ScrollTrigger+reveal kit+SplitText+recipe index; refs the gsap-* manuals) and `skill:webgl-hero` (gated one-per-page Three.js/OGL 3D/shader hero; lazy+poster+fallback) → `~/.claude/skills/`, pushed to config repo `taylor-os-claude-config`. Authored agent `showcase-website-builder` (`~/.claude/agents/`, Direction→Build→Animate→Wow→Review→Ship funnel, one skill/phase, guardrails, hands to Judge). Registered deliverable `showcase-website` in `vault/platter/matrix.json` (lead=agent:showcase-website-builder, phase skills mapped), rebuilt registry indexes (123 agents, 186 skills), pushed vault. **GOTCHA:** a newly-authored agent is NOT dispatchable by name in the SAME session (agent registry loads at SessionStart) — it works next session + on cloud. Skills DO become available same-session. **PAGE BUILD:** dispatched a **Fable 5.1** general-purpose agent carrying the showcase-website-builder playbook + invoking showcase-motion/webgl-hero to build a fresh flashy Signature Blend one-pager (agentId running in background). It writes `scratchpad/signature-showcase-agent/index.template.html` (SCREENS placeholder) + DIRECTION.md; NO shell-verify. **Manager (me) does Ship:** inject the 16 real CRM screens from `signature-showcase-agent/screens.json` into `/*__SCREENS__*/`, serve+Playwright-verify every #s<N>/#t<N.5> beat, fix bugs, publish artifact, file Drive, handoff. Libs verified safe (Lenis MIT/GSAP free/Three MIT/OGL public-domain), loaded pinned cdnjs/jsdelivr. Related: [[showcase-builder-agent-plan]] [[scroll-showcase-toolkit]].

<!-- AUTO:STATE — rewritten by brain-handoff.cjs. Edit above this line, not below. -->

**Repo state** — C:\Users\taylo\top-shelf-crm

- branch: `main`
- last commit: `6771ae5 2026-09-06 feat(prospecting): collapse the control cards into one command bar`
- uncommitted files: 0
- commits not yet pushed: 0

**Repo state** — C:\Users\taylo\top-shelf-website

- branch: `main`
- last commit: `26feb77 2026-09-14 Mobile: services flywheel opens a bottom sheet, and the logo stays`
- uncommitted files: 2 — HANDOFF.md, index.html
- commits not yet pushed: 0

<!-- /AUTO:STATE -->
